---
phase: 01-foundation-contracts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/DEMO-SCRIPT.md
autonomous: false
requirements:
  - ARCH-04
tags:
  - documentation
  - demo
  - narrative

must_haves:
  truths:
    - "`docs/DEMO-SCRIPT.md` exists and is a single readable narrative covering the new-team onboarding scenario from team creation to dashboard inventory view (D-16)"
    - "The document is written in flowing prose with embedded `pecp` commands and bracketed `[expected output]` placeholders — leads with story, commands inline (D-13, D-14)"
    - "Every CLI command in the script is a v1-roadmap command that will exist by Phase 5 (`pecp team create`, `pecp apply -f`, `pecp create awsaccount`, `pecp status awsaccount --watch`, `pecp get`, `pecp deployments`)"
    - "The script contains at least one `[expected output]` placeholder per command block (D-15)"
    - "A human reviewer (the user) signs off that the narrative matches the intended stakeholder session flow"
  artifacts:
    - path: "docs/DEMO-SCRIPT.md"
      provides: "Stakeholder-ready narrative walkthrough of new-team onboarding (ARCH-04)"
      contains: "pecp apply"
      min_lines: 80
  key_links:
    - from: "docs/DEMO-SCRIPT.md"
      to: ".planning/REQUIREMENTS.md (CLI-01 through CLI-10)"
      via: "Every pecp command in the script must trace to a v1 CLI requirement"
      pattern: "pecp (apply|get|delete|status|team|projects|deployments|create)"
---

<objective>
Write the stakeholder-ready demo script that captures the new-team onboarding scenario end-to-end — the narrative locked in CONTEXT.md D-16. This document drives Phase 5 acceptance: the same narrative becomes the live demo, with `[expected output]` placeholders replaced by real terminal output as the features land. Writing the script NOW (before any provisioning code per ARCH-04) forces the contract authors to build what the demo needs, not what they imagine.

Purpose: ARCH-04 — "Demo script (narrative walkthrough) is written before any implementation begins". This is the only way to keep the build aligned with the eventual stakeholder session. Per D-13, the audience is mixed (engineers + stakeholders) so the document leads with narrative and embeds commands inline.

Output: A single readable markdown document at `docs/DEMO-SCRIPT.md` that a stakeholder could read top-to-bottom in one sitting, with every `pecp` invocation a real Phase-3 / Phase-5 CLI command and every result represented by an `[expected output: ...]` placeholder until Phase 5 fills it in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-contracts/01-RESEARCH.md
@example.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/DEMO-SCRIPT.md narrative walkthrough</name>
  <files>docs/DEMO-SCRIPT.md</files>
  <read_first>
    - .planning/phases/01-foundation-contracts/01-CONTEXT.md (D-13, D-14, D-15, D-16 — format and content rules)
    - .planning/REQUIREMENTS.md (CLI-01 through CLI-11, TEAM-01 through TEAM-03, CTRL-01 through CTRL-04, UI-01, UI-02 — the script may only invoke commands defined in v1 requirements)
    - .planning/ROADMAP.md (Phase 5 success criteria — the script's narrative must align with the live demo flow)
    - example.yaml (canonical YAML wire format — use this exact shape for any inline YAML samples in the script)
    - CLAUDE.md (`pecp apply` invocation pattern; resource spec format `apiVersion: pecp/v1`, `kind: PECP<Type>`)
  </read_first>
  <action>
    Create `docs/DEMO-SCRIPT.md`. Structure as a single flowing narrative with these chapters as `##` headings (no numbered prefixes, narrative voice throughout): (1) "Setting the scene" — one paragraph introducing the new-team onboarding scenario per D-16, naming the team `payments`, identifying the speaker as a PE team member; (2) "Day one: a team is born" — narrative paragraph + `pecp team create payments` command block + `[expected output: team created confirmation with team id and metadata]` placeholder, then `pecp team payments` showing members/roles placeholder (cites CLI-05, CLI-06); (3) "Day two: deploying a lambda" — narrative paragraph introducing the Lambda use case + an inline YAML code-fenced block matching example.yaml structure exactly (apiVersion: pecp/v1, kind: PECPLambda, metadata.name, spec with name/exposure/api-gateway/source-code) + `pecp apply -f payments-lambda.yaml --team payments` command + `[expected output: resource id, status: pending, dispatched to AWS Lambda adapter]` placeholder + `pecp status PECPLambda payments-lambda --team payments --watch` command + `[expected output: status transitions pending → provisioning → ready with synthetic activity log lines]` placeholder (cites CLI-01, CLI-04, CTRL-01); (4) "Day three: the long-running ask — a new AWS account" — narrative paragraph explaining the slow path + `pecp create awsaccount --team payments` command + `[expected output: resource id, status: pending]` placeholder + `pecp status awsaccount --team payments --watch` command + `[expected output: stays in provisioning ~3+ seconds, PE notes appear mid-stream (e.g. "approval ticket filed"), eventually transitions to ready with synthetic account id and access keys]` placeholder (cites CLI-09, CLI-10, KINDS-04, CTRL-04); (5) "Day four: the full inventory" — narrative paragraph framing the dashboard view + `pecp get PECPLambda --team payments` and `pecp deployments --team payments --environment prod` command blocks each with `[expected output: rich table with name, kind, status badge, environment]` placeholders + a final paragraph stating "open the React dashboard at http://localhost:5173" with `[expected output: same data as table, filterable by environment]` placeholder (cites CLI-02, CLI-08, UI-01, UI-02); (6) "What we proved" — short closing paragraph mapping the narrative back to PECP's core value proposition from CLAUDE.md ("zero to provisioned infrastructure by writing a YAML and running pecp apply — without knowing which AWS account they're in"). The full document MUST be at least 80 lines of markdown, MUST use `[expected output: ...]` placeholders (D-15 — Phase 5 replaces these with real captured output), MUST embed every `pecp` command in fenced code blocks with `bash` language hint, MUST contain at least one inline YAML fenced block (with `yaml` language hint) showing the example resource spec format, and MUST NOT reference any v2 features from REQUIREMENTS.md (no JWT auth flow, no real backend, no PE approval flow for team creation, no UI form submission). Do not include section numbering (`1.`, `2.`) — use narrative chapter titles only per D-13/D-14.
  </action>
  <verify>
    <automated>cd /Users/punitlad/projects/pecp-poc && test -f docs/DEMO-SCRIPT.md && wc -l docs/DEMO-SCRIPT.md | awk '{print $1}' | (read n; [ "$n" -ge 80 ] && echo "LINES $n >= 80" || (echo "FAIL: $n lines"; exit 1)) && grep -c "\\[expected output" docs/DEMO-SCRIPT.md | (read n; [ "$n" -ge 6 ] && echo "PLACEHOLDERS $n >= 6" || (echo "FAIL: $n placeholders"; exit 1)) && grep -c "pecp " docs/DEMO-SCRIPT.md | (read n; [ "$n" -ge 6 ] && echo "PECP_CMDS $n >= 6" || (echo "FAIL: $n pecp cmds"; exit 1)) && grep -q "apiVersion: pecp/v1" docs/DEMO-SCRIPT.md && grep -q "kind: PECPLambda" docs/DEMO-SCRIPT.md && ! grep -i "JWT\\|OAuth" docs/DEMO-SCRIPT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/DEMO-SCRIPT.md` exists and is at least 80 lines long
    - Document contains at least 6 `[expected output` placeholder occurrences (D-15)
    - Document contains at least 6 distinct `pecp <command>` invocations
    - Document contains the literal string `apiVersion: pecp/v1` and `kind: PECPLambda` (inline YAML matches the wire format from example.yaml)
    - Document contains at least one fenced `yaml` code block
    - Document contains zero references to v2-only features: the case-insensitive regex `JWT|OAuth|real backend|approval flow|self-service.*UI` returns no matches
    - Document references the team name `payments` (per Task action specifying D-16 scenario)
    - Document references at least these v1 commands: `pecp team create`, `pecp apply`, `pecp status`, `pecp create awsaccount`, `pecp get`, `pecp deployments`
  </acceptance_criteria>
  <done>
    Demo script reads as a single narrative covering the new-team onboarding scenario from team creation through Lambda deploy, async AWS account request, PE notes update, and dashboard inventory view — with every command runnable in Phase 5 against the live system, every output represented by a placeholder, and zero references to v2 features.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify demo script narrative</name>
  <files>docs/DEMO-SCRIPT.md</files>
  <what-built>A complete `docs/DEMO-SCRIPT.md` narrative walkthrough at the project root, covering the new-team onboarding demo scenario from CONTEXT.md D-16. Written before any provisioning code per ARCH-04 so the contract authors build what the demo needs. The user is the only person who can judge whether the narrative matches the intended stakeholder session flow — this is a document review, not a code check.</what-built>
  <how-to-verify>
    1. Open `docs/DEMO-SCRIPT.md` in your editor and read top to bottom
    2. Confirm it reads as a single story (D-13: narrative drives, commands inline)
    3. Confirm it covers the D-16 scenario: team creation → Lambda apply → async AWS account → PE notes mid-provisioning → dashboard inventory
    4. Confirm every `[expected output: ...]` placeholder is followed by a clear plain-language description of what Phase 5 will replace it with
    5. Confirm the YAML sample matches `example.yaml` structure exactly (no fictional fields)
    6. Confirm no v2 features appear (no JWT, no real backends, no UI form submission)
    7. If any chapter feels incomplete or off-tone, describe the change you want
  </how-to-verify>
  <action>Present the rendered `docs/DEMO-SCRIPT.md` to the user and pause for review per ARCH-04. The executor performs no file edits during this checkpoint — only the user's "approved" signal or revision request advances the plan. If the user requests revisions, hand control back to Task 1 with the specific change list.</action>
  <verify>
    <human-check>User reads `docs/DEMO-SCRIPT.md` and confirms the narrative matches the intended stakeholder session flow per the steps in `<how-to-verify>`.</human-check>
  </verify>
  <done>User has typed "approved" at the checkpoint OR has confirmed all requested revisions from a prior round are addressed.</done>
  <resume-signal>Type "approved" to accept the script as the locked stakeholder narrative, or describe the revisions you want (which chapter, what to change). On "approved" the executor closes the plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan produces a documentation file only. No code execution, no network exposure, no persistent state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-DOC | Information Disclosure | docs/DEMO-SCRIPT.md committed to repo | accept | Script uses synthetic team name `payments` and bracketed `[expected output: ...]` placeholders — no real credentials, no real customer data, no internal infrastructure detail. Phase 5 replaces placeholders with synthetic data from the seed script (ARCH-03), so even live-demo screenshots are safe to commit. |
| T-01-SC | Tampering | npm/pip package installs | mitigate | Plan 02 installs no packages — pure markdown authoring. No supply chain surface. |
</threat_model>

<verification>
1. `test -f docs/DEMO-SCRIPT.md` exits 0
2. `wc -l docs/DEMO-SCRIPT.md` reports >= 80 lines
3. `grep -c "\[expected output" docs/DEMO-SCRIPT.md` reports >= 6
4. `grep -c "pecp " docs/DEMO-SCRIPT.md` reports >= 6
5. `grep -q "apiVersion: pecp/v1" docs/DEMO-SCRIPT.md` exits 0
6. Human reviewer (the user) types "approved" at the Task 2 checkpoint
</verification>

<success_criteria>
- `docs/DEMO-SCRIPT.md` exists, is >= 80 lines, and is human-approved as the stakeholder narrative for Phase 5
- Every `pecp` command in the script is a v1 CLI requirement
- Every result is represented by an `[expected output: ...]` placeholder per D-15
- The YAML inline sample matches example.yaml structure (apiVersion: pecp/v1, kind: PECPLambda, hyphenated keys)
- ROADMAP requirement `ARCH-04` (demo script written before implementation) is satisfied
</success_criteria>

<output>
After human approval, write `.planning/phases/01-foundation-contracts/01-02-SUMMARY.md` covering: the location of the demo script, total line count, number of commands and placeholders, the chapters present, and any revisions requested by the user during the checkpoint with how they were addressed.
</output>
