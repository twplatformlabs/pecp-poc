---
phase: 01-foundation-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .gitignore
  - src/pecp/__init__.py
  - src/pecp/models/__init__.py
  - src/pecp/models/enums.py
  - src/pecp/models/provision_result.py
  - src/pecp/models/resource_spec.py
  - src/pecp/adapters/__init__.py
  - src/pecp/adapters/base.py
  - src/pecp/api/__init__.py
  - src/pecp/api/dependencies.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_models/__init__.py
  - tests/test_models/test_resource_spec.py
  - tests/test_models/test_provision_result.py
  - tests/test_adapters/__init__.py
  - tests/test_adapters/test_adapter_base.py
  - tests/test_api/__init__.py
  - tests/test_api/test_routes.py
autonomous: true
requirements:
  - ARCH-02
  - ADPT-01
tags:
  - python
  - pydantic
  - fastapi
  - scaffolding
  - contracts

must_haves:
  truths:
    - "`pip install -e \".[dev]\"` succeeds against the freshly created pyproject.toml"
    - "`ruff check src/ tests/` exits 0 (zero errors) against the scaffold"
    - "`mypy src/` exits 0 (zero errors) — typed skeleton is complete per D-12 and discriminated union per D-10"
    - "A subclass of `AdapterBase` missing any abstract method raises `TypeError` at instantiation (ADPT-01)"
    - "A fully-implemented `AdapterBase` subclass instantiates without error"
    - "`ResourceSpec.model_validate(yaml.safe_load(example.yaml))` returns a `ResourceSpec` whose `.spec` is an instance of `LambdaSpec` with `source_code == 'github://myorg/lambda-code'` and `api_gateway == '/hello'` (D-10, hyphenated alias round-trip)"
    - "`ProvisionResult(status=ResourceStatus.pending)` returns an instance with `provider_metadata == {}`, `activity_log == []`, `error is None` (D-01, D-02, D-03)"
    - "`get_request_context()` returns a `RequestContext` whose `user_id`, `team_memberships`, `is_pe_admin` are populated (ARCH-02)"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata, deps, ruff/mypy/pytest config (D-07 — single config file)"
      contains: "[tool.ruff]"
    - path: "src/pecp/models/enums.py"
      provides: "ResourceStatus enum (D-12 — single source of truth)"
      contains: "class ResourceStatus"
    - path: "src/pecp/models/provision_result.py"
      provides: "ProvisionResult model (D-01, D-02, D-03)"
      contains: "class ProvisionResult"
    - path: "src/pecp/models/resource_spec.py"
      provides: "Discriminated union ResourceSpec covering all 6 kinds (D-09, D-10, D-11)"
      contains: "discriminator=\"kind\""
    - path: "src/pecp/adapters/base.py"
      provides: "AdapterBase ABC with 3 abstract methods (ADPT-01, D-04)"
      contains: "class AdapterBase(ABC)"
    - path: "src/pecp/api/dependencies.py"
      provides: "RequestContext model + get_request_context Depends() + ContextDep alias (ARCH-02)"
      contains: "ContextDep"
    - path: "tests/conftest.py"
      provides: "Async test fixtures wired for httpx ASGITransport (deferred app import — Plan 03 wires app)"
      contains: "ASGITransport"
    - path: "tests/test_adapters/test_adapter_base.py"
      provides: "ADPT-01 enforcement test (TypeError on incomplete subclass)"
      contains: "pytest.raises(TypeError"
    - path: "tests/test_models/test_resource_spec.py"
      provides: "D-10 discriminated union round-trip test against example.yaml"
      contains: "yaml.safe_load"
    - path: "tests/test_models/test_provision_result.py"
      provides: "D-01/D-02/D-03 ProvisionResult shape test"
      contains: "ProvisionResult"
  key_links:
    - from: "src/pecp/adapters/base.py"
      to: "src/pecp/models/provision_result.py"
      via: "ProvisionResult return type on all 3 abstract methods"
      pattern: "-> ProvisionResult"
    - from: "src/pecp/adapters/base.py"
      to: "src/pecp/models/resource_spec.py"
      via: "ResourceSpec parameter on all 3 abstract methods"
      pattern: "resource: ResourceSpec"
    - from: "src/pecp/models/provision_result.py"
      to: "src/pecp/models/enums.py"
      via: "ResourceStatus import for `status` field"
      pattern: "from pecp.models.enums import ResourceStatus"
    - from: "src/pecp/api/dependencies.py"
      to: "fastapi.Depends"
      via: "ContextDep Annotated alias"
      pattern: "Annotated\\[RequestContext, Depends\\("
---

<objective>
Scaffold the PECP Python project (src layout, pyproject.toml with ruff+mypy+pytest config) and lock all Phase 1 contracts in code: ResourceStatus enum (D-12), ProvisionResult model (D-01/D-02/D-03), Pydantic v2 discriminated union covering all 6 resource kinds (D-09/D-10/D-11), AdapterBase ABC with 3 abstract methods (ADPT-01, D-04), and RequestContext FastAPI dependency stub (ARCH-02). Also create the Wave 0 test scaffolds that prove the contracts. The FastAPI route handlers and live app instance are NOT included here — they are wired in Plan 03 once persistence is in place. Tests in this plan that depend on a running app (e.g., `tests/test_api/test_routes.py`) are created in a RED state with a single explicit `pytest.skip` reason pending Plan 03, EXCEPT the unit-level dependency-injection assertion which uses `get_request_context()` directly (no app needed).

Purpose: Phase 2 mock-adapter authors must build against stable AdapterBase + ResourceSpec types. Phase 3 route authors must build against a stable RequestContext dependency. Locking these now (per CONTEXT.md D-01 through D-12) prevents downstream rework.

Output: A typed Python project skeleton that passes `ruff check`, `mypy --strict`, and `pytest` (with RED skips for Plan 03 endpoints). All 6 resource kinds parseable, ADPT-01 TypeError enforcement provable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-contracts/01-RESEARCH.md
@.planning/phases/01-foundation-contracts/01-PATTERNS.md
@.planning/phases/01-foundation-contracts/01-VALIDATION.md
@CLAUDE.md
@example.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pyproject.toml + .gitignore + empty package layout</name>
  <files>pyproject.toml, .gitignore, src/pecp/__init__.py, src/pecp/models/__init__.py, src/pecp/adapters/__init__.py, src/pecp/api/__init__.py, tests/__init__.py, tests/test_models/__init__.py, tests/test_adapters/__init__.py, tests/test_api/__init__.py</files>
  <read_first>
    - .planning/phases/01-foundation-contracts/01-RESEARCH.md (sections: Standard Stack, Code Examples / pyproject.toml — copy the full block verbatim including [tool.ruff], [tool.mypy], [tool.pydantic-mypy], [tool.pytest.ini_options])
    - .planning/phases/01-foundation-contracts/01-PATTERNS.md (section: pyproject.toml — confirm asyncio_mode = "auto" and pythonpath = ["src"])
    - .planning/phases/01-foundation-contracts/01-CONTEXT.md (D-05, D-06, D-07, D-08)
    - CLAUDE.md (verify "What NOT to Use" list)
    - example.yaml (read raw — wire format reference, not modified)
  </read_first>
  <action>
    Create `pyproject.toml` at the project root copying the exact block from RESEARCH.md "Code Examples / pyproject.toml". Required keys: `[build-system]` using setuptools>=77.0; `[project]` with name="pecp", version="0.1.0", requires-python=">=3.12", dependencies list (fastapi>=0.136, uvicorn[standard]>=0.48, pydantic>=2.13, pyyaml>=6.0, typer>=0.26, rich>=15, httpx>=0.28, python-multipart>=0.0.29, python-dotenv>=1.0); `[project.optional-dependencies]` dev = [pytest>=9.0, pytest-asyncio>=1.4, mypy>=2.1, ruff>=0.15]; `[project.scripts]` pecp = "pecp.cli.main:app" (CLI module created in Plan 03 — entrypoint is registered now so the install step does not need re-running later); `[tool.setuptools.packages.find]` where = ["src"]; `[tool.ruff]` line-length=88, target-version="py312"; `[tool.ruff.lint]` select = ["E4", "E7", "E9", "F", "I"]; `[tool.ruff.format]` quote-style="double"; `[tool.mypy]` python_version="3.12", plugins=["pydantic.mypy"], strict=true, ignore_missing_imports=false; `[[tool.mypy.overrides]]` for module=["yaml"] with ignore_missing_imports=true; `[tool.pydantic-mypy]` init_forbid_extra=true, init_typed=true, warn_required_dynamic_aliases=true; `[tool.pytest.ini_options]` asyncio_mode="auto", pythonpath=["src"], testpaths=["tests"]. Create `.gitignore` with entries: `__pycache__/`, `*.py[cod]`, `.pytest_cache/`, `.mypy_cache/`, `.ruff_cache/`, `dist/`, `build/`, `*.egg-info/`, `.env`, `*.db`, `*.sqlite*`, `.venv/`, `venv/`. Create empty `__init__.py` files for the 8 package and test-package directories listed in `<files>`. Do not create cli/ or api/routes/ packages yet — they are created in Plan 03. After file creation run `pip install -e ".[dev]"` from the project root to verify the manifest installs.
  </action>
  <verify>
    <automated>cd /Users/punitlad/projects/pecp-poc && pip install -e ".[dev]" 2>&1 | tail -5 && python -c "import pecp" && ruff check src/ tests/ && python -c "import grep_check" 2>/dev/null; grep -q "\\[tool.ruff\\]" pyproject.toml && grep -q "asyncio_mode = \"auto\"" pyproject.toml && grep -q "discriminator" pyproject.toml || echo "OK (discriminator only in source files later)"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` exists at `/Users/punitlad/projects/pecp-poc/pyproject.toml` and contains the literal strings `[tool.ruff]`, `[tool.mypy]`, `[tool.pydantic-mypy]`, `[tool.pytest.ini_options]`, `asyncio_mode = "auto"`, `pythonpath = ["src"]`, `pecp = "pecp.cli.main:app"`
    - `pip install -e ".[dev]"` exits 0 and reports `Successfully installed pecp-0.1.0`
    - `python -c "import pecp"` exits 0
    - `python -c "import pecp.models, pecp.adapters, pecp.api"` exits 0 (verifies all three __init__.py files importable)
    - `ruff check src/ tests/` exits 0
    - `.gitignore` exists and contains `*.db`, `__pycache__/`, `.venv/`
    - There is NO `requirements.txt` anywhere in the project (D-07)
  </acceptance_criteria>
  <done>
    Project installs editable, ruff passes against the empty scaffold, and all package directories exist so subsequent tasks can land Python modules into typed sub-packages.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement contracts — enums, ProvisionResult, ResourceSpec discriminated union, AdapterBase ABC, RequestContext dep</name>
  <files>src/pecp/models/enums.py, src/pecp/models/provision_result.py, src/pecp/models/resource_spec.py, src/pecp/adapters/base.py, src/pecp/api/dependencies.py</files>
  <read_first>
    - .planning/phases/01-foundation-contracts/01-PATTERNS.md (sections: enums.py, provision_result.py, resource_spec.py, adapters/base.py, api/dependencies.py — copy the patterns verbatim)
    - .planning/phases/01-foundation-contracts/01-RESEARCH.md (sections: Code Examples / ResourceStatus Enum, ProvisionResult Model, Complete Discriminated Union, AdapterBase ABC, RequestContext Dependency; Pitfalls 3, 5, 6)
    - .planning/phases/01-foundation-contracts/01-CONTEXT.md (D-01, D-02, D-03, D-04, D-09, D-10, D-11, D-12, ARCH-02)
    - example.yaml (must parse without transformation through the discriminated union)
    - pyproject.toml (from Task 1 — confirm mypy plugins and asyncio_mode are present)
  </read_first>
  <behavior>
    - Behavior 1 (D-12): `from pecp.models.enums import ResourceStatus` succeeds; `ResourceStatus.ready.value == "ready"`; the four members are exactly `pending`, `provisioning`, `ready`, `failed` (string enum)
    - Behavior 2 (D-01, D-02, D-03): `ProvisionResult(status=ResourceStatus.pending)` constructs successfully with defaulted empty `provider_metadata={}`, `activity_log=[]`, `error=None`
    - Behavior 3 (D-02): `ProvisionResult(status=ResourceStatus.failed, error="quota exceeded")` constructs and exposes `.error == "quota exceeded"`
    - Behavior 4 (D-10): `ResourceSpec.model_validate(yaml.safe_load(open("example.yaml").read()))` returns a model whose `.spec` is an instance of `LambdaSpec` with `.source_code == "github://myorg/lambda-code"` and `.api_gateway == "/hello"`
    - Behavior 5 (D-10): Mutating example data to set `spec.kind = "InvalidKind"` raises `pydantic.ValidationError` on `model_validate`
    - Behavior 6 (D-10, Pitfall 3): Removing the `source-code` key from a valid example dict raises `pydantic.ValidationError` (per-kind field validation)
    - Behavior 7 (D-09): Constructing `ContainerSpec(kind="PECPContainer", name="x", exposure="public", image="nginx")`, `DataServiceSpec(kind="PECPDataService", name="y", subtype=DataServiceSubtype.s3)`, `AccountSpec(kind="PECPAccount")`, `SalesforceSpec(kind="PECPSalesforce")`, `AemSpec(kind="PECPAem")` all succeed
    - Behavior 8 (ADPT-01): Subclassing `AdapterBase` and omitting `provision` raises `TypeError` containing the substring `provision` when calling `IncompleteAdapter()`
    - Behavior 9 (ADPT-01): Subclassing `AdapterBase` and implementing all three async methods allows `FullAdapter()` instantiation without error
    - Behavior 10 (ARCH-02): `await get_request_context()` returns a `RequestContext` with `user_id="stub-user"`, `team_memberships=["platform"]`, `is_pe_admin=False`
  </behavior>
  <action>
    Implement all five files following the patterns in 01-PATTERNS.md verbatim. `src/pecp/models/enums.py` defines `class ResourceStatus(str, Enum)` with members `pending`, `provisioning`, `ready`, `failed`. `src/pecp/models/provision_result.py` defines `class ProvisionResult(BaseModel)` with fields `status: ResourceStatus`, `provider_metadata: dict[str, Any] = Field(default_factory=dict)`, `activity_log: list[str] = Field(default_factory=list)`, `error: str | None = None`. `src/pecp/models/resource_spec.py` defines `DataServiceSubtype(str, Enum)` (members s3, sqs, sns, rds, dynamodb), the six `*Spec` classes each with a `Literal["PECP<Kind>"]` `kind` field, `LambdaSpec` and `ContainerSpec` adding `model_config = ConfigDict(populate_by_name=True)` plus `Field(alias="api-gateway")` and `Field(alias="source-code")` for `LambdaSpec`, `AnySpec = Annotated[Union[...six specs...], Field(discriminator="kind")]`, `ResourceMetadata(name: str, team: str | None = None)`, and `ResourceSpec(api_version: str = Field(alias="apiVersion"), kind: str, metadata: ResourceMetadata, spec: AnySpec)` with `populate_by_name=True`. Add `model_config = ConfigDict(extra="forbid", populate_by_name=True)` to `LambdaSpec` and `ContainerSpec` per the RESEARCH.md Security Domain Pydantic-extra mitigation; leave the dict-config Salesforce/AEM stubs without extra=forbid since they intentionally accept arbitrary config. `src/pecp/adapters/base.py` defines `class AdapterBase(ABC)` with three `@abstractmethod async def` methods (`provision`, `deprovision`, `get_status`) each accepting `self, resource: ResourceSpec` and returning `ProvisionResult`. `src/pecp/api/dependencies.py` defines `class RequestContext(BaseModel)` with the three fields, `async def get_request_context() -> RequestContext` returning the hardcoded stub (user_id="stub-user", team_memberships=["platform"], is_pe_admin=False), and `ContextDep = Annotated[RequestContext, Depends(get_request_context)]`. Use absolute imports from the `pecp` package root only — no relative imports. Use Python 3.12 union syntax (`str | None`, not `Optional[str]`).
  </action>
  <verify>
    <automated>cd /Users/punitlad/projects/pecp-poc && mypy src/ && ruff check src/ && python -c "from pecp.models.enums import ResourceStatus; from pecp.models.provision_result import ProvisionResult; from pecp.models.resource_spec import ResourceSpec, LambdaSpec; from pecp.adapters.base import AdapterBase; from pecp.api.dependencies import RequestContext, get_request_context, ContextDep; assert ResourceStatus.ready.value == 'ready'; import yaml; data = yaml.safe_load(open('example.yaml').read()); spec = ResourceSpec.model_validate(data); assert isinstance(spec.spec, LambdaSpec); assert spec.spec.source_code == 'github://myorg/lambda-code'"</automated>
  </verify>
  <acceptance_criteria>
    - `mypy src/` exits 0 (zero errors) — strict mode with pydantic.mypy plugin
    - `ruff check src/` exits 0
    - `grep -q "class ResourceStatus" src/pecp/models/enums.py` exits 0
    - `grep -q "discriminator=\"kind\"" src/pecp/models/resource_spec.py` exits 0
    - `grep -q "class AdapterBase(ABC)" src/pecp/adapters/base.py` exits 0
    - `grep -c "@abstractmethod" src/pecp/adapters/base.py` outputs 3
    - `grep -q "ContextDep = Annotated\\[RequestContext, Depends(get_request_context)\\]" src/pecp/api/dependencies.py` exits 0
    - `python -c "from pecp.models.resource_spec import LambdaSpec, ContainerSpec, DataServiceSpec, AccountSpec, SalesforceSpec, AemSpec, DataServiceSubtype, AnySpec, ResourceSpec, ResourceMetadata"` exits 0
    - The example.yaml round-trip in the verify command above returns `source_code == "github://myorg/lambda-code"` (proves hyphenated alias works per Pitfall 3)
  </acceptance_criteria>
  <done>
    All five contract files exist, type-check under mypy strict, lint clean under ruff, and the example.yaml fixture round-trips through the discriminated union with the hyphenated alias preserved. AdapterBase enforcement and RequestContext stub return values are importable and callable.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave 0 test scaffolds — conftest, ADPT-01, ProvisionResult, ResourceSpec, route-test RED skips</name>
  <files>tests/conftest.py, tests/test_adapters/test_adapter_base.py, tests/test_models/test_provision_result.py, tests/test_models/test_resource_spec.py, tests/test_api/test_routes.py</files>
  <read_first>
    - .planning/phases/01-foundation-contracts/01-PATTERNS.md (sections: tests/conftest.py, test_adapter_base.py, test_provision_result.py, test_resource_spec.py, test_routes.py)
    - .planning/phases/01-foundation-contracts/01-RESEARCH.md (sections: Code Examples / Async Test Pattern, ABC TypeError Test; Pitfalls 1, 2, 4, 6; Validation Architecture)
    - .planning/phases/01-foundation-contracts/01-VALIDATION.md (per-task verification map + sampling rate)
    - .planning/phases/01-foundation-contracts/01-CONTEXT.md (ARCH-01, ARCH-02, ADPT-01)
    - example.yaml (used inline in test_resource_spec.py)
    - src/pecp/adapters/base.py (Task 2 — needed for ADPT-01 tests)
    - src/pecp/models/resource_spec.py (Task 2 — needed for discriminated union tests)
    - src/pecp/models/provision_result.py (Task 2)
    - src/pecp/api/dependencies.py (Task 2)
  </read_first>
  <behavior>
    - Behavior 1 (ADPT-01): `pytest tests/test_adapters/test_adapter_base.py -x -q` passes — TypeError raised on incomplete subclass, complete subclass instantiates
    - Behavior 2 (D-01/D-02/D-03): `pytest tests/test_models/test_provision_result.py -x -q` passes — default factories produce empty containers, error field accepts strings, get_status reuse permits empty log/metadata
    - Behavior 3 (D-10): `pytest tests/test_models/test_resource_spec.py -x -q` passes — example.yaml parses to LambdaSpec, invalid kind raises ValidationError, missing source-code raises ValidationError
    - Behavior 4 (ARCH-02): `pytest tests/test_api/test_routes.py -k "context_dependency_callable" -x -q` passes — calls `get_request_context()` directly and asserts the three fields populate (no app required)
    - Behavior 5 (ARCH-01 — RED until Plan 03): `tests/test_api/test_routes.py` contains tests `test_get_resources_without_team_returns_400`, `test_get_resources_with_team_returns_200`, `test_post_resource_without_team_returns_400` that are explicitly `pytest.skip(reason="Pending Plan 03: FastAPI app instance not yet created")` so they do not block the suite but remain visible as planned coverage
  </behavior>
  <action>
    Create `tests/conftest.py` with an async pytest fixture `client` that yields an `httpx.AsyncClient(transport=ASGITransport(app=app), base_url="http://test")` — but guard the `from pecp.api.main import app` import with `pytest.importorskip("pecp.api.main")` so the fixture only loads once Plan 03 creates `src/pecp/api/main.py`. Use `@pytest.fixture` not `@pytest_asyncio.fixture` per pytest-asyncio 1.x asyncio_mode="auto" behavior. Create `tests/test_adapters/test_adapter_base.py` with two tests verbatim from 01-PATTERNS.md: `test_adapter_base_raises_type_error_without_provision` (subclass omitting `provision` → `pytest.raises(TypeError, match="provision")`) and `test_complete_adapter_instantiates_without_error` (full subclass returns ResourceStatus.ready). Create `tests/test_models/test_provision_result.py` with three tests: `test_provision_result_defaults_to_empty_collections` (asserts provider_metadata=={}, activity_log==[], error is None), `test_provision_result_failed_status_carries_error` (status=FAILED, error="Quota exceeded"), `test_get_status_reuse_allows_empty_log_and_metadata` (D-03 reuse). Create `tests/test_models/test_resource_spec.py` with three tests using an inline EXAMPLE_YAML string identical to example.yaml: `test_lambda_spec_parses_from_example_yaml` (asserts ResourceSpec.spec is LambdaSpec, source_code value, api_gateway value), `test_invalid_kind_raises_validation_error` (mutate spec.kind to "InvalidKind", assert ValidationError), `test_lambda_spec_missing_required_field_raises_validation_error` (delete spec["source-code"], assert ValidationError). Create `tests/test_api/test_routes.py` with one passing test `test_context_dependency_callable` that does `from pecp.api.dependencies import get_request_context; ctx = await get_request_context(); assert ctx.user_id == "stub-user"; assert "platform" in ctx.team_memberships; assert ctx.is_pe_admin is False`, AND three skipped placeholder tests for ARCH-01 (`test_get_resources_without_team_returns_400`, `test_get_resources_with_team_returns_200`, `test_post_resource_without_team_returns_400`) each with `pytest.skip(reason="Pending Plan 03: FastAPI app instance and /resources route not yet created")`. Do not decorate any test with `@pytest.mark.asyncio` (asyncio_mode="auto" handles it). Use absolute imports from `pecp.*` everywhere.
  </action>
  <verify>
    <automated>cd /Users/punitlad/projects/pecp-poc && pytest tests/ -x -q 2>&1 | tail -10 && pytest tests/ --collect-only -q 2>&1 | grep -c "test_" | (read n; [ "$n" -ge 8 ] && echo "COLLECTED $n tests >= 8" || (echo "FAIL: only $n tests collected"; exit 1))</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/ -x -q` exits 0 with output reporting at least 8 collected tests, with the three Plan-03 placeholder tests reported as SKIPPED (not failed)
    - `pytest tests/test_adapters/test_adapter_base.py -k "provision" -x -q` exits 0 and runs the TypeError test
    - `pytest tests/test_models/test_resource_spec.py -k "example_yaml" -x -q` exits 0 and the test passes (example.yaml round-trip works)
    - `pytest tests/test_api/test_routes.py -k "context_dependency_callable" -x -q` exits 0 (one passing test using the dependency directly, no app needed)
    - `grep -c "pytest.skip" tests/test_api/test_routes.py` outputs 3
    - `grep -q "ASGITransport" tests/conftest.py` exits 0
    - `grep -q "importorskip" tests/conftest.py` exits 0 (fixture defers app import until Plan 03 creates it)
    - `mypy src/ tests/` exits 0
  </acceptance_criteria>
  <done>
    All Wave 0 test scaffolds exist, the suite is fully green (with three intentional skips clearly attributable to Plan 03), the example.yaml round-trip and ADPT-01 enforcement are proved by passing tests, and the suite runs in under 10 seconds per VALIDATION.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML input → Pydantic parse | Untrusted YAML byte stream crosses into typed Python objects. yaml.safe_load + Pydantic validation are the only sanitizers. |
| (none other) | Phase 1 Plan 01 has no network exposure, no DB, no user input loop. Live HTTP and team-scope enforcement land in Plan 03. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | YAML resource spec parsing (`yaml.safe_load` in tests; later in API) | mitigate | All YAML parsing uses `yaml.safe_load()` exclusively per CLAUDE.md prohibition. Test `test_lambda_spec_parses_from_example_yaml` confirms parse path. Code review checks: `grep -n "yaml.load(" src/ tests/` must return zero non-`safe_load` results. |
| T-01-02 | Tampering | Pydantic spec model (extra/undeclared field injection) | mitigate | `LambdaSpec` and `ContainerSpec` declare `model_config = ConfigDict(extra="forbid", populate_by_name=True)` so undeclared fields cause ValidationError. SalesforceSpec/AemSpec accept arbitrary `config` dict by design (D-11 stub specs); flagged for re-review in Phase 2 when real adapter schemas land. |
| T-01-03 | Information Disclosure | ProvisionResult.error string | accept | `error: str | None` is provider-supplied free text. Phase 1 has no live adapters and no serialization endpoint. Plan 03 / Phase 3 must redact secrets before responding — tracked as known constraint, not mitigated here. |
| T-01-04 | Elevation of Privilege | RequestContext stub | accept | `get_request_context()` is hardcoded `user_id="stub-user", is_pe_admin=False` for PoC. CLAUDE.md and CONTEXT.md explicitly accept absent auth in PoC. The stub structure isolates the change surface — Phase post-PoC swaps the function body for JWT decode without touching route signatures (ARCH-02). |
| T-01-SC | Tampering | npm/pip package installs | mitigate | RESEARCH.md "Package Legitimacy Audit" table lists all 13 packages installed in this plan with `[OK]` slopcheck verdict. No `[ASSUMED]` or `[SUS]` packages in Plan 01 — no human checkpoint needed for this plan. (Plan 03 adds sqlalchemy + aiosqlite and includes a slopcheck verification checkpoint.) |
</threat_model>

<verification>
After Task 3 completes, the executor must run:
1. `pip install -e ".[dev]"` exits 0
2. `ruff check src/ tests/` exits 0
3. `mypy src/ tests/` exits 0
4. `pytest tests/ -x -q` exits 0 with all non-skipped tests passing and skipped count == 3
5. `python -c "import yaml; from pecp.models.resource_spec import ResourceSpec, LambdaSpec; spec = ResourceSpec.model_validate(yaml.safe_load(open('example.yaml').read())); assert isinstance(spec.spec, LambdaSpec); assert spec.spec.source_code == 'github://myorg/lambda-code'"` exits 0
6. `grep -n "yaml.load(" src/ tests/ | grep -v "safe_load" | grep -v "^#"` returns no results (security gate per T-01-01)
</verification>

<success_criteria>
- pyproject.toml exists with all four tool configs (ruff, mypy, pydantic-mypy, pytest)
- `pip install -e ".[dev]"` succeeds; the `pecp` package is importable
- All six resource-kind Pydantic models exist; `ResourceSpec.model_validate(...)` correctly discriminates by `kind`
- `LambdaSpec` and `ContainerSpec` use `model_config = ConfigDict(extra="forbid")` (T-01-02 mitigation)
- `AdapterBase` has 3 `@abstractmethod` methods; incomplete subclass instantiation raises `TypeError`
- `get_request_context()` returns the hardcoded RequestContext; `ContextDep` Annotated alias exists
- All Wave 0 tests collected; non-skipped tests pass; `mypy` strict mode passes
- ROADMAP requirements `ARCH-02` (RequestContext stub) and `ADPT-01` (AdapterBase enforcement) are exercised by passing tests in this plan
</success_criteria>

<output>
After all tasks succeed, write `.planning/phases/01-foundation-contracts/01-01-SUMMARY.md` covering: files created, contracts locked (with D-XX citations), verify commands run + their results, test count (pass/skip), and the deferred items handed to Plan 03 (live FastAPI app + 3 skipped /resources route tests + SQLAlchemy persistence wiring).
</output>
