---
phase: 05-account-flow-ui-demo-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pecp/cli/main.py
  - src/pecp/api/main.py
  - src/pecp/api/routes/teams.py
  - tests/test_api/test_cli.py
  - tests/test_api/test_teams.py
autonomous: true
requirements: [CLI-09, CLI-10]
tags: [cli, typer, fastapi, cors, account]

must_haves:
  truths:
    - "A demo user can run `pecp create awsaccount --team customer-product-app` and receive a resource ID immediately (status pending), proving CLI-09 (D-01, D-02)"
    - "A demo user can run `pecp status awsaccount --team customer-product-app` and see a Rich table with status badge, account metadata (account_id, account_email, account_name, management_console_url), and PE notes — proving CLI-10 status view (D-03)"
    - "A demo user can run `pecp status awsaccount --team customer-product-app --watch` and see line-per-poll output (`[HH:MM:SS] status: provisioning`) updating every 2 seconds, exiting on `ready` or `failed` (D-05)"
    - "A demo user can run `pecp login awsaccount --team customer-product-app` and see env-var export lines for synthetic AWS credentials with exit code 0; exit code 1 if not found; exit code 2 if not yet ready (D-04)"
    - "A browser at http://localhost:5173 can call FastAPI without CORS errors (CORS middleware allows http://localhost:5173) — proving the UI dashboard wiring prerequisite (D-06)"
    - "A `GET /teams?limit=50` request returns a JSON list of `{id, name}` objects for every team in the database — proving the dashboard team-dropdown contract (D-07)"
  artifacts:
    - path: "src/pecp/cli/main.py"
      provides: "account_app Typer sub-app with `create`, `status`, `login` commands registered as `app.add_typer(account_app, name=\"create\")`"
      contains: "account_app"
    - path: "src/pecp/api/main.py"
      provides: "CORSMiddleware allowing http://localhost:5173 registered before include_router calls"
      contains: "CORSMiddleware"
    - path: "src/pecp/api/routes/teams.py"
      provides: "GET /teams list endpoint returning [{id, name}] for dashboard dropdown"
      contains: "async def list_teams"
    - path: "tests/test_api/test_cli.py"
      provides: "Pytest tests for account create/status/login/watch covering CLI-09 and CLI-10"
      contains: "test_account_create"
    - path: "tests/test_api/test_teams.py"
      provides: "Pytest test for GET /teams list endpoint"
      contains: "test_list_teams_returns_all"
  key_links:
    - from: "src/pecp/cli/main.py:account_create"
      to: "POST /resources via httpx"
      via: "yaml.dump() of PECPAccount spec then httpx.post with team query param"
      pattern: "httpx\\.post.*/resources"
    - from: "src/pecp/cli/main.py:account_status,account_login"
      to: "GET /resources?team=&kind=PECPAccount then GET /resources/{id}"
      via: "two-step list-then-detail lookup (analog: existing status command)"
      pattern: "/resources\\?team="
    - from: "src/pecp/api/main.py"
      to: "fastapi.middleware.cors.CORSMiddleware"
      via: "app.add_middleware before app.include_router(...)"
      pattern: "add_middleware\\(CORSMiddleware"
    - from: "src/pecp/api/routes/teams.py:list_teams"
      to: "TeamRecord ORM"
      via: "select(TeamRecord).limit(limit) + .scalars().all()"
      pattern: "select\\(TeamRecord\\)"
---

<objective>
Deliver the **server-side foundation for the dashboard** and the **complete account-flow CLI vertical slice**: three new `pecp create/status/login awsaccount` commands, FastAPI CORS for the React dev server, and a `GET /teams` list endpoint that powers the dashboard team dropdown. This single plan owns every backend artifact the UI plan (Plan 03) consumes, so Plan 03 can be developed in isolation against a stable contract.

Purpose: Wave 1 ships the runnable demo from the terminal (CLI-09 + CLI-10) and unblocks the UI wave by adding the CORS middleware and `GET /teams` list endpoint required by D-06 and D-07.
Output: 3 new Typer commands under `pecp create awsaccount`, `pecp status awsaccount [--watch] [--json]`, `pecp login awsaccount`, plus `CORSMiddleware` on the FastAPI app and `GET /teams` route — all covered by unit/integration tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-account-flow-ui-demo-readiness/05-CONTEXT.md
@.planning/phases/05-account-flow-ui-demo-readiness/05-RESEARCH.md
@.planning/phases/05-account-flow-ui-demo-readiness/05-PATTERNS.md

# Existing source-of-truth files this plan extends
@src/pecp/cli/main.py
@src/pecp/api/main.py
@src/pecp/api/routes/teams.py
@src/pecp/api/routes/resources.py
@src/pecp/adapters/mock/aws_account.py
@tests/test_api/test_cli.py
@tests/test_api/test_teams.py
</context>

<artifacts_this_phase_produces>
**Symbols/files created or first-introduced by this plan (downstream plan-review-convergence and source-grounding passes treat these as known new artifacts, not drift):**

- `src/pecp/cli/main.py`:
  - `account_app` — `typer.Typer(help="AWS account provisioning commands")` sub-app
  - `account_create(team, env, project, file, api_url, json_output)` — CLI command function
  - `account_status(team, watch, json_output, api_url)` — CLI command function
  - `account_login(team, api_url)` — CLI command function
  - `app.add_typer(account_app, name="create")` — registration call
- `src/pecp/api/main.py`:
  - `CORSMiddleware` import from `fastapi.middleware.cors`
  - `app.add_middleware(CORSMiddleware, allow_origins=["http://localhost:5173"], ...)` block (added before `include_router` calls)
- `src/pecp/api/routes/teams.py`:
  - `list_teams(limit, ctx, session)` — new `@router.get("")` handler returning `list[dict[str, str]]`
- `tests/test_api/test_cli.py`:
  - `test_account_create_flag_path_returns_resource_id`
  - `test_account_create_yaml_override_uses_file`
  - `test_account_status_renders_metadata_and_notes`
  - `test_account_status_json_output`
  - `test_account_status_watch_exits_on_ready`
  - `test_account_login_prints_export_lines_when_ready`
  - `test_account_login_exit_code_2_when_not_ready`
  - `test_account_login_exit_code_1_when_not_found`
- `tests/test_api/test_teams.py`:
  - `test_list_teams_returns_all`
  - `test_list_teams_respects_limit`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CORS middleware + GET /teams list endpoint to FastAPI (UI prerequisite)</name>
  <files>src/pecp/api/main.py, src/pecp/api/routes/teams.py, tests/test_api/test_teams.py</files>
  <read_first>
    - src/pecp/api/main.py (current FastAPI app construction — confirm CORS goes BEFORE all `app.include_router(...)` calls; Pitfall 7)
    - src/pecp/api/routes/teams.py (existing `@router.get("/{name}")` handler; new `@router.get("")` must be registered BEFORE the `/{name}` handler so path matching does not consume `""` as a `name`)
    - src/pecp/api/routes/resources.py (list_resources lines 76-110 — the analog for the new list_teams handler: SessionDep, ContextDep, select + .scalars().all())
    - src/pecp/api/dependencies.py (ContextDep type — must be reused verbatim)
    - src/pecp/persistence/database.py (SessionDep type — must be reused verbatim)
    - src/pecp/persistence/models.py (TeamRecord — fields: id, name, owner_id, created_at)
    - tests/test_api/test_teams.py (existing test patterns — TestClient or AsyncClient fixture, how to seed TeamRecord rows in test DB)
    - tests/conftest.py (client fixture pattern used across test_api/*)
  </read_first>
  <behavior>
    - Test 1 (`test_list_teams_returns_all`): seed 3 TeamRecord rows; `GET /teams` returns 200 with a JSON list of length 3 where every element has `id` and `name` keys only.
    - Test 2 (`test_list_teams_respects_limit`): seed 5 TeamRecord rows; `GET /teams?limit=2` returns 200 with a list of length 2.
    - Test 3 (existing `test_create_team` and `test_get_team` continue to pass — the new `""` route must not shadow `/{name}`).
    - Test 4 (CORS smoke): preflight `OPTIONS /resources` with `Origin: http://localhost:5173` returns 200 and `access-control-allow-origin: http://localhost:5173` header. Add as `test_cors_allows_vite_dev_origin` in `tests/test_api/test_teams.py` (co-located is fine; no new test file needed).
  </behavior>
  <action>
    In `src/pecp/api/main.py`:
    - Add `from fastapi.middleware.cors import CORSMiddleware` to the imports.
    - Immediately after `app = FastAPI(...)` and BEFORE any `app.include_router(...)` call, add `app.add_middleware(CORSMiddleware, allow_origins=["http://localhost:5173"], allow_credentials=True, allow_methods=["*"], allow_headers=["*"])` per D-06 (research Pattern 4; Pitfall 7 — order is load-bearing).

    In `src/pecp/api/routes/teams.py`:
    - Add a new `@router.get("")` handler named `list_teams` registered BEFORE the existing `@router.get("/{name}")` handler.
    - Signature: `async def list_teams(limit: int = 50, ctx: ContextDep = ..., session: SessionDep = ...) -> list[dict[str, str]]` — match exact `ContextDep`/`SessionDep` typing already used by `create_team` and `get_team` in this file.
    - Body: `result = await session.execute(select(TeamRecord).limit(limit))`, `rows = result.scalars().all()`, return `[{"id": r.id, "name": r.name} for r in rows]`. No team membership data — dashboard does not need it (avoids N+1).
    - No new imports beyond `select` and `TeamRecord` which already exist in this file.

    In `tests/test_api/test_teams.py`:
    - Add `test_list_teams_returns_all`: seed teams via the existing test fixture pattern, GET /teams, assert status 200 and list shape `[{id, name}]`.
    - Add `test_list_teams_respects_limit`: seed 5 teams, GET /teams?limit=2, assert length 2.
    - Add `test_cors_allows_vite_dev_origin`: send OPTIONS preflight with `Origin: http://localhost:5173`, assert response header `access-control-allow-origin` equals `http://localhost:5173`.
  </action>
  <verify>
    <automated>cd /Users/punitlad/projects/work/pecp-poc &amp;&amp; python -m pytest tests/test_api/test_teams.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "from fastapi.middleware.cors import CORSMiddleware" src/pecp/api/main.py | grep -v '^[[:space:]]*#'` returns 1 match
    - `grep -nE "add_middleware\(CORSMiddleware" src/pecp/api/main.py | grep -v '^[[:space:]]*#'` returns 1 match, and the matching line number is LESS THAN the first `app.include_router(` line number in the same file
    - `grep -nE "allow_origins=\[\"http://localhost:5173\"\]" src/pecp/api/main.py | grep -v '^[[:space:]]*#'` returns 1 match
    - `grep -nE "async def list_teams" src/pecp/api/routes/teams.py | grep -v '^[[:space:]]*#'` returns 1 match
    - `grep -nE "@router.get\(\"\"\)" src/pecp/api/routes/teams.py | grep -v '^[[:space:]]*#'` returns 1 match, and that line number is LESS THAN the line number of `@router.get("/{name}")` in the same file
    - `python -m pytest tests/test_api/test_teams.py::test_list_teams_returns_all -x` exits 0
    - `python -m pytest tests/test_api/test_teams.py::test_list_teams_respects_limit -x` exits 0
    - `python -m pytest tests/test_api/test_teams.py::test_cors_allows_vite_dev_origin -x` exits 0
    - Pre-existing `test_api/test_teams.py` tests still pass: `python -m pytest tests/test_api/test_teams.py -x` exits 0
  </acceptance_criteria>
  <done>
    GET /teams returns a JSON list of teams for the dashboard dropdown. CORS middleware allows http://localhost:5173 so the React dev server can call the API. All pre-existing tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add `account_app` Typer sub-app — `pecp create awsaccount` + `pecp status awsaccount [--watch] [--json]` + `pecp login awsaccount` (CLI-09, CLI-10)</name>
  <files>src/pecp/cli/main.py, tests/test_api/test_cli.py</files>
  <read_first>
    - src/pecp/cli/main.py — entire file. Required pattern sources: `project_app` declaration and registration (~lines 558-602), `_resolve_base_url` (~lines 50-53), `status_badge` + STATUS_COLORS (~lines 37-42), apply command POST pattern with httpx error handling (~lines 92-112), two-step list-then-detail lookup used by existing status command (~lines 182-214), notes rendering (~lines 220-239), `--json` flag pattern (~lines 144-147). Confirm no existing `@app.command("create")` exists before registering `app.add_typer(account_app, name="create")` (Pitfall 1).
    - src/pecp/adapters/mock/aws_account.py — confirm `provider_metadata` exact field names: `account_id`, `account_email`, `account_name`, `management_console_url`. These are the keys `account_status` displays and `account_login` reads. Do NOT invent `allowed_services` or other fields (RESEARCH.md Open Question 2).
    - src/pecp/api/routes/resources.py — confirm `GET /resources?team=&kind=` response shape (record dicts with `id, name, kind, status, env, project`) and `GET /resources/{id}` response shape (adds `notes`, `provider_metadata`).
    - tests/test_api/test_cli.py — existing test patterns: CliRunner usage, httpx mock pattern (`respx` or `httpx.MockTransport`), how existing apply/status/get tests structure assertions.
    - tests/conftest.py — global fixtures.
    - .planning/phases/05-account-flow-ui-demo-readiness/05-CONTEXT.md — D-01, D-02, D-03, D-04, D-05 (every behavioral decision lives here).
  </read_first>
  <behavior>
    - Test `test_account_create_flag_path_returns_resource_id`: invoke `pecp create awsaccount --team customer-product-app --env prod --project cpa-core` with a mocked POST /resources returning 202; assert CLI prints the returned id, the request body parsed as YAML has `kind: PECPAccount`, `metadata.name = "pecp-customer-product-app"`, `metadata.team = "customer-product-app"`, `metadata.env = "prod"`, `metadata.project = "cpa-core"` (D-01, D-02).
    - Test `test_account_create_yaml_override_uses_file`: invoke `pecp create awsaccount -f demo/07-account-async-slowpath/account.yaml --team customer-product-app`; assert the POST body is the file bytes verbatim (no internal YAML build).
    - Test `test_account_status_renders_metadata_and_notes`: mock GET /resources?team=...&kind=PECPAccount returning `[{id: r1, name: pecp-X, kind: PECPAccount, ...}]` and GET /resources/r1 returning a record with `provider_metadata: {account_id: 123, account_email: a@b, account_name: pecp-X, management_console_url: https://...}` and `notes: [{author, timestamp, text}]`; assert stdout contains all four provider_metadata field VALUES and all note text (D-03). Assert stdout does NOT contain `AKIA` access keys (no credentials in status output — D-03).
    - Test `test_account_status_json_output`: same setup with `--json` flag; assert stdout is valid JSON containing the full detail dict, no Rich formatting.
    - Test `test_account_status_watch_exits_on_ready`: mock a sequence of GETs that return status `provisioning, provisioning, ready`; patch `time.sleep` to no-op; assert command exits with code 0 after seeing `ready`, stdout contains lines matching the pattern `[HH:MM:SS] status: ` for each poll, and a new note appearing between polls is printed inline (D-05).
    - Test `test_account_login_prints_export_lines_when_ready`: mock GET /resources/{id} returning status `ready` + `provider_metadata` with synthetic AWS keys (`access_key_id`, `secret_access_key`, `default_region` — or whichever exact keys the AwsAccountMockAdapter populates); assert stdout contains three lines starting with `export AWS_ACCESS_KEY_ID=`, `export AWS_SECRET_ACCESS_KEY=`, `export AWS_DEFAULT_REGION=`, and a `# Profile: ... | Account: ...` comment line. Exit code 0 (D-04).
    - Test `test_account_login_exit_code_2_when_not_ready`: mock account status `provisioning`; assert command exits with code 2 (D-04) and stdout contains a message like "not yet ready".
    - Test `test_account_login_exit_code_1_when_not_found`: mock GET /resources?team=&kind=PECPAccount returning `[]`; assert command exits with code 1 (D-04).
    - Test `test_status_awsaccount_subapp_registered`: `runner.invoke(app, ["status", "awsaccount", "--help"])` exits 0 and help text contains `--team` and `--watch` (proves the `status` sub-typer registration works end-to-end).
    - Test `test_login_awsaccount_subapp_registered`: `runner.invoke(app, ["login", "awsaccount", "--help"])` exits 0 and help text contains `--team` (proves the `login` sub-typer registration works end-to-end).
  </behavior>
  <action>
    In `src/pecp/cli/main.py`:
    - Add `import time` and `from datetime import datetime` to the existing import block if not already present.
    - After the existing `project_app` block (and after the `app.add_typer(project_app, name="project")` registration), add `account_app = typer.Typer(help="AWS account provisioning commands")`.
    - Implement `account_create(team: str = typer.Option(..., "--team"), env: str | None = typer.Option(None, "--env"), project: str | None = typer.Option(None, "--project"), file: Path | None = typer.Option(None, "-f", "--file"), api_url: str | None = typer.Option(None, "--api-url"))` decorated with `@account_app.command("awsaccount")`. Body: resolve base URL via `_resolve_base_url`; if `file is None`, build a Python dict `{"apiVersion": "pecp/v1", "kind": "PECPAccount", "metadata": {"name": f"pecp-{team}", "team": team, "env": env, "project": project}, "spec": {}}`, strip None values from `metadata`, then `yaml.dump(...).encode("utf-8")`; otherwise `file.read_bytes()`. POST to `f"{base}/resources"` with `params={"team": team}` and `Content-Type: application/x-yaml`. Use the same `httpx.RequestError` handling pattern as `apply`. On 202, print `f"[green]Applied[/green] {kind} {name} → id={id} status={status}"`. On other status codes, print error and `raise typer.Exit(code=1)`.
    - Implement `account_status(team: str = typer.Option(..., "--team"), watch: bool = typer.Option(False, "--watch"), json_output: bool = typer.Option(False, "--json"), api_url: str | None = typer.Option(None, "--api-url"))` decorated with `@<status-sub-typer>.command("awsaccount")` (see registration architecture below). Body: define inner helper `_lookup_and_fetch(base, team)` that calls GET `/resources?team={team}&kind=PECPAccount`, finds record with `name == f"pecp-{team}"`, then GET `/resources/{id}`. Return `(id, detail_dict)`. If lookup yields nothing, print red error and exit 1. For non-watch path: render a Rich `Table` titled `f"PECPAccount: pecp-{team}"` with rows for `id, name, status, env, created_at, account_id, account_email, account_name, management_console_url` — `account_*` and `management_console_url` come from `detail.get("provider_metadata", {})`. Use `status_badge(status)` for the status row. After the table, print notes in the same format the existing `status` command uses. If `--json`, print the detail dict as JSON and return. Per D-03, do NOT print AWS access keys in this output. For watch path: loop with 2s `time.sleep`, print `f"[{ts}] status: {status_badge(status)}"` each poll where `ts = datetime.now().strftime('%H:%M:%S')`, track `last_note_count` and print new notes inline as `f"  [{note['timestamp']}] {note['author']}: {note['text']}"`, exit loop when `status in ("ready", "failed")`.
    - Implement `account_login(team: str = typer.Option(..., "--team"), api_url: str | None = typer.Option(None, "--api-url"))` decorated with `@<login-sub-typer>.command("awsaccount")`. Body: reuse the `_lookup_and_fetch` pattern. If no record found, print `f"[red]Not found[/red]: PECPAccount pecp-{team} in team {team}"` and `raise typer.Exit(code=1)`. If status is not `ready`, print `f"[yellow]Account not yet ready[/yellow] (status: {status})"` and `raise typer.Exit(code=2)`. Otherwise read `provider_metadata` and print three export lines using the field names populated by `AwsAccountMockAdapter` (verify exact keys by reading aws_account.py — likely `access_key_id`, `secret_access_key`, `default_region` or equivalent), followed by `# Profile: pecp-{team} | Account: {account_id}`. Then print a usage note: `# Copy and paste the above into your terminal, or run: eval $(pecp login awsaccount --team {team})` (printed with `#` so eval-safe).
    - **Command surface (LOCKED — non-negotiable, per D-01/D-04/D-05):** The resulting user-facing CLI commands MUST be exactly `pecp create awsaccount`, `pecp status awsaccount [--watch] [--json]`, and `pecp login awsaccount`. Renaming to `pecp account-create`, `pecp account-status`, `pecp account-login`, or any other variant is FORBIDDEN. If the chosen registration architecture cannot deliver these exact commands, the architecture is wrong — pick a different architecture.
    - **Registration architecture (Claude's Discretion — internal Typer wiring only):** Because three different commands all share the subcommand name `awsaccount` under three different top-level groups (`create`, `status`, `login`), they cannot all live on a single `account_app` sub-typer. The required shape is: register THREE separate sub-typers on the top-level `app`:
      1. `account_app = typer.Typer()` holds `@account_app.command("awsaccount")` → `account_create`. Register via `app.add_typer(account_app, name="create")`.
      2. `account_status_app = typer.Typer()` holds `@account_status_app.command("awsaccount")` → `account_status`. Register via `app.add_typer(account_status_app, name="status")`.
      3. `account_login_app = typer.Typer()` holds `@account_login_app.command("awsaccount")` → `account_login`. Register via `app.add_typer(account_login_app, name="login")`.
    - **Existing `@app.command("status")` collision (resolve cleanly):** The current top-level `status` is `@app.command("status")` taking positional `kind` and `name`. Registering `app.add_typer(account_status_app, name="status")` will conflict. Resolve by promoting `status` into a sub-typer parent: create `status_app = typer.Typer(invoke_without_command=True)`, move the existing `status` logic into a callback (so `pecp status <kind> <name>` continues to work for backwards compatibility), add `@status_app.command("awsaccount")` for the new behavior, and register via `app.add_typer(status_app, name="status")`. Update any existing tests that invoke `pecp status <kind> <name>` to confirm they still pass. The executor has full discretion on the exact callback wiring pattern (Typer `invoke_without_command=True` + callback, OR a default sub-command pattern, OR any equivalent), as long as both `pecp status <kind> <name>` AND `pecp status awsaccount --team <team>` work.
    - **`login` is a new top-level group:** No existing `@app.command("login")` exists, so `app.add_typer(account_login_app, name="login")` is conflict-free. Verify with `grep -nE "@app.command\(\"login\"\)" src/pecp/cli/main.py` returning no matches before registering.
    - Verify Pitfall 1 condition before registering `create`: `grep -nE "@app.command\(\"create\"\)" src/pecp/cli/main.py` returns no matches.

    In `tests/test_api/test_cli.py`:
    - Add the 10 tests enumerated in `<behavior>` using the existing CliRunner + httpx mock pattern from the file. Reuse fixtures for the in-memory DB / mocked API client.
    - Tests must invoke commands via `runner.invoke(app, [...])` where `app` is the existing top-level Typer app imported from `pecp.cli.main`. Invocations MUST use the locked command surface: `["create", "awsaccount", ...]`, `["status", "awsaccount", ...]`, `["login", "awsaccount", ...]`.
    - For the watch test, patch `pecp.cli.main.time.sleep` to a no-op via `monkeypatch.setattr` so the test does not actually sleep.
    - Add `test_status_awsaccount_subapp_registered` and `test_login_awsaccount_subapp_registered` to prove the sub-typer wiring (these are the end-to-end registration smoke tests required by acceptance criteria).
  </action>
  <verify>
    <automated>cd /Users/punitlad/projects/work/pecp-poc &amp;&amp; python -m pytest tests/test_api/test_cli.py -x -v -k "account or awsaccount"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^account_app = typer.Typer" src/pecp/cli/main.py | grep -v '^[[:space:]]*#'` returns at least 1 match
    - `grep -nE "def account_create" src/pecp/cli/main.py | grep -v '^[[:space:]]*#'` returns 1 match
    - `grep -nE "def account_status" src/pecp/cli/main.py | grep -v '^[[:space:]]*#'` returns 1 match
    - `grep -nE "def account_login" src/pecp/cli/main.py | grep -v '^[[:space:]]*#'` returns 1 match
    - `grep -nE "app.add_typer\(account_app, name=\"create\"\)" src/pecp/cli/main.py | grep -v '^[[:space:]]*#'` returns 1 match
    - `grep -cE "app.add_typer\([^)]*name=\"status\"" src/pecp/cli/main.py` returns 1 (the new status sub-typer registration)
    - `grep -cE "app.add_typer\([^)]*name=\"login\"" src/pecp/cli/main.py` returns 1 (the new login sub-typer registration)
    - `grep -cE "^@app.command\(\"create\"\)" src/pecp/cli/main.py` returns 0 (no top-level `create` command exists; Pitfall 1)
    - `grep -cE "^@app.command\(\"login\"\)" src/pecp/cli/main.py` returns 0 (no top-level `login` command exists; would conflict with sub-typer)
    - `grep -cE "account-create|account-status|account-login" src/pecp/cli/main.py` returns 0 (forbidden command naming surface)
    - `grep -cE "account-create|account-status|account-login" tests/test_api/test_cli.py` returns 0 (no test uses the forbidden command names)
    - `python -m pytest tests/test_api/test_cli.py -k "account or awsaccount" -x` exits 0 with all 10 enumerated tests passing
    - `python -c "from typer.testing import CliRunner; from pecp.cli.main import app; r=CliRunner().invoke(app, ['status', 'awsaccount', '--help']); assert r.exit_code == 0, r.output; print('OK')"` prints `OK`
    - `python -c "from typer.testing import CliRunner; from pecp.cli.main import app; r=CliRunner().invoke(app, ['login', 'awsaccount', '--help']); assert r.exit_code == 0, r.output; print('OK')"` prints `OK`
    - `python -c "from typer.testing import CliRunner; from pecp.cli.main import app; r=CliRunner().invoke(app, ['create', 'awsaccount', '--help']); assert r.exit_code == 0, r.output; print('OK')"` prints `OK`
    - `python -c "from typer.testing import CliRunner; from pecp.cli.main import app; r=CliRunner().invoke(app, ['create', 'awsaccount', '--help']); assert '--team' in r.output and '--env' in r.output and '--project' in r.output and '--file' in r.output.lower() or '-f' in r.output, r.output"` succeeds
    - Running `python -c "from pecp.cli.main import app; app(['--help'])"` exits 0 (no import-time errors from the new sub-apps)
    - `python -m pytest tests/test_api/test_cli.py -x` (full file, not just `-k account`) exits 0 — pre-existing CLI tests still pass (including the legacy `pecp status <kind> <name>` invocations)
  </acceptance_criteria>
  <done>
    `pecp create awsaccount`, `pecp status awsaccount [--watch] [--json]`, and `pecp login awsaccount` work end-to-end against a live FastAPI server. The CLI command surface matches D-01/D-04/D-05 exactly (no renamed-command fallback). All 10 enumerated tests pass. Pre-existing CLI tests still pass (including the legacy `pecp status <kind> <name>` invocation). Status output never includes AWS access keys (D-03 invariant). Login output exits with code 0/1/2 per D-04.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (localhost:5173) → FastAPI (localhost:8000) | CORS-enforced trust boundary; the React app is the only allowed origin for the PoC |
| CLI → FastAPI | Trusted local-dev boundary; no auth in PoC (ARCH-02 stub) |
| User-supplied YAML (`-f account.yaml`) → API | YAML deserialization boundary; existing `safe_load` enforced on server side |
| User-supplied flag values (`--team`, `--env`, `--project`) → FastAPI | Untrusted input crosses the CLI/HTTP boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | CORSMiddleware allow_origins | mitigate | Whitelist only `http://localhost:5173` per D-06; never `["*"]`. Verified by `test_cors_allows_vite_dev_origin` and source assertion in acceptance criteria. |
| T-05-02 | Tampering | YAML `-f account.yaml` deserialization | mitigate | API uses existing `yaml.safe_load` (project-wide constraint from CLAUDE.md). CLI uses `yaml.dump()` only — no untrusted YAML construction. |
| T-05-03 | Tampering | `--env`/`--project` flag values forwarded to ResourceMetadata | mitigate | Pydantic validates `ResourceMetadata` server-side at POST /resources (existing Phase 1 contract); CLI passes values verbatim, no string concatenation into URLs. URL params use httpx `params=` dict (URL-encoded). |
| T-05-04 | Information disclosure | `pecp status awsaccount` accidentally leaking AWS keys | mitigate | D-03 invariant: status output reads `provider_metadata` but explicitly excludes credential fields. Acceptance criterion asserts stdout does NOT contain `AKIA` strings or `access_key_id` field values. |
| T-05-05 | Information disclosure | `pecp login awsaccount` printing credentials to terminal | accept | PoC uses synthetic credentials only; no real AWS keys generated. CONTEXT.md D-04 explicitly approves env-var export pattern. v2 will write to `~/.aws/credentials` with file-mode 0600. |
| T-05-06 | Denial of service | `GET /teams?limit=` unbounded query | mitigate | Default `limit=50` and Pydantic int validation via FastAPI route signature. Document upper bound enforcement as out-of-scope for PoC (single-process SQLite). |
| T-05-07 | Elevation of privilege | CORS `allow_credentials=True` with localhost origin | accept | Required for cookie/credential auth flow if added; localhost-only origin scope makes this a controlled exposure for dev environment. No production deployment in PoC. |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | This plan adds NO new dependency installs; Python deps already present (`typer`, `httpx`, `fastapi`, `pyyaml`). Per RESEARCH.md package legitimacy audit, no checkpoint required for this plan. |
</threat_model>

<verification>
After both tasks complete:
1. `python -m pytest tests/ -x` — full suite passes (regression gate).
2. `ruff check src/pecp tests` — no new lint errors.
3. `mypy src/pecp` — no new type errors.
4. Manual smoke (executor performs as part of `<done>` verification):
   - Start `uvicorn pecp.api.main:app --reload`
   - `curl -i -X OPTIONS http://localhost:8000/resources -H "Origin: http://localhost:5173" -H "Access-Control-Request-Method: GET"` shows `access-control-allow-origin: http://localhost:5173`.
   - `curl http://localhost:8000/teams?limit=50` returns JSON array of teams (may be empty until seed runs in Plan 02).
   - `pecp create awsaccount --team customer-product-app --env prod --project cpa-core` prints applied resource ID.
   - `pecp status awsaccount --team customer-product-app` prints Rich table with metadata.
   - `pecp status awsaccount --team customer-product-app --watch` prints timestamped poll lines and exits on `ready`/`failed`.
   - `pecp login awsaccount --team customer-product-app` prints `export AWS_*` lines when ready.
</verification>

<success_criteria>
- All acceptance criteria for both tasks pass.
- Full test suite (`python -m pytest tests/ -x`) green.
- No mypy or ruff regressions.
- ROADMAP success criteria 1 and 2 for Phase 5 are now demonstrable from a terminal.
- The UI plan (Plan 03) can develop against `GET /teams` and `GET /resources` with CORS unblocked.
</success_criteria>

<output>
Create `.planning/phases/05-account-flow-ui-demo-readiness/05-01-SUMMARY.md` when done — record:
- The exact Typer registration architecture chosen for resolving the `status` collision (callback pattern, `invoke_without_command=True`, etc.) — note which approach the executor selected and why.
- Exact `provider_metadata` keys read by `account_login` (verified from `aws_account.py`).
- Whether `allowed_services` field exists in adapter output (RESEARCH.md Open Question 2).
- Any decisions about `--watch` polling that diverged from D-05 (interval, exit conditions).
</output>
</output>
