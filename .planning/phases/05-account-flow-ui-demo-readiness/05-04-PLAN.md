---
phase: 05-account-flow-ui-demo-readiness
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - .planning/STATE.md
autonomous: false
requirements: [ARCH-03, CLI-09, CLI-10, UI-01, UI-02]
tags: [demo, integration, checkpoint]

must_haves:
  truths:
    - "A stakeholder can run a complete end-to-end demo: seed → start API → start UI → `pecp create awsaccount` → `pecp status awsaccount --watch` → `pecp login awsaccount` → switch to browser → see resource in dashboard Inventory + Deployments tabs (all 5 Phase 5 success criteria simultaneously)"
    - "All Phase 5 requirements (CLI-09, CLI-10, UI-01, UI-02, ARCH-03) are demonstrably satisfied in a single live session"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Updated milestone progress with Phase 5 completion"
      contains: "Phase 5"
  key_links:
    - from: "stakeholder demo session"
      to: "all three Phase 5 deliverables (CLI account flow, UI dashboard, seed script)"
      via: "integrated walkthrough described in docs/DEMO-SCRIPT.md and the human-verify checkpoint below"
      pattern: "pecp create awsaccount"
---

<objective>
Integration checkpoint for Phase 5. All three deliverables (CLI account commands from Plan 01, seed script from Plan 02, UI dashboard from Plan 03) work in isolation by their own acceptance criteria. This plan exists to prove they compose into a coherent stakeholder demo and to gate the milestone-complete transition.

Purpose: Phase 5 is the final phase of the v1 PoC. Closing this plan closes the milestone. The integration walkthrough is the goal-state assertion that turns three independent vertical slices into one demo experience.
Output: Verified end-to-end stakeholder demo flow + STATE.md updated to reflect milestone completion candidate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-account-flow-ui-demo-readiness/05-CONTEXT.md
@docs/DEMO-SCRIPT.md
</context>

<artifacts_this_phase_produces>
**Symbols/files created or first-introduced by this plan:**

- `.planning/phases/05-account-flow-ui-demo-readiness/05-04-SUMMARY.md` — end-of-phase summary capturing the integration verification outcome
- Updates to `.planning/STATE.md` reflecting Phase 5 completion candidate

No new code symbols. This plan is integration verification only.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Stakeholder demo walkthrough — prove all 5 Phase 5 success criteria simultaneously</name>
  <what-built>
    The complete Phase 5 deliverable: a stakeholder can watch the full PECP demo in one session, end to end.

    Setup the executor performs automatically before pausing:
    1. From repo root: `rm -f pecp.db` (fresh DB), then `alembic upgrade head` to recreate schema.
    2. `python scripts/seed.py` — populates 4 teams, 3 projects, lifecycle-spanning resources, plus the `pecp-customer-product-app` PECPAccount in `provisioning` state with PE notes.
    3. Terminal A: `uvicorn pecp.api.main:app --reload --port 8000`.
    4. Terminal B: `cd ui && npm run dev` — UI on 5173.
    5. Terminal C: ready for `pecp` commands.
    6. Browser open at http://localhost:5173.

    Then the executor reports each setup step's exit code/status to the user and pauses for verification.
  </what-built>
  <how-to-verify>
    The user runs (or watches the executor narrate) the following demo flow. Every numbered step must succeed before "approved":

    **Phase 5 Success Criterion #1 — CLI create + watch flow:**
    1. In Terminal C: `pecp create awsaccount --team customer-product-app --env prod --project cpa-core`. Expected: prints `Applied PECPAccount pecp-customer-product-app → id=<uuid> status=pending` (or similar) and exits 0.
    2. `pecp status awsaccount --team customer-product-app --watch`. Expected: prints timestamped lines `[HH:MM:SS] status: <badge>` every 2 seconds; status transitions from `provisioning` to `ready` within ~5 seconds; PE notes appear inline when added; command exits 0 when `ready` is reached.

    **Phase 5 Success Criterion #2 — Credential output (NOT in status; in `login`):**
    3. `pecp status awsaccount --team customer-product-app`. Expected: prints Rich table with status badge, `account_id`, `account_email`, `account_name`, `management_console_url` fields, AND the PE notes history. CRITICAL: stdout must NOT contain `AKIA` or `access_key_id` values (D-03 invariant).
    4. `pecp login awsaccount --team customer-product-app`. Expected: prints three `export AWS_*=...` lines, a `# Profile: ... | Account: ...` comment, and a usage note. Exit code 0.

    **Phase 5 Success Criterion #3 (D-09 wording) — Dashboard inventory:**
    5. In browser, the team dropdown lists 4 teams. Select `customer-product-app`. Inventory tab shows a row for `pecp-customer-product-app` with kind `PECPAccount`, status `ready` (green badge), env `prod`, project `cpa-core` — matching the CLI state. Other team resources are also visible.
    6. Wait 30 seconds. Confirm via DevTools Network tab that no automatic refetch occurs. Click Refresh — the icon spins; a single new `/api/resources?team=customer-product-app` request fires.

    **Phase 5 Success Criterion #4 — Deployment view filter:**
    7. Click the Deployments tab. The Environment filter dropdown appears with options All, dev, staging, prod.
    8. Select `prod`. The table filters to only `env=prod` rows. Select `dev`. The table updates to only `env=dev` rows. No new API call fires (DevTools Network tab confirms client-side filter — D-08).

    **Phase 5 Success Criterion #5 — Seed:**
    9. Stop the API server. Run `rm -f pecp.db && alembic upgrade head && python scripts/seed.py`. Expected: exits 0 with stdout matching `Seeded: 4 teams, 3 projects, [0-9]+ resources`. Re-run `python scripts/seed.py`. Expected: exits 0; stdout includes `skipped` count > 0; no IntegrityError.

    **Cross-cutting:**
    10. The status badge color in the dashboard matches the CLI: `provisioning` is blue in both; `ready` is green in both. Side-by-side visual confirmation.
    11. ROADMAP.md Phase 5 SC #3 reads `data refreshes on demand without a page reload` (verified by Plan 03 Task 4, asserted here as still present).
    12. Pre-existing CLI commands (`pecp apply`, `pecp get`, `pecp status` with positional args, `pecp team`, `pecp project`, `pecp projects`, `pecp deployments`) still work — no Phase 4 regression. The executor demonstrates at least 2 of these.

    If any step fails, report the failing step and what was observed. The phase does not close until all 12 pass.

    After all 12 pass, the executor updates `.planning/STATE.md`:
    - Set `progress.completed_phases` from 4 to 5.
    - Set `progress.percent` to 100.
    - Set `progress.completed_plans` from 13 to 17 (4 new plans in Phase 5).
    - Set `progress.total_plans` from 13 to 17.
    - Update `Current Position` to "Phase: 5 / Plan: All complete / Status: Phase 5 complete — milestone candidate".
    - Update the velocity tables with Phase 5 row.
    - Update `stopped_at` to "Phase 5 complete; awaiting milestone close".
    - Update `last_updated` to the current ISO timestamp.

    Then the executor commits with message `docs(05): Phase 5 milestone-complete state update`.
  </how-to-verify>
  <resume-signal>Type "approved" to finalize Phase 5, or describe the failing step(s) for remediation.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Demo session → all three deliverables | Integration boundary; this plan only verifies; it adds no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Information disclosure | `pecp status awsaccount` accidentally regressing to print credentials | mitigate | Verification step 3 explicitly asserts stdout does NOT contain `AKIA` or `access_key_id` — the D-03 invariant. Catches any regression introduced after Plan 01's tests. |
| T-05-04-02 | Tampering | Phase 5 deliverables work in isolation but interact incorrectly when composed | mitigate | The 12-step demo walkthrough is the integration test. Any state mismatch (CLI shows `ready`, UI shows `pending`) is a defect surfaced here. |
| T-05-04-03 | Repudiation | STATE.md update inaccurately reports plan counts | mitigate | Acceptance criterion in Task 1 specifies the exact target values; reviewer cross-checks against ROADMAP plan totals before approval. |
| T-05-04-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs nothing. All packages installed by Plans 01-03. |
</threat_model>

<verification>
Single human-verify checkpoint covers verification end-to-end. No automated tests at this level; all per-component automated tests live in Plans 01-03.
</verification>

<success_criteria>
- All 12 demo steps in Task 1 pass.
- `.planning/STATE.md` reflects Phase 5 completion (5/5 phases, plans incremented, milestone-candidate status).
- No regression to Phase 1-4 functionality observed during demo.
- The stakeholder demo described in `docs/DEMO-SCRIPT.md` is now reproducible end-to-end with the seed script as the single setup command.
</success_criteria>

<output>
Create `.planning/phases/05-account-flow-ui-demo-readiness/05-04-SUMMARY.md` when done — record:
- Outcome of each of the 12 verification steps (PASS / FAIL with notes).
- Any defects discovered and how they were resolved (which Plan was touched, what changed).
- The final demo-session timing (e.g., "full walkthrough takes 4 minutes from `seed.py` to closing the browser").
- Recommendation for the next milestone (e.g., real adapter implementation, v2 features from REQUIREMENTS.md).
</output>
