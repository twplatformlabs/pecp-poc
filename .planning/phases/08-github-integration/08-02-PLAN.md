---
phase: 08-github-integration
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - tests/test_integrations/test_github.py
autonomous: true
requirements: [GH-01, GH-02, GH-03, GH-04, GH-05]
user_setup: []

must_haves:
  truths:
    - "All five GH requirements have automated passing tests"
    - "Team creation success path calls POST /orgs/{org}/teams and writes slug to DB"
    - "Team creation 422 is non-fatal (logged and skipped, slug stays NULL)"
    - "Project creation success path calls POST /orgs/{org}/repos and writes ProjectRepoRecord"
    - "Project creation 422 is non-fatal (logged and skipped, no repo record created)"
    - "Member add calls PUT /orgs/{org}/teams/{slug}/memberships/{username}"
    - "Member add with NULL github_team_slug skips gracefully (warning logged, no HTTP call)"
    - "Member remove calls DELETE /orgs/{org}/teams/{slug}/memberships/{username}"
    - "Member remove with 404 on DELETE is treated as success (idempotent)"
    - "Member add with 404 on PUT (user not found) raises HTTPStatusError (caught by fire_integrations)"
    - "Rate limit (429) raises HTTPStatusError (caught by fire_integrations)"
    - "aclose() does not raise"
    - "Name sanitization lowercases and replaces spaces with hyphens"
    - "No real GitHub API calls during pytest — all httpx calls intercepted by httpx_mock"
  artifacts:
    - path: "tests/test_integrations/test_github.py"
      provides: "Comprehensive pytest-httpx test suite for GitHubIntegration (GH-01 through GH-05)"
      min_lines: 200
      contains: "test_on_team_create_creates_team_and_writes_slug"
  key_links:
    - from: "tests/test_integrations/test_github.py"
      to: "src/pecp/integrations/github.py"
      via: "Imports GitHubIntegration class, IntegrationConfig, and snapshot dataclasses"
      pattern: "from pecp\\.integrations\\.github import"
    - from: "tests/test_integrations/test_github.py"
      to: "src/pecp/persistence/models.py"
      via: "Test DB writes use TeamRecord and ProjectRepoRecord via db_session fixture"
      pattern: "TeamRecord"
---

<objective>
Create a comprehensive pytest-httpx test suite for `GitHubIntegration` covering all five GH requirements and all seven design decisions.

**Purpose:** Verify GH-01 through GH-05 with isolated pytest-httpx mock responses — no real GitHub API calls ever during pytest. Each test registers mock responses before exercising the integration and asserts correct behavior (success paths, error paths, DB writebacks, slug re-fetch, name sanitization, resource cleanup).

**Output:**
- `tests/test_integrations/test_github.py` — 13 test functions covering all hooks, error conditions, and edge cases
</objective>

<execution_context>
@/Users/punitlad/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/punitlad/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-github-integration/08-CONTEXT.md
@.planning/phases/08-github-integration/08-RESEARCH.md
@.planning/phases/08-github-integration/08-PATTERNS.md
@src/pecp/integrations/github.py
@src/pecp/integrations/base.py
@src/pecp/integrations/__init__.py
@tests/test_integrations/test_noop.py
@tests/test_integrations/test_registry.py
@tests/test_integrations/test_commit_ordering.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write team/project/repo hook tests and name sanitization tests</name>
  <files>
    tests/test_integrations/test_github.py
  </files>
  <action>
    Create `tests/test_integrations/test_github.py` with the following tests for GH-01, GH-02, GH-03, GH-05, and D-01/D-07.

    **File scaffold:**
    - Module docstring: "Tests for GitHubIntegration (GH-01 through GH-05)."
    - Imports: datetime (datetime, timezone), httpx, pytest, HTTPXMock from pytest_httpx, TeamSnapshot/ProjectSnapshot/MemberSnapshot from pecp.integrations.base, GitHubIntegration from pecp.integrations.github, IntegrationConfig from pecp.integrations
    - Use asyncio_mode = "auto" (set in pyproject.toml) — no @pytest.mark.asyncio decorators needed
    - Module constant: `FAKE_CONFIG = IntegrationConfig(GITHUB_PAT="ghp_FAKE", GITHUB_ORG="acme")`

    **Helper factory functions:**
    - `_make_team(**overrides)` returning TeamSnapshot(id="t1", name="Toxins Research", owner_id="u1", created_at=datetime.now(timezone.utc), **overrides)
    - `_make_project(**overrides)` returning ProjectSnapshot(id="p1", name="ML Platform", team_id="t1", environments=["dev"], created_at=datetime.now(timezone.utc), **overrides)
    - `_make_member(**overrides)` returning MemberSnapshot(user_id="alice", role="contributor", **overrides)

    Test functions to write:

    **1. `test_github_integration_is_integration_base` (GH-01)**
    - Instantiate GitHubIntegration(FAKE_CONFIG)
    - Assert isinstance(integration, IntegrationBase) (import IntegrationBase inside the test)
    - Assert all four hooks exist as callable async methods using callable(getattr(integration, name)) for each hook name

    **2. `test_sanitize_transforms_name` (D-07)**
    - Import _sanitize from pecp.integrations.github
    - Assert _sanitize("Toxins Research") == "toxins-research"
    - Assert _sanitize("ML Platform") == "ml-platform"
    - Assert _sanitize("already-lower") == "already-lower"
    - Assert _sanitize("UPPER CASE") == "upper case"

    **3. `test_on_team_create_creates_team_and_writes_slug` (GH-02, GH-05)**
    - Fixtures: httpx_mock: HTTPXMock, db_session
    - Arrange:
      - Register httpx_mock response: POST to "https://api.github.com/orgs/acme/teams" with status 201 and json {"slug": "toxins-research", "name": "toxins-research"}
      - Insert a TeamRecord into db_session with id="t1", name="Toxins Research", owner_id="u1" — so _write_team_slug has a row to update
      - Commit via await db_session.commit()
    - Act:
      - integration = GitHubIntegration(FAKE_CONFIG)
      - team = _make_team() — name "Toxins Research", will be sanitized internally
      - await integration.on_team_create(team)
    - Assert:
      - Refresh team record: await db_session.refresh(record)
      - record.github_team_slug == "toxins-research"

    **4. `test_on_team_create_422_is_non_fatal` (GH-05, D-01)**
    - Fixture: httpx_mock: HTTPXMock
    - Register httpx_mock response: POST to teams URL with status 422
    - integration = GitHubIntegration(FAKE_CONFIG)
    - team = _make_team()
    - Assert await integration.on_team_create(team) completes WITHOUT raising (the 422 handler returns before raise_for_status)
    - This proves D-01: 422 is logged-and-skip, not propagated

    **5. `test_on_project_create_creates_repo_and_writes_url` (GH-03)**
    - Fixtures: httpx_mock: HTTPXMock, db_session
    - Arrange:
      - Register httpx_mock response: POST to "https://api.github.com/orgs/acme/repos" with status 201 and json {"html_url": "https://github.com/acme/toxins-research-ml-platform", "name": "toxins-research-ml-platform"}
      - Insert a ProjectRecord into db_session with id="p1", team_id="t1", name="ML Platform" — so the repo writeback makes sense (FK reference)
      - Commit
    - Act:
      - integration = GitHubIntegration(FAKE_CONFIG)
      - team = _make_team()
      - project = _make_project()
      - await integration.on_project_create(project, team)
    - Assert:
      - Query ProjectRepoRecord by project_id: one row exists with repo_name == "toxins-research-ml-platform" and repo_url == "https://github.com/acme/toxins-research-ml-platform"

    **6. `test_on_project_create_422_is_non_fatal` (GH-05, D-01)**
    - Fixture: httpx_mock: HTTPXMock
    - Register httpx_mock response: POST to repos URL with status 422
    - integration = GitHubIntegration(FAKE_CONFIG)
    - project = _make_project(); team = _make_team()
    - Assert await integration.on_project_create(project, team) completes WITHOUT raising (422 handler returns before raise_for_status)
  </action>
  <verify>
    <automated>python -m pytest tests/test_integrations/test_github.py::test_github_integration_is_integration_base tests/test_integrations/test_github.py::test_sanitize_transforms_name -x -q</automated>
  </verify>
  <done>
    Test file scaffold exists at tests/test_integrations/test_github.py with team/project/repo hook tests passing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append member hook tests, error path tests, and aclose tests</name>
  <files>
    tests/test_integrations/test_github.py
  </files>
  <action>
    Read tests/test_integrations/test_github.py (created in Task 1) and verify the file has correct Python syntax, then append the following test functions at the end of the file, before any trailing newline:

    **7. `test_on_member_add_syncs_to_github` (GH-04)**
    - Fixtures: httpx_mock: HTTPXMock, db_session
    - Arrange:
      - Insert TeamRecord into db_session with id="t1", name="Toxins Research", github_team_slug="toxins-research" — so _fetch_team_slug returns a value
      - Commit
      - Register httpx_mock: PUT to "https://api.github.com/orgs/acme/teams/toxins-research/memberships/alice" with status 200, json {"state": "active", "role": "member"}
    - Act:
      - integration = GitHubIntegration(FAKE_CONFIG)
      - user = _make_member()
      - team = _make_team()
      - await integration.on_member_add(user, team)
    - Assert: No exception raised (membership sync is fire-and-forget)

    **8. `test_on_member_add_null_slug_skips_gracefully` (GH-04, D-05, D-06)**
    - Fixtures: httpx_mock: HTTPXMock, db_session, caplog
    - Arrange:
      - Insert TeamRecord into db_session with id="t1", name="Toxins Research", github_team_slug=None — simulates race condition where slug writeback hasnt happened yet
      - Commit
      - Do NOT register any httpx_mock response (proves no HTTP call is made when slug is NULL)
    - Act:
      - integration = GitHubIntegration(FAKE_CONFIG)
      - user = _make_member(); team = _make_team()
      - await integration.on_member_add(user, team)
    - Assert: No exception raised (D-06 says return silently)
    - Verify with caplog at WARNING level on pecp.integrations.github logger: there is a record mentioning "no github_team_slug" and the team name

    **9. `test_on_member_remove_syncs_to_github` (GH-04)**
    - Fixtures: httpx_mock: HTTPXMock, db_session
    - Arrange:
      - Insert TeamRecord into db_session with id="t1", name="Toxins Research", github_team_slug="toxins-research"
      - Commit
      - Register httpx_mock: DELETE to "https://api.github.com/orgs/acme/teams/toxins-research/memberships/alice" with status 204
    - Act:
      - integration = GitHubIntegration(FAKE_CONFIG)
      - user = _make_member(); team = _make_team()
      - await integration.on_member_remove(user, team)
    - Assert: No exception raised

    **10. `test_on_member_remove_404_is_idempotent` (GH-04, GH-05, D-02)**
    - Fixtures: httpx_mock: HTTPXMock, db_session
    - Arrange:
      - Insert TeamRecord with github_team_slug="toxins-research"
      - Register httpx_mock: DELETE to memberships URL with status 404 (user already removed)
    - Act:
      - await integration.on_member_remove(user, team)
    - Assert: No exception raised (D-02: 404 on DELETE = success)

    **11. `test_on_member_add_user_not_found_is_non_fatal` (GH-05)**
    - Fixtures: httpx_mock: HTTPXMock, db_session
    - Arrange:
      - Insert TeamRecord with github_team_slug="toxins-research"
      - Register httpx_mock: PUT to memberships URL with status 404 (GitHub username not found)
    - Act and Assert:
      - with pytest.raises(httpx.HTTPStatusError): around await integration.on_member_add(user, team)
      - This proves the error propagates up to fire_integrations (which catches it)

    **12. `test_rate_limit_is_non_fatal` (GH-05)**
    - Fixture: httpx_mock: HTTPXMock
    - Register httpx_mock: POST to teams URL with status 429 (rate limit exceeded)
    - integration = GitHubIntegration(FAKE_CONFIG)
    - team = _make_team()
    - with pytest.raises(httpx.HTTPStatusError): around await integration.on_team_create(team)
    - This proves 429 propagates through the hook as a proper exception (fire_integrations catches it downstream)

    **13. `test_aclose_closes_client` (D-03)**
    - integration = GitHubIntegration(FAKE_CONFIG)
    - await integration.aclose()
    - Assert no exception raised — proves the method exists and calls client.aclose() without error

    After appending, the file MUST still have valid Python syntax and all imports from the scaffold in Task 1.
  </action>
  <verify>
    <automated>python -m pytest tests/test_integrations/test_github.py -x -q</automated>
  </verify>
  <done>
    All 13 test functions exist in test_github.py. Member hooks, error paths, and aclose are tested. Full test file passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PECP test -> httpx_mock fixture | All GitHub HTTP calls intercepted at transport layer — no real API calls during pytest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | Test PAT values in logs | mitigate | Tests use placeholder value "ghp_FAKE" — never real PAT values. Consistent with Phase 7 test_config.py pattern. |
| T-08-02 | Tampering | Mock response registration | accept | Tests register mock responses for each endpoint before exercising. pytest-httpx raises on unregistered requests — tests that leak unregistered calls fail fast, preventing false passes. |
| T-08-03 | Availability | httpx_mock failing on unexpected requests | mitigate | Each test registers only the expected mock responses. Tests that exercise error paths (422, 429, 404) register responses with those status codes. No unexpected requests occur. |
</threat_model>

<verification>

1. `python -m pytest tests/test_integrations/test_github.py -x -q` — All GitHub integration tests pass
2. `python -m pytest tests/ -x -q` — Full test suite passes with no regressions (all existing Phase 1-7 tests still pass)

</verification>

<success_criteria>

- [ ] tests/test_integrations/test_github.py created with 13 test functions
- [ ] Team creation success test (GH-02) verifies slug written to DB
- [ ] Team creation 422 test (GH-05, D-01) verifies non-fatal behavior
- [ ] Project creation success test (GH-03) verifies ProjectRepoRecord inserted
- [ ] Project creation 422 test (GH-05, D-01) verifies non-fatal behavior
- [ ] Member add test (GH-04) verifies PUT membership called
- [ ] Member add null slug test (D-05, D-06) verifies graceful skip with warning
- [ ] Member remove test (GH-04) verifies DELETE membership called
- [ ] Member remove 404 test (D-02) verifies idempotent behavior
- [ ] User not found test (GH-05) verifies HTTPStatusError propagation
- [ ] Rate limit test (GH-05) verifies 429 raises caught exception
- [ ] aclose test (D-03) verifies no exception on cleanup
- [ ] Name sanitization test (D-07) verifies lowercase + hyphen substitution
- [ ] No real GitHub API calls during pytest

</success_criteria>

<output>
Create `.planning/phases/08-github-integration/08-02-SUMMARY.md` when done.
</output>
