---
phase: 04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pecp/persistence/models.py
  - src/pecp/models/resource_spec.py
  - alembic/versions/0003_add_teams_projects_deployments.py
  - tests/test_api/test_teams.py
  - tests/test_api/test_projects.py
  - tests/test_api/test_deployments.py
  - tests/test_api/test_soft_delete.py
  - tests/test_api/test_cli.py
autonomous: true
requirements:
  - TEAM-01
  - TEAM-02
  - TEAM-03
  - CLI-05
  - CLI-06
  - CLI-07
  - CLI-08
tags:
  - schema
  - alembic
  - orm
  - test-scaffolds
must_haves:
  truths:
    - "ResourceMetadata accepts a nullable project field alongside env"
    - "ResourceRecord has nullable project and deleted_at columns after migration 0003"
    - "teams, team_members, projects, deployments tables exist with correct columns and constraints after migration 0003"
    - "Alembic migration 0003 upgrades cleanly on an existing 0002 SQLite DB"
    - "Wave 0 test files exist for teams, projects, deployments, soft-delete and import successfully"
    - "Wave 0 CLI test extensions for team/projects/deployments/--json exist and import successfully"
    - "Wave 0 CLI test scaffold for pecp project create exists in test_cli.py"
  artifacts:
    - path: "src/pecp/persistence/models.py"
      provides: "TeamRecord, TeamMemberRecord, ProjectRecord, DeploymentRecord ORM classes plus project + deleted_at columns on ResourceRecord"
      contains: "class TeamRecord"
    - path: "src/pecp/models/resource_spec.py"
      provides: "ResourceMetadata.project nullable field"
      contains: "project: str | None"
    - path: "alembic/versions/0003_add_teams_projects_deployments.py"
      provides: "Alembic migration 0003 adding all Phase 4 schema changes"
      contains: "revision = \"0003\""
    - path: "tests/test_api/test_teams.py"
      provides: "Wave 0 failing scaffolds for TEAM-01"
      contains: "async def test_"
    - path: "tests/test_api/test_projects.py"
      provides: "Wave 0 failing scaffolds for TEAM-02"
      contains: "async def test_"
    - path: "tests/test_api/test_deployments.py"
      provides: "Wave 0 failing scaffolds for TEAM-03 (deployment audit + env filter)"
      contains: "async def test_"
    - path: "tests/test_api/test_soft_delete.py"
      provides: "Wave 0 failing scaffolds for D-11 soft-delete behavior"
      contains: "async def test_"
    - path: "tests/test_api/test_cli.py"
      provides: "Wave 0 failing scaffolds for CLI-05/06/07/08 and --json"
      contains: "def test_team_"
  key_links:
    - from: "src/pecp/persistence/models.py"
      to: "alembic/versions/0003_add_teams_projects_deployments.py"
      via: "column + table parity"
      pattern: "TeamRecord|TeamMemberRecord|ProjectRecord|DeploymentRecord"
    - from: "src/pecp/persistence/models.py"
      to: "tests/conftest.py db_session / client fixtures"
      via: "Base.metadata.create_all picks up new tables"
      pattern: "Base.metadata"
    - from: "src/pecp/models/resource_spec.py"
      to: "src/pecp/api/routes/resources.py (Plan 03)"
      via: "spec.metadata.project consumed by POST /resources create/update paths"
      pattern: "metadata.project"
---

## Phase Goal

**As a** platform engineer using PECP, **I want to** create teams, group resources into projects scoped to environments, and query a deployment audit trail per environment via the `pecp` CLI, **so that** PECP becomes a multi-team tool with compliance-grade visibility into who changed what, where, and when.

<objective>
Lay the schema, data model, and test-harness foundation that all Phase 4 vertical slices depend on. This plan adds the new ORM tables (`teams`, `team_members`, `projects`, `deployments`), extends `ResourceRecord` with `project` + `deleted_at`, extends `ResourceMetadata` with `project`, and writes a single Alembic migration (`0003`) that applies all six changes atomically. It also lands the Wave 0 RED test scaffolds for teams, projects, deployments, soft-delete, and CLI commands — the failing tests that subsequent plans (02 and 03) will turn green.

Purpose: All four Phase 4 success criteria, every requirement ID (TEAM-01/02/03, CLI-05/06/07/08), and every locked decision D-01 through D-17 depend on these tables and the ResourceMetadata field. The Wave 0 tests are the validation contract — Plan 02 and Plan 03 succeed exactly when the tests in this plan turn green.

Output: One Alembic migration, four new ORM classes + two new columns on ResourceRecord, one new Pydantic field, four new test files, and a CLI test extension. After this plan, the schema is ready, the data model is ready, and the failing tests for every Phase 4 behavior exist.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-teams-projects-deployments/04-CONTEXT.md
@.planning/phases/04-teams-projects-deployments/04-RESEARCH.md
@.planning/phases/04-teams-projects-deployments/04-PATTERNS.md
@.planning/phases/04-teams-projects-deployments/04-VALIDATION.md
@src/pecp/persistence/models.py
@src/pecp/models/resource_spec.py
@alembic/versions/0002_add_env_notes_unique.py
@tests/conftest.py
@tests/test_api/test_cli.py
@tests/test_api/test_routes.py
@tests/test_api/test_idempotency.py
</context>

## Artifacts this phase produces

The following symbols are NEW in Phase 4 — they do not exist before this plan runs. Downstream verification must treat them as newly-created (not drift):

**ORM classes (added to `src/pecp/persistence/models.py`):**
- `TeamRecord` (table `teams`)
- `TeamMemberRecord` (table `team_members`)
- `ProjectRecord` (table `projects`)
- `DeploymentRecord` (table `deployments`)

**ResourceRecord columns (added):**
- `ResourceRecord.project: Mapped[str | None]` — nullable Text
- `ResourceRecord.deleted_at: Mapped[datetime | None]` — nullable DateTime(timezone=True)

**Pydantic fields (added):**
- `ResourceMetadata.project: str | None = None`

**Alembic revisions:**
- Revision `"0003"` with `down_revision = "0002"` in file `alembic/versions/0003_add_teams_projects_deployments.py`

**DB constraints (added):**
- `uq_teams_name` UNIQUE on `teams(name)`
- composite PK on `team_members(team_id, user_id)`
- `uq_projects_team_name` UNIQUE on `projects(team_id, name)`
- FK `team_members.team_id → teams.id`
- FK `projects.team_id → teams.id`
- FK `deployments.resource_id → resource_records.id`
- FK `deployments.project_id → projects.id` (nullable)

**Test functions / files (added):**
- `tests/test_api/test_teams.py` (new file)
- `tests/test_api/test_projects.py` (new file)
- `tests/test_api/test_deployments.py` (new file)
- `tests/test_api/test_soft_delete.py` (new file)
- New tests added to `tests/test_api/test_cli.py` for team/projects/deployments/--json

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ORM (ResourceRecord columns + 4 new tables) and ResourceMetadata.project field</name>
  <files>src/pecp/persistence/models.py, src/pecp/models/resource_spec.py</files>
  <read_first>
    - src/pecp/persistence/models.py (current ResourceRecord — add columns, never reorder existing ones; matches Phase 3 env/notes pattern)
    - src/pecp/models/resource_spec.py (current ResourceMetadata at lines 92-95 — add project alongside env)
    - .planning/phases/04-teams-projects-deployments/04-PATTERNS.md (Pattern Map sections "src/pecp/persistence/models.py" and "src/pecp/models/resource_spec.py" — copy exact field definitions)
    - .planning/phases/04-teams-projects-deployments/04-RESEARCH.md ("ORM Models for New Tables" Code Examples section — verbatim shapes for all four classes)
    - .planning/phases/04-teams-projects-deployments/04-CONTEXT.md (D-01, D-04, D-05, D-08, D-09, D-11 — column-by-column source of truth)
  </read_first>
  <behavior>
    - Test 1: `from pecp.persistence.models import TeamRecord, TeamMemberRecord, ProjectRecord, DeploymentRecord` succeeds.
    - Test 2: `TeamRecord.__tablename__ == "teams"`, `TeamRecord.__table_args__` contains a `UniqueConstraint("name", name="uq_teams_name")`, columns: `id` (String, primary_key), `name` (Text, not null), `owner_id` (Text, not null), `created_at` (DateTime(timezone=True), not null, default = now UTC).
    - Test 3: `TeamMemberRecord.__tablename__ == "team_members"`, composite PK on `(team_id, user_id)`, FK `team_id → teams.id`, columns: `team_id` (String, FK, not null), `user_id` (Text, not null), `role` (Text, not null), `joined_at` (DateTime(timezone=True), not null, default = now UTC).
    - Test 4: `ProjectRecord.__tablename__ == "projects"`, `UniqueConstraint("team_id", "name", name="uq_projects_team_name")`, FK `team_id → teams.id`, columns: `id` (String, primary_key), `team_id` (String, FK, not null), `name` (Text, not null), `environments` (Text, not null), `created_at` (DateTime(timezone=True), not null, default = now UTC).
    - Test 5: `DeploymentRecord.__tablename__ == "deployments"`, FK `resource_id → resource_records.id` (not null), FK `project_id → projects.id` (nullable), columns: `id` (String, primary_key), `resource_id` (String, FK, not null), `project_id` (String, FK, nullable), `environment` (Text, nullable), `status` (Text, not null), `change_type` (Text, not null), `deployed_at` (DateTime(timezone=True), not null).
    - Test 6: `ResourceRecord.project` exists as a Mapped[str | None] mapped to a nullable Text column. `ResourceRecord.deleted_at` exists as a Mapped[datetime | None] mapped to a nullable DateTime(timezone=True) column. All existing ResourceRecord columns (id, team, kind, name, status, spec_json, created_at, provider_metadata, activity_log, env, notes) remain unchanged.
    - Test 7: `ResourceMetadata(name="x").project is None` (project is optional with default None). `ResourceMetadata(name="x", project="payments-backend").project == "payments-backend"`. The existing `env` field continues to work unchanged.
    - Test 8: `Base.metadata.create_all` over an in-memory SQLite engine produces all five tables (`resource_records`, `teams`, `team_members`, `projects`, `deployments`) with no errors. The existing `db_session` fixture in `tests/conftest.py` already calls `create_all` and must continue to work without modification.
  </behavior>
  <action>
    Extend `src/pecp/persistence/models.py` per D-01, D-04, D-05, D-09, D-11.

    On `ResourceRecord`: append two new `mapped_column` declarations after the existing `notes` column. Add `project: Mapped[str | None] = mapped_column(Text, nullable=True)` (D-05). Add `deleted_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)` (D-11). Do not change any existing column, do not reorder, do not remove the existing `UniqueConstraint("team", "kind", "name", name="uq_resource_team_kind_name")`.

    Add four new ORM classes after `ResourceRecord` (all inheriting from `Base`):

    - `TeamRecord` with `__tablename__ = "teams"` and `__table_args__ = (UniqueConstraint("name", name="uq_teams_name"),)`. Columns: `id` String primary_key, `name` Text nullable=False, `owner_id` Text nullable=False, `created_at` DateTime(timezone=True) nullable=False with `default=lambda: datetime.now(timezone.utc)`. Source: PATTERNS.md "New ORM classes to ADD" section.

    - `TeamMemberRecord` with `__tablename__ = "team_members"` and `__table_args__ = (PrimaryKeyConstraint("team_id", "user_id"),)`. Columns: `team_id` String with `ForeignKey("teams.id")` nullable=False, `user_id` Text nullable=False, `role` Text nullable=False, `joined_at` DateTime(timezone=True) nullable=False with default = now UTC.

    - `ProjectRecord` with `__tablename__ = "projects"` and `__table_args__ = (UniqueConstraint("team_id", "name", name="uq_projects_team_name"),)`. Columns: `id` String primary_key, `team_id` String with `ForeignKey("teams.id")` nullable=False, `name` Text nullable=False, `environments` Text nullable=False (stores JSON-serialized list — round-trip via `json.dumps`/`json.loads` in route handlers, not in the model), `created_at` DateTime(timezone=True) nullable=False with default = now UTC.

    - `DeploymentRecord` with `__tablename__ = "deployments"` and no `__table_args__` (no unique constraint — append-only audit log; multiple rows per resource expected). Columns: `id` String primary_key, `resource_id` String with `ForeignKey("resource_records.id")` nullable=False, `project_id` String with `ForeignKey("projects.id")` nullable=True (D-09), `environment` Text nullable=True (D-09), `status` Text nullable=False, `change_type` Text nullable=False (values: `create`, `update`, `delete` — validated at the route handler, not the DB layer), `deployed_at` DateTime(timezone=True) nullable=False (no default — route handler always supplies it explicitly for audit clarity).

    Add the necessary imports at the top of the file: `ForeignKey`, `PrimaryKeyConstraint` from `sqlalchemy`. The existing imports of `DateTime`, `String`, `Text`, `UniqueConstraint`, `DeclarativeBase`, `Mapped`, `mapped_column` and the `Base` class stay unchanged.

    Extend `src/pecp/models/resource_spec.py` per D-08. In `ResourceMetadata` (currently at lines 92–95), add `project: str | None = None` as a new field after `env`. Field order in the class: `name`, `team`, `env`, `project`. Do not change `name`, `team`, or `env` field declarations.

    Do not add tests in this task — Task 3 lands the Wave 0 scaffolds.
  </action>
  <verify>
    <automated>cd /Users/punitlad/projects/pecp-poc &amp;&amp; python -c "from pecp.persistence.models import TeamRecord, TeamMemberRecord, ProjectRecord, DeploymentRecord, ResourceRecord, Base; assert TeamRecord.__tablename__ == 'teams'; assert TeamMemberRecord.__tablename__ == 'team_members'; assert ProjectRecord.__tablename__ == 'projects'; assert DeploymentRecord.__tablename__ == 'deployments'; assert 'project' in ResourceRecord.__table__.columns; assert 'deleted_at' in ResourceRecord.__table__.columns; from pecp.models.resource_spec import ResourceMetadata; m = ResourceMetadata(name='x'); assert m.project is None; m2 = ResourceMetadata(name='x', project='p'); assert m2.project == 'p'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pecp/persistence/models.py` contains class `TeamRecord`, class `TeamMemberRecord`, class `ProjectRecord`, class `DeploymentRecord` declared after `ResourceRecord`.
    - `ResourceRecord` contains `project: Mapped[str | None]` and `deleted_at: Mapped[datetime | None]` mapped to nullable columns.
    - `src/pecp/models/resource_spec.py` contains `project: str | None = None` inside `ResourceMetadata` after `env`.
    - `python -c "from pecp.persistence.models import TeamRecord, TeamMemberRecord, ProjectRecord, DeploymentRecord"` exits 0.
    - `mypy src/pecp/persistence/models.py src/pecp/models/resource_spec.py` exits 0 (no new type errors introduced).
    - `ruff check src/pecp/persistence/models.py src/pecp/models/resource_spec.py` exits 0.
    - Running `pytest tests/test_api/test_idempotency.py::test_post_resources_unique_constraint_blocks_duplicate_team_kind_name -x -q` exits 0 (existing test that relies on `Base.metadata.create_all` continues to pass with the new tables).
  </acceptance_criteria>
  <done>
    Four new ORM classes plus two new ResourceRecord columns exist and import cleanly. `ResourceMetadata.project` accepts a nullable string. Existing tests that touch `Base.metadata.create_all` continue to pass because the new tables compile and the existing columns are unchanged. The model is the contract for Task 2's migration and Plan 02/03's route handlers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author Alembic migration 0003 adding all four tables and both ResourceRecord columns</name>
  <files>alembic/versions/0003_add_teams_projects_deployments.py</files>
  <read_first>
    - alembic/versions/0002_add_env_notes_unique.py (template — copy header style, batch_alter_table usage, downgrade pattern; `render_as_batch=True` is already set in alembic/env.py per Phase 3 D-01)
    - alembic/versions/0001_add_provider_cols.py (template for add_column without batch — informational)
    - alembic/versions/0000_create_resource_records.py (template for op.create_table with PrimaryKeyConstraint and UniqueConstraint — informational)
    - src/pecp/persistence/models.py (the source-of-truth schema after Task 1 — migration column types must match exactly)
    - .planning/phases/04-teams-projects-deployments/04-PATTERNS.md (section "alembic/versions/0003_add_teams_projects_deployments.py" — header + upgrade + downgrade structure)
    - .planning/phases/04-teams-projects-deployments/04-RESEARCH.md (Pattern 6 — full upgrade body)
    - .planning/phases/04-teams-projects-deployments/04-CONTEXT.md (D-01, D-04, D-09, D-11 — column-by-column constraints)
  </read_first>
  <action>
    Create `alembic/versions/0003_add_teams_projects_deployments.py`. File header docstring: `"""Add teams, team_members, projects, deployments tables and extend resource_records.\n\nRevision ID: 0003\nRevises: 0002\nCreate Date: 2026-06-14\n"""`. Module-level constants: `revision = "0003"`, `down_revision = "0002"`, `branch_labels = None`, `depends_on = None`. Imports: `import sqlalchemy as sa` and `from alembic import op` (mirror migration 0002 exactly).

    `upgrade()` body, in this order:

    1. Extend `resource_records` using `op.batch_alter_table("resource_records")` (batch mode required for SQLite per Phase 3 D-01 — `render_as_batch=True` is already set in alembic/env.py). Inside the batch context: `batch_op.add_column(sa.Column("project", sa.Text(), nullable=True))` and `batch_op.add_column(sa.Column("deleted_at", sa.DateTime(timezone=True), nullable=True))`. Both columns nullable so existing rows do not need a default.

    2. `op.create_table("teams", ...)`: columns `id` (`sa.String()`, nullable=False), `name` (`sa.Text()`, nullable=False), `owner_id` (`sa.Text()`, nullable=False), `created_at` (`sa.DateTime(timezone=True)`, nullable=False). Constraints: `sa.PrimaryKeyConstraint("id")`, `sa.UniqueConstraint("name", name="uq_teams_name")`.

    3. `op.create_table("team_members", ...)`: columns `team_id` (`sa.String()`, nullable=False), `user_id` (`sa.Text()`, nullable=False), `role` (`sa.Text()`, nullable=False), `joined_at` (`sa.DateTime(timezone=True)`, nullable=False). Constraints: `sa.ForeignKeyConstraint(["team_id"], ["teams.id"])`, `sa.PrimaryKeyConstraint("team_id", "user_id")`.

    4. `op.create_table("projects", ...)`: columns `id` (`sa.String()`, nullable=False), `team_id` (`sa.String()`, nullable=False), `name` (`sa.Text()`, nullable=False), `environments` (`sa.Text()`, nullable=False), `created_at` (`sa.DateTime(timezone=True)`, nullable=False). Constraints: `sa.ForeignKeyConstraint(["team_id"], ["teams.id"])`, `sa.PrimaryKeyConstraint("id")`, `sa.UniqueConstraint("team_id", "name", name="uq_projects_team_name")`.

    5. `op.create_table("deployments", ...)`: columns `id` (`sa.String()`, nullable=False), `resource_id` (`sa.String()`, nullable=False), `project_id` (`sa.String()`, nullable=True), `environment` (`sa.Text()`, nullable=True), `status` (`sa.Text()`, nullable=False), `change_type` (`sa.Text()`, nullable=False), `deployed_at` (`sa.DateTime(timezone=True)`, nullable=False). Constraints: `sa.ForeignKeyConstraint(["resource_id"], ["resource_records.id"])`, `sa.ForeignKeyConstraint(["project_id"], ["projects.id"])`, `sa.PrimaryKeyConstraint("id")`.

    `downgrade()` body, in strict reverse order to satisfy FK constraints:

    1. `op.drop_table("deployments")`
    2. `op.drop_table("projects")`
    3. `op.drop_table("team_members")`
    4. `op.drop_table("teams")`
    5. `with op.batch_alter_table("resource_records") as batch_op:` then `batch_op.drop_column("deleted_at")` then `batch_op.drop_column("project")`.

    Verify column types match the SQLAlchemy model declarations from Task 1 (Text vs String — use Text for human-facing values per Phase 2 D-04 column conventions, use String for UUID hex ids consistent with `resource_records.id`).
  </action>
  <verify>
    <automated>cd /Users/punitlad/projects/pecp-poc &amp;&amp; rm -f /tmp/pecp_mig_test.db &amp;&amp; PECP_DATABASE_URL='sqlite+aiosqlite:////tmp/pecp_mig_test.db' DATABASE_URL='sqlite:////tmp/pecp_mig_test.db' alembic upgrade head &amp;&amp; python -c "import sqlite3; c = sqlite3.connect('/tmp/pecp_mig_test.db'); names = sorted(r[0] for r in c.execute(\"SELECT name FROM sqlite_master WHERE type='table'\")); print(names); assert 'teams' in names and 'team_members' in names and 'projects' in names and 'deployments' in names and 'resource_records' in names; cols_rr = sorted(r[1] for r in c.execute('PRAGMA table_info(resource_records)')); assert 'project' in cols_rr and 'deleted_at' in cols_rr; print('OK')" &amp;&amp; DATABASE_URL='sqlite:////tmp/pecp_mig_test.db' alembic downgrade 0002 &amp;&amp; python -c "import sqlite3; c = sqlite3.connect('/tmp/pecp_mig_test.db'); names = sorted(r[0] for r in c.execute(\"SELECT name FROM sqlite_master WHERE type='table'\")); assert 'teams' not in names and 'team_members' not in names and 'projects' not in names and 'deployments' not in names; cols_rr = sorted(r[1] for r in c.execute('PRAGMA table_info(resource_records)')); assert 'project' not in cols_rr and 'deleted_at' not in cols_rr; print('OK')" &amp;&amp; rm -f /tmp/pecp_mig_test.db</automated>
  </verify>
  <acceptance_criteria>
    - File `alembic/versions/0003_add_teams_projects_deployments.py` exists with `revision = "0003"` and `down_revision = "0002"`.
    - `alembic upgrade head` on a fresh SQLite DB (starting from `0002`) exits 0 and produces tables `teams`, `team_members`, `projects`, `deployments` plus `project` and `deleted_at` columns on `resource_records`.
    - `alembic downgrade 0002` exits 0 and reverses every change without error (verified via PRAGMA).
    - `grep -v '^#' alembic/versions/0003_add_teams_projects_deployments.py | grep -c 'op.create_table'` returns 4.
    - `grep -v '^#' alembic/versions/0003_add_teams_projects_deployments.py | grep -c 'op.drop_table'` returns 4.
    - `grep -v '^#' alembic/versions/0003_add_teams_projects_deployments.py | grep -c 'batch_alter_table'` returns 2 (one in upgrade for add_column, one in downgrade for drop_column).
    - `ruff check alembic/versions/0003_add_teams_projects_deployments.py` exits 0.
  </acceptance_criteria>
  <done>
    Migration 0003 applies cleanly on top of 0002, creates all four new tables with proper FK and unique constraints, adds two columns to `resource_records`, and rolls back cleanly. The database schema state after `alembic upgrade head` matches the ORM declarations from Task 1.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write Wave 0 RED test scaffolds for teams, projects, deployments, soft-delete, and CLI commands</name>
  <files>tests/test_api/test_teams.py, tests/test_api/test_projects.py, tests/test_api/test_deployments.py, tests/test_api/test_soft_delete.py, tests/test_api/test_cli.py</files>
  <read_first>
    - tests/conftest.py (existing `client` and `db_session` fixtures — reuse `client: AsyncClient` for API integration tests against `pecp.api.main.app`; the fixture already drops + recreates all tables via `Base.metadata`)
    - tests/test_api/test_routes.py (template for `client: AsyncClient` integration tests against /resources — header docstring style, async def, `await client.post(...)`)
    - tests/test_api/test_idempotency.py (template for `client: AsyncClient` integration tests with body bytes + Content-Type header)
    - tests/test_api/test_notes.py (template for POST then GET sequence asserting JSON list shape)
    - tests/test_api/test_cli.py (existing CLI tests with `runner.invoke` + `mock.patch("httpx.*")` — extend this file, do not replace existing tests)
    - .planning/phases/04-teams-projects-deployments/04-VALIDATION.md ("Per-Task Verification Map" — each row tells you exactly which file maps to which requirement)
    - .planning/phases/04-teams-projects-deployments/04-RESEARCH.md ("Phase Requirements → Test Map" + "Wave 0 Gaps" — the exact behaviors to assert)
    - .planning/phases/04-teams-projects-deployments/04-PATTERNS.md (section "tests/test_api/test_teams.py..." — analog + mock pattern)
  </read_first>
  <behavior>
    Every test in every new file must FAIL when first run because the corresponding routes/CLI commands do not yet exist (Wave 0 RED). Subsequent plans turn them GREEN. Each test imports successfully (no import errors) — failures must come from missing routes (404), missing CLI commands (CliRunner exit_code != 0), or incorrect output shape, NOT from `ImportError`.

    `tests/test_api/test_teams.py` — TEAM-01:
    - `test_post_teams_creates_team_and_owner_member`: `await client.post("/teams", json={"name": "payments", "owner": "alice"})` returns 201 with body `{"id": "...", "name": "payments", "owner_id": "alice", "created_at": "...", "members": [{"user_id": "alice", "role": "owner", "joined_at": "..."}]}`.
    - `test_post_teams_duplicate_name_returns_409`: first POST 201, second POST with same name returns 409 (D-03).
    - `test_get_teams_by_name_returns_members`: after POST, `await client.get("/teams/payments")` returns 200 with the same body shape including the members list with owner.
    - `test_get_teams_unknown_name_returns_404`: GET on unknown name returns 404.
    - `test_post_teams_missing_owner_returns_422`: POST `{"name": "payments"}` (no `owner`) returns 422 (Pydantic validation).

    `tests/test_api/test_projects.py` — TEAM-02:
    - `test_post_projects_creates_project_with_environments`: precondition (POST /teams to create team), then POST `/projects` with body `{"name": "payments-backend", "team": "payments", "environments": ["dev", "staging", "prod"]}` returns 201 with `{"id": "...", "name": "payments-backend", "environments": ["dev", "staging", "prod"], ...}`. The `environments` value in the response must be a JSON list (not a string).
    - `test_get_projects_lists_team_projects_with_resource_count`: create team, create project, GET `/projects?team=payments` returns 200 with a list containing one entry with `resource_count == 0` (no resources yet).
    - `test_get_projects_with_resource_returns_count_one`: create team, create project, POST a resource with `metadata.project = "payments-backend"` and `team=payments` query param (relies on Plan 03 wiring), then GET `/projects?team=payments` returns the project with `resource_count == 1`. This test will fail until Plan 03 lands the `project` write path — that is intentional.
    - `test_get_projects_without_team_returns_400`: GET `/projects` (no `team` param) returns 400 with `detail == "team parameter is required"` (ARCH-01 parity).
    - `test_post_projects_duplicate_name_in_same_team_returns_409`: two POSTs with same `(team, name)` — second returns 409 from `uq_projects_team_name`.

    `tests/test_api/test_deployments.py` — TEAM-03 (audit trail + env filter):
    - `test_post_resources_writes_deployment_row_with_change_type_create`: POST `/resources` with team=platform creates a deployment row. Verify by querying `GET /deployments?team=platform&environment=dev` (resource YAML has metadata.env=dev) — expect at least one row with `change_type="create"`. Will RED until Plan 03.
    - `test_delete_resources_writes_deployment_row_with_change_type_delete`: POST then DELETE — expect a deployment row with `change_type="delete"` retrievable via GET /deployments.
    - `test_post_resources_changed_spec_writes_deployment_row_with_change_type_update`: POST then POST with changed spec — expect a deployment row with `change_type="update"`.
    - `test_get_deployments_filters_by_environment`: create one resource in `env=prod` and one in `env=dev`, GET `/deployments?team=X&environment=prod` returns ONLY the prod-environment rows (resource_records joined for name/kind).
    - `test_get_deployments_without_team_returns_400`: ARCH-01 enforcement on the deployments route (matches CONTEXT and RESEARCH "Security Domain" section).
    - `test_get_deployments_sorted_newest_first`: create two resources in sequence; assert the returned list is ordered by `deployed_at` DESC.

    `tests/test_api/test_soft_delete.py` — D-11/D-12:
    - `test_delete_resource_sets_deleted_at_not_hard_delete`: POST resource returns 202, DELETE returns 204, then GET /resources/{id} returns 404 (soft-deleted resources are not visible via the single-resource endpoint).
    - `test_get_resources_filters_out_soft_deleted`: POST a resource, DELETE it, then GET `/resources?team=X` returns an empty list (not the soft-deleted resource).
    - `test_get_resource_by_id_returns_404_when_soft_deleted`: POST + DELETE + `await client.get(f"/resources/{resource_id}")` returns 404 (D-11 single-record lookup hides soft-deleted rows).
    - `test_pecp_get_does_not_show_deleted_resources`: integration check — POST, DELETE, list — soft-deleted resource is gone (mirrors `test_get_resources_filters_out_soft_deleted` from the route layer; this is the same assertion at the CLI-facing API surface).
    - `test_double_delete_returns_404_second_time`: POST, DELETE (204), DELETE again (404).

    `tests/test_api/test_cli.py` extensions — CLI-05/06/07/08 + --json:
    - `test_team_create_command_renders_panel`: mock `httpx.post` to return the full team body, invoke `pecp team create payments --owner alice --api-url http://t:8000`, assert exit_code 0, output contains `"payments"`, `"alice"`, and the literal string `"Members"` (table title).
    - `test_team_show_command_renders_members_table`: mock `httpx.get` to return team body with members, invoke `pecp team payments --api-url http://t:8000`, assert exit_code 0, output contains `"payments"`, `"alice"`, `"owner"`.
    - `test_team_command_json_flag_outputs_clean_json`: invoke with `--json`, assert output parses as JSON (`json.loads(result.output)` succeeds and returns a dict with key `"name"`).
    - `test_projects_command_renders_table_with_resource_count`: mock GET `/projects?team=X`, assert output contains project name and integer resource count.
    - `test_projects_command_json_flag_outputs_array`: invoke `pecp projects --team payments --json`, assert `json.loads(result.output)` is a list of dicts each with keys `id`, `name`, `environments`, `resource_count`.
    - `test_deployments_command_renders_sorted_newest_first`: mock GET `/deployments?team=X&environment=prod`, return two records with different `deployed_at`. Assert the output displays them in the order the API returned (CLI does not re-sort — API is responsible per D-16). Assert columns `Resource`, `Kind`, `Change`, `Status`, `Deployed` (or equivalent) appear.
    - `test_deployments_command_json_flag_outputs_array`: invoke with `--json`, assert `json.loads(result.output)` is a list.
    - `test_get_command_json_flag_outputs_array`: existing `pecp get` extended with `--json`; assert clean JSON list output.
    - `test_status_command_json_flag_outputs_object`: existing `pecp status` extended with `--json`; assert clean JSON object output containing `notes`, `activity_log`, `provider_metadata`.
    - `test_project_create_command_renders_confirmation`: mock `httpx.post` to return `{"id": "abc-123", "name": "payments-backend", "environments": ["dev", "staging", "prod"]}`, invoke `pecp project create payments-backend --team payments --env dev,staging,prod --api-url http://t:8000`, assert `exit_code == 0` and output contains `"payments-backend"` and `"abc-123"` (D-06).

    Each test scaffold uses the same import patterns as the existing test files. The `client` fixture is sourced from `tests/conftest.py`. CLI tests use `from typer.testing import CliRunner` and `from pecp.cli.main import app`. Tests are marked async via the existing `asyncio_mode = "auto"` setting in pyproject.toml — no explicit `@pytest.mark.asyncio` decorator needed.
  </behavior>
  <action>
    Create four new test files plus extend `tests/test_api/test_cli.py`. Use the import patterns from `tests/test_api/test_routes.py` (for client-fixture-based async tests), `tests/test_api/test_idempotency.py` (for body-bytes POST patterns), `tests/test_api/test_notes.py` (for POST-then-GET assertions), and `tests/test_api/test_cli.py` (for CliRunner + httpx mock patterns).

    For `tests/test_api/test_teams.py`: file docstring `"""Tests for TEAM-01 — team creation, member tracking, duplicate-name rejection.\n\nWave 0 scaffolds — FAIL intentionally until Plan 02 implements team routes."""`. Imports: `httpx.AsyncClient`. Define five async tests per the Behavior block. Use `await client.post("/teams", json=...)` for the request body (NOT YAML — teams use JSON per PATTERNS.md TeamCreate model). Assert status codes and response body shapes.

    For `tests/test_api/test_projects.py`: file docstring `"""Tests for TEAM-02 — project creation, listing with resource_count.\n\nWave 0 scaffolds — FAIL intentionally until Plan 03 implements project routes."""`. Use the helper pattern from `test_routes.py` to create teams/resources as preconditions. Reuse `_make_lambda_yaml(name)` (define it locally or copy from `test_routes.py` — copy is acceptable; do not introduce a shared fixture in this task). For the resource-with-project precondition test, include `project: payments-backend` under `metadata` in the YAML body.

    For `tests/test_api/test_deployments.py`: file docstring `"""Tests for TEAM-03 — deployment audit trail and environment filter.\n\nWave 0 scaffolds — FAIL intentionally until Plan 03 implements deployment trail."""`. Preconditions chain POST team → POST project → POST resource. Assertions on `GET /deployments?team=&environment=` body shape (each row contains `resource_name`, `kind`, `change_type`, `status`, `deployed_at`, `environment` per Pattern 2 in RESEARCH.md). For sort assertion, use two resources with deliberate slight time gap (await asyncio.sleep(0.01) between POSTs if needed) and assert `body[0]['deployed_at'] >= body[1]['deployed_at']`.

    For `tests/test_api/test_soft_delete.py`: file docstring `"""Tests for D-11 / D-12 — soft-delete invisibility and deleted_at filter.\n\nWave 0 scaffolds — FAIL intentionally until Plan 03 modifies resources.py for soft-delete."""`. For `test_delete_resource_sets_deleted_at_not_hard_delete`: POST a resource (202), DELETE it (204), then `GET /resources/{id}` returns 404. This definitively fails (RED) before Plan 03 implements the soft-delete filter on get_resource, requires only the `client` fixture, and becomes GREEN when Plan 03 adds `WHERE deleted_at IS NULL` to the get_resource handler. Do NOT reference the two-engine problem, the 202-POST workaround, or any TODO comments. All five tests use only the `client: AsyncClient` fixture.

    For `tests/test_api/test_cli.py`: APPEND new test functions to the existing file (do not modify existing ones). Add `import json` at the top of the file (already imported indirectly — verify). Each new test mocks `httpx.get`/`httpx.post` per the existing CliRunner pattern. For `--json` flag tests, parse `result.output` with `json.loads()`. The CLI commands `team`, `team create`, `projects`, `deployments` do not yet exist — `runner.invoke(app, ["team", "create", ...])` will exit non-zero with "No such command" message until Plan 02/03 implement them. Wave 0 RED is exactly this state.

    Do not invent fixtures. Do not add a conftest.py. Use what already exists.
  </action>
  <verify>
    <automated>cd /Users/punitlad/projects/pecp-poc &amp;&amp; python -c "import ast; [ast.parse(open(p).read()) for p in ('tests/test_api/test_teams.py', 'tests/test_api/test_projects.py', 'tests/test_api/test_deployments.py', 'tests/test_api/test_soft_delete.py', 'tests/test_api/test_cli.py')]; print('OK')" &amp;&amp; pytest tests/test_api/test_teams.py tests/test_api/test_projects.py tests/test_api/test_deployments.py tests/test_api/test_soft_delete.py --collect-only -q 2>&amp;1 | grep -E 'test_|error' | head -40 &amp;&amp; pytest tests/test_api/test_cli.py -k "team_create_command or projects_command or deployments_command or json_flag" --collect-only -q 2>&amp;1 | grep -E 'test_|error' | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_api/test_teams.py` exists and contains test functions: `test_post_teams_creates_team_and_owner_member`, `test_post_teams_duplicate_name_returns_409`, `test_get_teams_by_name_returns_members`, `test_get_teams_unknown_name_returns_404`, `test_post_teams_missing_owner_returns_422`.
    - File `tests/test_api/test_projects.py` exists and contains test functions: `test_post_projects_creates_project_with_environments`, `test_get_projects_lists_team_projects_with_resource_count`, `test_get_projects_with_resource_returns_count_one`, `test_get_projects_without_team_returns_400`, `test_post_projects_duplicate_name_in_same_team_returns_409`.
    - File `tests/test_api/test_deployments.py` exists and contains test functions: `test_post_resources_writes_deployment_row_with_change_type_create`, `test_delete_resources_writes_deployment_row_with_change_type_delete`, `test_post_resources_changed_spec_writes_deployment_row_with_change_type_update`, `test_get_deployments_filters_by_environment`, `test_get_deployments_without_team_returns_400`, `test_get_deployments_sorted_newest_first`.
    - File `tests/test_api/test_soft_delete.py` exists and contains test functions: `test_delete_resource_sets_deleted_at_not_hard_delete`, `test_get_resources_filters_out_soft_deleted`, `test_get_resource_by_id_returns_404_when_soft_deleted`, `test_pecp_get_does_not_show_deleted_resources`, `test_double_delete_returns_404_second_time`.
    - `tests/test_api/test_cli.py` contains all of: `test_team_create_command_renders_panel`, `test_team_show_command_renders_members_table`, `test_team_command_json_flag_outputs_clean_json`, `test_projects_command_renders_table_with_resource_count`, `test_projects_command_json_flag_outputs_array`, `test_deployments_command_renders_sorted_newest_first`, `test_deployments_command_json_flag_outputs_array`, `test_get_command_json_flag_outputs_array`, `test_status_command_json_flag_outputs_object`, `test_project_create_command_renders_confirmation`.
    - Every new test file parses as valid Python (no `SyntaxError` from `ast.parse`).
    - `pytest tests/test_api/test_teams.py tests/test_api/test_projects.py tests/test_api/test_deployments.py tests/test_api/test_soft_delete.py --collect-only -q` collects all tests without `ERROR` lines (collection passes; tests may FAIL when run, but they must COLLECT cleanly — no ImportError).
    - `ruff check tests/test_api/test_teams.py tests/test_api/test_projects.py tests/test_api/test_deployments.py tests/test_api/test_soft_delete.py tests/test_api/test_cli.py` exits 0.
  </acceptance_criteria>
  <done>
    All four new test files exist with the named test functions, all CLI test extensions exist in `test_cli.py`. The test suite collects successfully (no import errors). Wave 0 RED is achieved: the tests run and FAIL because the routes and commands they target do not yet exist. Plan 02 turns the team-related tests green; Plan 03 turns the projects, deployments, soft-delete, and `--json`-extension tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pydantic validation at API edge (POST /teams body, POST /projects body) | Untrusted JSON input parsed into `TeamCreate` / `ProjectCreate` Pydantic models. Phase 4 does not yet add these route handlers; this plan only adds the data model. |
| Alembic migration apply | Migration runs against the production SQLite file on startup or via explicit `alembic upgrade head`. The migration is code-as-data — same trust as source code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | Alembic migration 0003 columns vs ORM models | mitigate | Task 2 verify step compares actual SQLite schema (PRAGMA table_info) against expected columns and constraints. Task 1 acceptance includes `python -c` import check ensuring ORM matches. |
| T-04-01-02 | Information Disclosure | New `team_members.role` field exposes member roles | accept | PoC has no auth; `role` is free-text Text column and not personally identifying. Plan 02 will return roles in `GET /teams/{name}` response (intended visibility). |
| T-04-01-03 | Denial of Service | Alembic migration locks the SQLite DB during upgrade | accept | SQLite single-writer model; PoC has no concurrent traffic during migration. Migration runs offline before server start. |
| T-04-01-04 | Tampering | `Base.metadata.create_all` in conftest fixtures creates the new tables for tests, masking a broken Alembic migration | mitigate | Task 2 verify step runs `alembic upgrade head` on a real file-backed SQLite DB (not in-memory) and inspects table presence + column presence. This catches Alembic-only bugs that `create_all` would hide. |
| T-04-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced in Phase 4 (per 04-RESEARCH.md "No New Packages Required"). No legitimacy checkpoint needed. |
</threat_model>

<verification>
- All five files compile (no SyntaxError).
- `python -c "from pecp.persistence.models import TeamRecord, TeamMemberRecord, ProjectRecord, DeploymentRecord; from pecp.models.resource_spec import ResourceMetadata; assert ResourceMetadata(name='x').project is None"` exits 0.
- `alembic upgrade head` exits 0 on a fresh DB; resulting schema has tables `teams`, `team_members`, `projects`, `deployments` + columns `project`, `deleted_at` on `resource_records`.
- `alembic downgrade 0002` exits 0 and removes all five additions.
- `pytest tests/test_api/test_teams.py tests/test_api/test_projects.py tests/test_api/test_deployments.py tests/test_api/test_soft_delete.py --collect-only -q` collects all tests with no errors.
- `ruff check src/pecp/persistence/models.py src/pecp/models/resource_spec.py alembic/versions/0003_add_teams_projects_deployments.py tests/test_api/test_teams.py tests/test_api/test_projects.py tests/test_api/test_deployments.py tests/test_api/test_soft_delete.py tests/test_api/test_cli.py` exits 0.
- `mypy src/pecp/persistence/models.py src/pecp/models/resource_spec.py` exits 0.
- Existing tests that depend on `Base.metadata.create_all` (the `client` and `db_session` conftest fixtures) continue to pass — running `pytest tests/test_api/test_idempotency.py tests/test_api/test_routes.py -x -q` exits 0.
</verification>

<success_criteria>
- All Phase 4 schema additions are present in both the ORM models and migration 0003.
- All Wave 0 RED test scaffolds exist and import cleanly; they fail because Plan 02/03 routes/commands do not yet exist, NOT because of test infrastructure problems.
- Existing tests do not regress (Phase 3 idempotency, routes, CLI tests pass unchanged).
- The plan delivers the validation contract: every subsequent Phase 4 task can claim "done" only when its corresponding Wave 0 test turns green.
</success_criteria>

<output>
Create `.planning/phases/04-teams-projects-deployments/04-01-SUMMARY.md` when done. Include:
- Tables created (teams, team_members, projects, deployments)
- ResourceRecord columns added (project, deleted_at)
- ResourceMetadata field added (project)
- Alembic revision created (0003)
- Wave 0 test files created and test counts per file
- Any deviations from the plan and why
- Confirmation that all existing tests still pass
</output>
