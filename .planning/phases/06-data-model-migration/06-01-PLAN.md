---
phase: 06-data-model-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pecp/persistence/models.py
  - alembic/versions/0004_add_github_fields.py
  - tests/test_migration.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
tags:
  - alembic
  - migration
  - sqlalchemy
  - schema
  - sqlite

must_haves:
  truths:
    - "Running `alembic upgrade head` from revision 0003 against a fresh SQLite file adds the `github_team_slug` column to `teams` and creates the `project_repos` table"
    - "Running `alembic downgrade -1` after the upgrade removes both the `github_team_slug` column and the `project_repos` table"
    - "Existing team rows surviving the migration retain all original column values; `github_team_slug` is `NULL` for them"
    - "Inserting a `ProjectRepoRecord` with a duplicate `(project_id, repo_name)` pair raises an IntegrityError due to `uq_project_repos_project_name`"
    - "All 165 existing tests continue to pass after `models.py` is modified (no regression)"
  artifacts:
    - path: "alembic/versions/0004_add_github_fields.py"
      provides: "Atomic migration adding github_team_slug column and project_repos table"
      contains: "revision = \"0004\""
    - path: "src/pecp/persistence/models.py"
      provides: "TeamRecord.github_team_slug field and ProjectRepoRecord class"
      contains: "class ProjectRepoRecord"
    - path: "tests/test_migration.py"
      provides: "Upgrade/downgrade smoke test for migration 0004"
      contains: "def test_migration_upgrade_and_downgrade"
  key_links:
    - from: "alembic/versions/0004_add_github_fields.py"
      to: "alembic/versions/0003_add_teams_projects_deployments.py"
      via: "down_revision chain — 0004 revises 0003"
      pattern: "down_revision = \"0003\""
    - from: "src/pecp/persistence/models.py"
      to: "alembic/versions/0004_add_github_fields.py"
      via: "ORM model fields must match migration DDL (column names, types, nullability, constraint names)"
      pattern: "github_team_slug.*Text.*nullable=True"
    - from: "tests/test_migration.py"
      to: "alembic/env.py"
      via: "Test monkeypatches PECP_DATABASE_URL and reloads pecp.persistence.database so env.py imports the temp DB URL"
      pattern: "monkeypatch.setenv.*PECP_DATABASE_URL"
    - from: "src/pecp/persistence/models.py (ProjectRepoRecord.project_id)"
      to: "src/pecp/persistence/models.py (ProjectRecord.id)"
      via: "ForeignKey(\"projects.id\") — references existing projects table"
      pattern: "ForeignKey\\(\"projects\\.id\"\\)"
---

<objective>
Deliver Alembic migration 0004 that atomically adds a nullable `github_team_slug Text` column to the `teams` table and creates a new `project_repos` table (with FK to `projects.id` and a `uq_project_repos_project_name` unique constraint), backed by matching `TeamRecord` and `ProjectRepoRecord` ORM model changes and a smoke test that proves the migration applies and rolls back cleanly.

Purpose: Unblock Phases 7–10 (GitHub integration framework, GitHub adapter, service layer, CLI) by giving them the data model surface they will read and write — without touching any application logic in this phase.
Output: One new migration file, one modified ORM module, one new test file, all backed by `python -m pytest tests/test_migration.py -x -q` passing and the full suite continuing to pass with no regression.
</objective>

<artifacts_this_phase_produces>
This phase introduces the following new symbols / files. The plan-review-convergence source-grounding pass MUST treat these as in-flight (not "drift") because they do not yet exist at planning time:

**New files:**
- `alembic/versions/0004_add_github_fields.py`
- `tests/test_migration.py`

**New module-level symbols in `alembic/versions/0004_add_github_fields.py`:**
- `revision = "0004"`
- `down_revision = "0003"`
- `branch_labels = None`
- `depends_on = None`
- `upgrade()` function
- `downgrade()` function

**New class in `src/pecp/persistence/models.py`:**
- `ProjectRepoRecord` (ORM class with `__tablename__ = "project_repos"`)
  - Fields: `id`, `project_id`, `repo_name`, `repo_url`, `created_at`
  - `__table_args__ = (UniqueConstraint("project_id", "repo_name", name="uq_project_repos_project_name"),)`

**New field on existing class `TeamRecord` in `src/pecp/persistence/models.py`:**
- `github_team_slug: Mapped[str | None]` (nullable Text column)

**New schema objects (DB-level) created by migration 0004:**
- Table `project_repos` with columns `id`, `project_id`, `repo_name`, `repo_url`, `created_at`
- Constraint `uq_project_repos_project_name` on `(project_id, repo_name)`
- Foreign key from `project_repos.project_id` → `projects.id`
- Column `github_team_slug` on `teams` table (TEXT, nullable, no server_default)

**New test function in `tests/test_migration.py`:**
- `test_migration_upgrade_and_downgrade(tmp_path, monkeypatch)`
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-data-model-migration/06-CONTEXT.md
@.planning/phases/06-data-model-migration/06-RESEARCH.md
@.planning/phases/06-data-model-migration/06-PATTERNS.md
@src/pecp/persistence/models.py
@src/pecp/persistence/database.py
@alembic/env.py
@alembic/versions/0003_add_teams_projects_deployments.py
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ProjectRepoRecord ORM class and github_team_slug field to models.py</name>
  <files>src/pecp/persistence/models.py</files>
  <read_first>
    - src/pecp/persistence/models.py (current ORM module — already imports Text, ForeignKey, UniqueConstraint, Mapped, mapped_column, datetime, timezone; defines Base, ResourceRecord, TeamRecord, TeamMemberRecord, ProjectRecord, DeploymentRecord)
    - .planning/phases/06-data-model-migration/06-PATTERNS.md (column type rules: PK String, FK String, other strings Text; nullable no-default pattern; created_at default lambda pattern; __table_args__ single-element tuple with trailing comma)
    - .planning/phases/06-data-model-migration/06-CONTEXT.md (D-01 unique constraint name; D-02 unique covers repo_name only; D-03 Text nullable no default; D-04 store slug not URL)
  </read_first>
  <behavior>
    - The ORM class `ProjectRepoRecord` exists, inherits from `Base`, maps to table `project_repos`, and exposes typed `Mapped[]` attributes `id` (PK String), `project_id` (FK String → projects.id), `repo_name` (Text), `repo_url` (Text), `created_at` (DateTime(timezone=True), nullable False, default lambda returning datetime.now(timezone.utc))
    - `ProjectRepoRecord.__table_args__` is a single-element tuple containing `UniqueConstraint("project_id", "repo_name", name="uq_project_repos_project_name")` with a trailing comma after the constraint
    - `TeamRecord` gains a single new typed attribute `github_team_slug: Mapped[str | None] = mapped_column(Text, nullable=True)` appended after `created_at`, with no `server_default` and no `default` kwarg
    - `Base.metadata.create_all()` against an in-memory SQLite engine succeeds with the new model registered (proven indirectly by the existing test suite continuing to pass)
    - All 165 existing tests continue to pass — adding the new column and class does not break any current behavior
  </behavior>
  <action>
    Edit `src/pecp/persistence/models.py`. Make exactly two scoped additions; do not modify any existing column, class, import, or docstring beyond the additions below.

    Addition 1 — add one line to `TeamRecord` (per D-01, D-03, D-04 in 06-CONTEXT.md): append `github_team_slug` as the last typed attribute of the class, immediately after `created_at`. The column MUST be declared as `Mapped[str | None]` mapped via `mapped_column(Text, nullable=True)` — no `default`, no `server_default`, no `index=True`. The pre-existing `__table_args__` tuple containing `UniqueConstraint("name", name="uq_teams_name")` MUST remain unchanged.

    Addition 2 — append a new class `ProjectRepoRecord` at end of file, after `DeploymentRecord`. Use the existing `ProjectRecord` class (lines 95–114) as the structural template (PK pattern, FK pattern, created_at default, `__table_args__` tuple shape). Concrete required attributes:
    - `__tablename__ = "project_repos"`
    - `__table_args__ = (UniqueConstraint("project_id", "repo_name", name="uq_project_repos_project_name"),)` — single-element tuple, trailing comma required
    - `id: Mapped[str] = mapped_column(String, primary_key=True)` — PK uses String (not Text), matching existing PK pattern
    - `project_id: Mapped[str] = mapped_column(String, ForeignKey("projects.id"), nullable=False)` — FK uses String (not Text); bare `ForeignKey("projects.id")` with no `ondelete` argument (per 06-PATTERNS.md "FK Without ondelete")
    - `repo_name: Mapped[str] = mapped_column(Text, nullable=False)`
    - `repo_url: Mapped[str] = mapped_column(Text, nullable=False)`
    - `created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, default=lambda: datetime.now(timezone.utc))` — exactly the `ProjectRecord.created_at` pattern

    Include a class docstring matching the project's existing one-paragraph docstring style (see `ProjectRecord` and `DeploymentRecord` for tone). Do NOT add a `relationship()` — no model in this codebase declares `relationship()`. Do NOT add any new imports — `Text`, `String`, `ForeignKey`, `UniqueConstraint`, `DateTime`, `Mapped`, `mapped_column`, `datetime`, and `timezone` are already imported.

    After saving, run `python -m pytest tests/ -x -q` and confirm all existing tests still pass. If any test fails, fix the model (likely a typo in `ForeignKey("projects.id")` or a missing trailing comma in `__table_args__`) before proceeding to the next task.
  </action>
  <verify>
    <automated>python -c "from pecp.persistence.models import TeamRecord, ProjectRepoRecord, Base; assert hasattr(TeamRecord, 'github_team_slug'); assert ProjectRepoRecord.__tablename__ == 'project_repos'; assert any(uc.name == 'uq_project_repos_project_name' for uc in ProjectRepoRecord.__table_args__); import asyncio; from sqlalchemy.ext.asyncio import create_async_engine; e = create_async_engine('sqlite+aiosqlite:///:memory:'); asyncio.run((lambda: __import__('asyncio').get_event_loop())())" && python -m pytest tests/ -x -q --ignore=tests/test_migration.py</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -E "github_team_slug: Mapped\[str \| None\] = mapped_column\(Text, nullable=True\)" src/pecp/persistence/models.py` returns exactly 1 line
    - Source assertion: `grep -E "^class ProjectRepoRecord\(Base\):" src/pecp/persistence/models.py` returns exactly 1 line
    - Source assertion: `grep -E "__tablename__ = \"project_repos\"" src/pecp/persistence/models.py` returns exactly 1 line
    - Source assertion: `grep -E "UniqueConstraint\(\"project_id\", \"repo_name\", name=\"uq_project_repos_project_name\"\)" src/pecp/persistence/models.py` returns exactly 1 line
    - Source assertion: `grep -E "ForeignKey\(\"projects\\.id\"\)" src/pecp/persistence/models.py` returns at least 1 line (the new ProjectRepoRecord FK joins existing DeploymentRecord.project_id usage; the count after this task is exactly 2)
    - Source assertion (negative): `grep -E "ondelete" src/pecp/persistence/models.py` returns 0 lines
    - Source assertion (negative): `grep -E "github_team_slug.*server_default" src/pecp/persistence/models.py` returns 0 lines
    - Behavior assertion: `python -c "from pecp.persistence.models import ProjectRepoRecord, TeamRecord; assert TeamRecord.github_team_slug.property.columns[0].nullable is True; assert ProjectRepoRecord.project_id.property.columns[0].foreign_keys"` exits 0
    - Test-command assertion: `python -m pytest tests/ -x -q --ignore=tests/test_migration.py` returns exit code 0 with no regressions in any of the 165 existing tests
  </acceptance_criteria>
  <done>
    TeamRecord has a nullable `github_team_slug: Mapped[str | None]` field with `Text` type and no defaults; a new `ProjectRepoRecord` class exists at the bottom of `models.py` with `id`/`project_id`/`repo_name`/`repo_url`/`created_at` fields and the `uq_project_repos_project_name` unique constraint; the existing test suite (excluding tests/test_migration.py which doesn't exist yet) passes without modification.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Alembic migration 0004 (github_team_slug column + project_repos table)</name>
  <files>alembic/versions/0004_add_github_fields.py</files>
  <read_first>
    - alembic/versions/0003_add_teams_projects_deployments.py (revision/down_revision header, batch_alter_table pattern for resource_records column adds, op.create_table patterns with ForeignKeyConstraint + UniqueConstraint, reverse-order downgrade)
    - alembic/env.py (already has render_as_batch=True in do_run_migrations; no changes needed)
    - src/pecp/persistence/models.py (now contains the ORM definitions whose DDL the migration must produce — column types and constraint names must agree)
    - .planning/phases/06-data-model-migration/06-RESEARCH.md (sections "Pattern 1", "Pattern 2", "Common Pitfalls", "Anti-Patterns to Avoid")
    - .planning/phases/06-data-model-migration/06-CONTEXT.md (D-01 through D-06 — locks all DDL decisions)
  </read_first>
  <behavior>
    - File `alembic/versions/0004_add_github_fields.py` exists with module-level identifiers `revision = "0004"`, `down_revision = "0003"`, `branch_labels = None`, `depends_on = None`
    - `upgrade()` first uses `op.batch_alter_table("teams")` to add a `github_team_slug` column of type `sa.Text()` with `nullable=True` and no `server_default`, then calls `op.create_table("project_repos", ...)` with columns `id` (String, not nullable), `project_id` (String, not nullable), `repo_name` (Text, not nullable), `repo_url` (Text, not nullable), `created_at` (DateTime(timezone=True), not nullable), and constraints `ForeignKeyConstraint(["project_id"], ["projects.id"])` (no ondelete), `PrimaryKeyConstraint("id")`, `UniqueConstraint("project_id", "repo_name", name="uq_project_repos_project_name")`
    - `downgrade()` reverses upgrade in strict reverse order: `op.drop_table("project_repos")` first, then `op.batch_alter_table("teams")` to drop `github_team_slug`
    - `python -m alembic upgrade head` from current revision 0003 advances the live `pecp.db` to revision 0004 without error
    - `python -m alembic downgrade -1` from revision 0004 returns to revision 0003 without error and removes both the column and the table
  </behavior>
  <action>
    Create a new file `alembic/versions/0004_add_github_fields.py` modeled exactly on `alembic/versions/0003_add_teams_projects_deployments.py`. Do NOT use `op.execute()` with raw SQL — use only the `op.batch_alter_table` / `op.create_table` / `op.drop_table` Alembic operations.

    Module header (matching 0003's style):
    - Docstring (one paragraph): `"""Add github_team_slug to teams and create project_repos table.\n\nRevision ID: 0004\nRevises: 0003\nCreate Date: 2026-06-24\n"""`
    - Imports: `import sqlalchemy as sa` and `from alembic import op` — same as 0003
    - Module-level constants: `revision = "0004"`, `down_revision = "0003"`, `branch_labels = None`, `depends_on = None`

    `upgrade()` body (exactly two operations, in this order — per D-01..D-04 and 06-RESEARCH.md Pattern 1+2):
    1. Add the column to the existing `teams` table using batch mode (required for SQLite, per 06-RESEARCH.md anti-pattern note): use `op.batch_alter_table("teams")` as a context manager and inside call `batch_op.add_column(sa.Column("github_team_slug", sa.Text(), nullable=True))`. Do NOT pass `server_default` to the column.
    2. Create the new table: `op.create_table("project_repos", ...)` with column specs in this exact order — `sa.Column("id", sa.String(), nullable=False)`, `sa.Column("project_id", sa.String(), nullable=False)`, `sa.Column("repo_name", sa.Text(), nullable=False)`, `sa.Column("repo_url", sa.Text(), nullable=False)`, `sa.Column("created_at", sa.DateTime(timezone=True), nullable=False)`. Then add the constraints in this exact order — `sa.ForeignKeyConstraint(["project_id"], ["projects.id"])` (bare, no `ondelete`), `sa.PrimaryKeyConstraint("id")`, `sa.UniqueConstraint("project_id", "repo_name", name="uq_project_repos_project_name")`.

    `downgrade()` body (reverse order — newest object dropped first, per 06-RESEARCH.md Pitfall 4):
    1. `op.drop_table("project_repos")`
    2. `op.batch_alter_table("teams")` context manager containing `batch_op.drop_column("github_team_slug")`

    After saving, run `python -m alembic upgrade head` against the live `pecp.db` (current revision is 0003 per RESEARCH.md "Environment Availability") to confirm the migration applies cleanly, then run `python -m alembic downgrade -1` to confirm it rolls back, then run `python -m alembic upgrade head` again to leave the live DB at 0004. Run `python -m alembic current` and confirm the revision is now `0004 (head)`.
  </action>
  <verify>
    <automated>test -f alembic/versions/0004_add_github_fields.py && grep -E '^revision = "0004"' alembic/versions/0004_add_github_fields.py | grep -v '^#' | grep -c 'revision = "0004"' | grep -q '^1$' && grep -E '^down_revision = "0003"' alembic/versions/0004_add_github_fields.py | grep -v '^#' | grep -c 'down_revision = "0003"' | grep -q '^1$' && python -m alembic upgrade head && python -m alembic downgrade -1 && python -m alembic upgrade head && python -m alembic current 2>&1 | grep -q "0004"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -E '^revision = "0004"$' alembic/versions/0004_add_github_fields.py | grep -v '^#' | wc -l` returns 1
    - Source assertion: `grep -E '^down_revision = "0003"$' alembic/versions/0004_add_github_fields.py | grep -v '^#' | wc -l` returns 1
    - Source assertion: `grep -E 'batch_alter_table\("teams"\)' alembic/versions/0004_add_github_fields.py | grep -v '^#' | wc -l` returns 2 (once in upgrade, once in downgrade)
    - Source assertion: `grep -E 'add_column\(sa\.Column\("github_team_slug", sa\.Text\(\), nullable=True\)\)' alembic/versions/0004_add_github_fields.py | grep -v '^#' | wc -l` returns 1
    - Source assertion: `grep -E 'drop_column\("github_team_slug"\)' alembic/versions/0004_add_github_fields.py | grep -v '^#' | wc -l` returns 1
    - Source assertion: `grep -E 'op\.create_table\(\s*$|op\.create_table\("project_repos"' alembic/versions/0004_add_github_fields.py | grep -v '^#' | wc -l` returns at least 1
    - Source assertion: `grep -E 'op\.drop_table\("project_repos"\)' alembic/versions/0004_add_github_fields.py | grep -v '^#' | wc -l` returns 1
    - Source assertion: `grep -E 'UniqueConstraint\("project_id", "repo_name", name="uq_project_repos_project_name"\)' alembic/versions/0004_add_github_fields.py | grep -v '^#' | wc -l` returns 1
    - Source assertion: `grep -E 'ForeignKeyConstraint\(\["project_id"\], \["projects\.id"\]\)' alembic/versions/0004_add_github_fields.py | grep -v '^#' | wc -l` returns 1
    - Source assertion (negative): `grep -E 'server_default' alembic/versions/0004_add_github_fields.py | grep -v '^#' | wc -l` returns 0
    - Source assertion (negative): `grep -E 'ondelete' alembic/versions/0004_add_github_fields.py | grep -v '^#' | wc -l` returns 0
    - Source assertion (negative): `grep -E 'op\.execute\(' alembic/versions/0004_add_github_fields.py | grep -v '^#' | wc -l` returns 0 (no raw SQL — per security threat T-06-01)
    - CLI assertion: `python -m alembic current 2>&1` output contains the string `0004`
    - Behavior assertion: `python -m alembic downgrade -1 && python -m alembic upgrade head` both exit 0 with no errors
    - Behavior assertion (downgrade is symmetric): after running `python -m alembic downgrade -1`, `python -m alembic current` output contains `0003`; then after re-running `python -m alembic upgrade head`, output contains `0004`
  </acceptance_criteria>
  <done>
    File `alembic/versions/0004_add_github_fields.py` exists with the exact revision chain (0004 → 0003); `upgrade()` adds the column via batch mode and creates `project_repos` via `op.create_table` with the FK and named unique constraint; `downgrade()` reverses both in correct order; the live `pecp.db` is at revision `0004 (head)`; round-trip upgrade/downgrade/upgrade succeeds.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add tests/test_migration.py upgrade+downgrade smoke test</name>
  <files>tests/test_migration.py</files>
  <read_first>
    - tests/conftest.py (existing test fixture patterns; note `os.environ.setdefault("PECP_DATABASE_URL", ...)` at line 14 — this only SETS if unset, so monkeypatch.setenv is required to OVERRIDE inside this test)
    - alembic/env.py (does `from pecp.persistence.database import DATABASE_URL` at import time — this is what forces the importlib.reload pattern below)
    - src/pecp/persistence/database.py (DATABASE_URL is bound at module-import time from PECP_DATABASE_URL — reload re-binds it)
    - alembic/versions/0004_add_github_fields.py (the migration this test exercises)
    - .planning/phases/06-data-model-migration/06-RESEARCH.md (Pattern 4 in-process Alembic test; Common Pitfalls 1, 2, 4; Validation Architecture section)
    - .planning/phases/06-data-model-migration/06-PATTERNS.md (test file role-match; sync engine for inspect; file-based SQLite required)
  </read_first>
  <behavior>
    - File `tests/test_migration.py` exists with a single test function `test_migration_upgrade_and_downgrade(tmp_path, monkeypatch)` that takes the standard pytest `tmp_path` and `monkeypatch` fixtures
    - The test creates a file-based SQLite DB path under `tmp_path` (not `:memory:` — per RESEARCH Pitfall 1), sets `PECP_DATABASE_URL` to that file's `sqlite+aiosqlite:///` URL via `monkeypatch.setenv`, reloads `pecp.persistence.database` so `alembic/env.py`'s import-time read of `DATABASE_URL` picks up the new value
    - Calling `alembic.command.upgrade(cfg, "head")` against `alembic.config.Config("alembic.ini")` runs all migrations 0000→0004 against the temp DB without error
    - Schema inspection via `sqlalchemy.inspect()` on a sync engine (`sqlite:///` URL — per RESEARCH anti-pattern note) confirms after upgrade: `teams` table has a `github_team_slug` column; `project_repos` table exists; `project_repos` has columns `id`, `project_id`, `repo_name`, `repo_url`, `created_at`; a unique constraint named `uq_project_repos_project_name` is present on `project_repos`; the FK from `project_repos.project_id` targets table `projects`
    - Calling `alembic.command.downgrade(cfg, "-1")` rolls back to revision 0003; re-inspection confirms `github_team_slug` is gone from `teams` and `project_repos` is no longer in the table list
    - The test passes when run with `python -m pytest tests/test_migration.py -x -q` and does NOT mutate the live `./pecp.db` (the live DB is untouched because the test points Alembic at `tmp_path/test_migration.db`)
    - Running the full suite `python -m pytest tests/ -x -q` after this test exists shows the test count increased by 1 from the prior 165 baseline (to 166) and all tests pass
  </behavior>
  <action>
    Create `tests/test_migration.py` with a single test function `test_migration_upgrade_and_downgrade(tmp_path, monkeypatch)` that exercises the full upgrade/downgrade cycle of migration 0004 in-process. Build it using only the standard library (`importlib`, `pathlib`), `pytest`, `alembic.command`, `alembic.config`, and `sqlalchemy` — no new project dependencies.

    Required test structure (per 06-RESEARCH.md Pattern 4 and 06-PATTERNS.md):

    1. Compute paths: `db_path = tmp_path / "test_migration.db"`, `async_url = f"sqlite+aiosqlite:///{db_path}"`, `sync_url = f"sqlite:///{db_path}"`. The file path MUST be inside `tmp_path` (per 06-RESEARCH.md Pitfall 1 — in-memory SQLite fails Alembic's multi-connection pattern).

    2. Isolate the test DB from the live `pecp.db`: call `monkeypatch.setenv("PECP_DATABASE_URL", async_url)`, then `import pecp.persistence.database as db_module` and `importlib.reload(db_module)`. The reload is mandatory because `alembic/env.py` does `from pecp.persistence.database import DATABASE_URL` at exec time (per 06-RESEARCH.md Pitfall 2). The monkeypatch fixture auto-restores the env var after the test; explicitly reload `pecp.persistence.database` again at the end of the test (after assertions) so subsequent tests in the same pytest session see the original DATABASE_URL (per RESEARCH Open Question 1).

    3. Configure Alembic: `cfg = alembic.config.Config("alembic.ini")` — no need to call `cfg.set_main_option("sqlalchemy.url", ...)` because env.py bypasses it (per anti-pattern note).

    4. Run upgrade: `alembic.command.upgrade(cfg, "head")`. This runs the full chain 0000→0004 against the temp DB.

    5. Inspect post-upgrade schema using a SYNC engine (`create_engine(sync_url)` — not `create_async_engine`, per 06-RESEARCH.md anti-pattern note). Use `sqlalchemy.inspect(engine)` and assert:
       - `"github_team_slug" in [c["name"] for c in insp.get_columns("teams")]`
       - `"project_repos" in insp.get_table_names()`
       - The set of column names on `project_repos` is a superset of `{"id", "project_id", "repo_name", "repo_url", "created_at"}`
       - `"uq_project_repos_project_name" in [uc["name"] for uc in insp.get_unique_constraints("project_repos")]`
       - At least one FK on `project_repos` has `referred_table == "projects"`
       - Call `engine.dispose()` before proceeding (release file handle so downgrade's connection has clean access on Windows-compatible code, but primarily a hygiene step here).

    6. Run downgrade: `alembic.command.downgrade(cfg, "-1")` — should leave DB at revision 0003.

    7. Inspect post-downgrade schema with a fresh sync engine (`create_engine(sync_url)`) and assert:
       - `"github_team_slug" not in [c["name"] for c in insp2.get_columns("teams")]`
       - `"project_repos" not in insp2.get_table_names()`
       - Call `engine.dispose()`.

    8. Restore prior session state: re-run `importlib.reload(db_module)` once more at the end of the test — monkeypatch will have restored `PECP_DATABASE_URL` to its prior value by the time the next test runs, but performing one explicit reload here ensures the `db_module.DATABASE_URL` attribute is re-bound to the pre-test value before this function returns.

    Do NOT use `:memory:` SQLite. Do NOT use `cfg.set_main_option("sqlalchemy.url", ...)`. Do NOT use `create_async_engine` for the inspection step. Do NOT call `Base.metadata.create_all()` — Alembic owns schema creation in this test.

    Add a top-of-file module docstring summarizing the test purpose (one paragraph, matching conftest.py docstring style).
  </action>
  <verify>
    <automated>python -m pytest tests/test_migration.py -x -q && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - File assertion: `test -f tests/test_migration.py` exits 0
    - Source assertion: `grep -E "^def test_migration_upgrade_and_downgrade\(tmp_path, monkeypatch\)" tests/test_migration.py | grep -v '^#' | wc -l` returns 1
    - Source assertion: `grep -E "monkeypatch\.setenv\(\"PECP_DATABASE_URL\"" tests/test_migration.py | grep -v '^#' | wc -l` returns at least 1
    - Source assertion: `grep -E "importlib\.reload\(db_module\)" tests/test_migration.py | grep -v '^#' | wc -l` returns at least 1
    - Source assertion: `grep -E "alembic\.command\.upgrade\(cfg, \"head\"\)" tests/test_migration.py | grep -v '^#' | wc -l` returns 1
    - Source assertion: `grep -E "alembic\.command\.downgrade\(cfg, \"-1\"\)" tests/test_migration.py | grep -v '^#' | wc -l` returns 1
    - Source assertion: `grep -E "tmp_path / \"test_migration\.db\"" tests/test_migration.py | grep -v '^#' | wc -l` returns 1
    - Source assertion (negative): `grep -E ":memory:" tests/test_migration.py | grep -v '^#' | wc -l` returns 0 (file-based DB required per RESEARCH Pitfall 1)
    - Source assertion (negative): `grep -E "create_async_engine" tests/test_migration.py | grep -v '^#' | wc -l` returns 0 (sync engine required for inspect per RESEARCH anti-pattern)
    - Source assertion (negative): `grep -E "set_main_option" tests/test_migration.py | grep -v '^#' | wc -l` returns 0 (cfg override is ignored — must use module reload)
    - Test-command assertion: `python -m pytest tests/test_migration.py -x -q` returns exit code 0
    - Test-command assertion: `python -m pytest tests/ -x -q` returns exit code 0 with collected test count >= 166 (165 prior + at least 1 new in test_migration.py)
    - Behavior assertion (test isolation): After running the migration test, `python -m alembic current` against the live `pecp.db` still reports revision `0004` (the test must not have downgraded or altered the live DB)
  </acceptance_criteria>
  <done>
    `tests/test_migration.py` exists with a single passing test that runs `alembic upgrade head` against an isolated `tmp_path`-scoped SQLite file, asserts the github_team_slug column and project_repos table (with FK and unique constraint) exist via sync `sqlalchemy.inspect`, runs `alembic downgrade -1`, asserts both are gone, and leaves the live `pecp.db` untouched. Full suite green at >= 166 tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem→migration runner | `alembic upgrade head` writes DDL to the SQLite file at `DATABASE_URL` — accidental targeting of the live `pecp.db` in tests would corrupt prod-shape data |
| test→live DB | `tests/test_migration.py` controls which SQLite file Alembic writes to via `PECP_DATABASE_URL` env var; misuse of `os.environ.setdefault` (vs `monkeypatch.setenv`) could let the test silently fall back to the live DB |
| migration source→DB engine | `op.execute(raw_sql)` would bypass Alembic's parameterized DDL; not used in this phase, but a negative gate enforces it stays out |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `alembic/versions/0004_add_github_fields.py` | mitigate | Use only `op.batch_alter_table`, `op.create_table`, `op.drop_table` — never `op.execute(raw_sql)`. Acceptance criterion in Task 2 negative-greps for `op.execute(` (count == 0). Aligns with 06-RESEARCH.md security domain "SQL injection via hand-rolled DDL strings". |
| T-06-02 | Tampering | `tests/test_migration.py` | mitigate | Use `monkeypatch.setenv("PECP_DATABASE_URL", ...)` (not `os.environ.setdefault`) and `tmp_path / "test_migration.db"` (not `:memory:`). Task 3 acceptance criteria negative-grep for `:memory:` (count == 0) and assert live `pecp.db` revision stays at 0004 post-test. Mitigates accidental writes to live DB. |
| T-06-03 | Denial of Service | Migration 0004 `downgrade()` | mitigate | Reverse-order drops (`project_repos` before `teams.github_team_slug`) avoid FK constraint failure that would jam the migration history at 0004 with no rollback path. Task 2 acceptance criterion asserts symmetric downgrade/upgrade round-trip succeeds. |
| T-06-04 | Information Disclosure | `github_team_slug` column on `teams` | accept | Slug is a public GitHub team identifier (visible on github.com/orgs/{org}/teams). Storing it does not introduce confidentiality risk. ASVS L1 V6 (Cryptography) does not apply. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 6 installs zero new packages (per RESEARCH.md "No New Packages Required" and "Package Legitimacy Audit"). The four packages used (alembic, sqlalchemy, aiosqlite, pytest) are all pre-installed and previously verified by the existing 165-test suite. No `[ASSUMED]` or `[SUS]` packages introduced this phase. |

**Phase 6 ASVS L1 applicability:** V1 (Architecture / SQL injection) is the only relevant category — mitigated by exclusive use of Alembic parameterized DDL operations (T-06-01). V2/V3/V4/V5/V6 do not apply (no auth, sessions, routes, user input, or secrets in this phase).
</threat_model>

<verification>
## Phase-Level Verification

After all three tasks are complete, the executor MUST run:

1. **Migration round-trip (proves DATA-01, DATA-02, DATA-03 together):**
   ```bash
   python -m alembic downgrade -1 && python -m alembic upgrade head && python -m alembic current
   ```
   Output must end at revision `0004 (head)`.

2. **Migration smoke test (proves D-05 + isolates the assertion from live DB):**
   ```bash
   python -m pytest tests/test_migration.py -x -q
   ```
   Must exit 0 with 1 test collected and 1 test passed.

3. **Regression sweep (proves "all existing tests pass without modification" success criterion):**
   ```bash
   python -m pytest tests/ -x -q
   ```
   Must exit 0 with collected count >= 166 (165 prior + 1 new) and zero failures.

4. **Schema introspection sanity check (manual one-liner, optional but cheap):**
   ```bash
   python -c "from sqlalchemy import create_engine, inspect; e = create_engine('sqlite:///./pecp.db'); insp = inspect(e); assert 'github_team_slug' in [c['name'] for c in insp.get_columns('teams')]; assert 'project_repos' in insp.get_table_names(); print('schema OK')"
   ```
   Must print `schema OK`.

5. **Security gate (T-06-01 negative grep on the migration):**
   ```bash
   grep -E 'op\.execute\(' alembic/versions/0004_add_github_fields.py | grep -v '^#' | wc -l
   ```
   Must return `0`.
</verification>

<success_criteria>
Phase 6 is complete when ALL of the following are TRUE:

- [ ] `src/pecp/persistence/models.py` contains `TeamRecord.github_team_slug: Mapped[str | None]` (Text, nullable, no default) and a new `ProjectRepoRecord` class with `id`/`project_id`/`repo_name`/`repo_url`/`created_at` and the `uq_project_repos_project_name` unique constraint
- [ ] `alembic/versions/0004_add_github_fields.py` exists with `revision = "0004"` and `down_revision = "0003"`, adds the column via batch mode in `upgrade()`, creates `project_repos` with FK + unique constraint in `upgrade()`, and reverses both correctly in `downgrade()`
- [ ] `tests/test_migration.py` exists with one passing test that runs upgrade → schema inspect → downgrade → schema inspect, all against a `tmp_path` SQLite file (not `:memory:`, not the live DB)
- [ ] `python -m alembic current` reports `0004 (head)` on the live `pecp.db`
- [ ] `python -m pytest tests/ -x -q` exits 0 with >= 166 tests collected and zero failures
- [ ] No raw SQL (`op.execute`) in the migration file (T-06-01 mitigated)
- [ ] The live `pecp.db` is at revision 0004 and was never written-to by the test (T-06-02 mitigated)
- [ ] DATA-01, DATA-02, and DATA-03 are all backed by the same migration applying cleanly and rolling back cleanly (success criterion #3 from ROADMAP Phase 6)
- [ ] Existing 165 tests pass without modification (success criterion #4 from ROADMAP Phase 6)
</success_criteria>

<output>
Create `.planning/phases/06-data-model-migration/06-01-SUMMARY.md` when done, capturing:
- The exact final content shape of `TeamRecord` field addition and `ProjectRepoRecord` class
- Migration revision chain confirmation (`alembic current` output)
- Test count delta (165 → 166)
- Any deviations from the locked decisions D-01..D-06 (expected: none)
- Confirmation that no new packages were installed
</output>
