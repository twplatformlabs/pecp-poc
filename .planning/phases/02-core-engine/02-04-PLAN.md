---
phase: 02-core-engine
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/pecp/adapters/mock/kubernetes.py
  - src/pecp/adapters/mock/salesforce.py
  - src/pecp/adapters/mock/aem.py
  - src/pecp/adapters/mock/datadog.py
  - src/pecp/adapters/mock/servicenow.py
  - src/pecp/adapters/mock/jfrog.py
  - tests/test_adapters/mock/test_kubernetes.py
  - tests/test_adapters/mock/test_salesforce.py
  - tests/test_adapters/mock/test_aem.py
  - tests/test_adapters/mock/test_datadog.py
  - tests/test_adapters/mock/test_servicenow.py
  - tests/test_adapters/mock/test_jfrog.py
  - tests/test_dispatcher/test_dispatch_extended_kinds.py
autonomous: true
requirements:
  - ADPT-02
  - ADPT-03
  - KINDS-05
  - KINDS-06

must_haves:
  truths:
    - "KubernetesMockAdapter.provision returns ProvisionResult(status=ready) with provider_metadata containing namespace + manifest_path + team and a 'Would call: kubectl ...' activity_log of length >= 2"
    - "SalesforceMockAdapter.provision returns activity_log[0] == f'Would provision Salesforce resource for team {team}' (D-01 exact string)"
    - "AemMockAdapter.provision returns activity_log[0] == f'Would provision AEM resource for team {team}' (D-01 pattern)"
    - "DatadogMockAdapter, ServiceNowMockAdapter, JFrogMockAdapter each return ProvisionResult(status=ready) with system-specific 'Would provision <system> resource for team <team>' activity_log entries"
    - "All 6 adapters subclass AdapterBase DIRECTLY — NO shared GenericMockAdapter base class (D-02)"
    - "All 6 placeholder markers '(placeholder — implemented in plan' have been removed from the 6 adapter files"
    - "A new Dispatcher integration test file drives at least 3 representative non-AWS kinds (PECPSalesforce, PECPKubernetes, PECPDatadog) from pending → ready through dispatch() and asserts persisted activity_log content"
    - "python -m pytest tests/ -q exits 0 with the full Phase 2 suite green"
  artifacts:
    - path: "src/pecp/adapters/mock/kubernetes.py"
      provides: "Real KubernetesMockAdapter"
      contains: "class KubernetesMockAdapter(AdapterBase)"
    - path: "src/pecp/adapters/mock/salesforce.py"
      provides: "Real SalesforceMockAdapter per KINDS-05 (generic team-scoped per D-01)"
      contains: "Would provision Salesforce resource for team"
    - path: "src/pecp/adapters/mock/aem.py"
      provides: "Real AemMockAdapter per KINDS-06"
      contains: "Would provision AEM resource for team"
    - path: "src/pecp/adapters/mock/datadog.py"
      provides: "Real DatadogMockAdapter (ADPT-02)"
    - path: "src/pecp/adapters/mock/servicenow.py"
      provides: "Real ServiceNowMockAdapter (ADPT-02)"
    - path: "src/pecp/adapters/mock/jfrog.py"
      provides: "Real JFrogMockAdapter (ADPT-02)"
    - path: "tests/test_dispatcher/test_dispatch_extended_kinds.py"
      provides: "Dispatcher integration tests for 3 representative non-AWS kinds"
  key_links:
    - from: "src/pecp/adapters/mock/salesforce.py"
      to: "src/pecp/models/resource_spec.py"
      via: "imports SalesforceSpec and reads resource.metadata.team"
      pattern: "SalesforceSpec"
    - from: "src/pecp/adapters/mock/aem.py"
      to: "src/pecp/models/resource_spec.py"
      via: "imports AemSpec and reads resource.metadata.team"
      pattern: "AemSpec"
    - from: "src/pecp/adapters/mock/kubernetes.py"
      to: "src/pecp/models/resource_spec.py"
      via: "imports KubernetesSpec (added by Plan 01) and reads resource.metadata.team"
      pattern: "KubernetesSpec"
    - from: "tests/test_dispatcher/test_dispatch_extended_kinds.py"
      to: "src/pecp/dispatcher.py"
      via: "calls dispatch() with PECPSalesforce, PECPKubernetes, PECPDatadog resources"
      pattern: "from pecp.dispatcher import dispatch"
---

<objective>
Replace the six remaining placeholder mock adapters from Plan 02 with their real (lightweight) implementations: `KubernetesMockAdapter`, `SalesforceMockAdapter` (KINDS-05), `AemMockAdapter` (KINDS-06), `DatadogMockAdapter`, `ServiceNowMockAdapter`, and `JFrogMockAdapter`. Promote each Wave 0 scaffold test to a real test, and add Dispatcher integration coverage proving the Dispatcher routes at least 3 representative non-AWS kinds end-to-end.

Purpose: Complete the 10-adapter set required by ADPT-02. Per CONTEXT.md D-01, Salesforce and AEM use generic team-scoped placeholder log messages — no domain research; the goal is proving adapter wiring. Per D-02, each adapter is its own class with no shared base beyond AdapterBase (when real Salesforce specs arrive, they diverge cleanly without refactoring). KubernetesMockAdapter has slightly richer metadata (namespace, manifest path) since the Kubernetes story is more concrete than Salesforce/AEM today, but it still follows the same simple structure.

This plan runs in Wave 3 in parallel with Plan 03. Plans 03 and 04 modify disjoint adapter files. The only contention point is `tests/test_dispatcher/test_dispatch.py` — Plan 03 appends one test there; this plan creates a SEPARATE file `tests/test_dispatcher/test_dispatch_extended_kinds.py` for its integration coverage, eliminating the shared-file conflict entirely.

Output:
- 6 real adapter files replacing Plan 02 placeholders
- 6 real test files promoted from Wave 0 scaffolds
- `tests/test_dispatcher/test_dispatch_extended_kinds.py` — new integration tests for 3 representative non-AWS kinds (Salesforce, Kubernetes, Datadog)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-engine/02-CONTEXT.md
@.planning/phases/02-core-engine/02-RESEARCH.md
@.planning/phases/02-core-engine/02-PATTERNS.md
@.planning/phases/02-core-engine/02-VALIDATION.md
@.planning/phases/02-core-engine/02-01-PLAN.md
@.planning/phases/02-core-engine/02-02-PLAN.md
@.planning/phases/01-foundation-contracts/01-CONTEXT.md

@src/pecp/adapters/base.py
@src/pecp/adapters/mock/aws_lambda.py
@src/pecp/dispatcher.py
@src/pecp/models/provision_result.py
@src/pecp/models/resource_spec.py
@src/pecp/models/enums.py
@src/pecp/persistence/models.py
@tests/conftest.py
@tests/test_dispatcher/test_dispatch.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace placeholder kubernetes.py, salesforce.py, aem.py with real adapters and promote their tests</name>
  <files>src/pecp/adapters/mock/kubernetes.py, src/pecp/adapters/mock/salesforce.py, src/pecp/adapters/mock/aem.py, tests/test_adapters/mock/test_kubernetes.py, tests/test_adapters/mock/test_salesforce.py, tests/test_adapters/mock/test_aem.py</files>
  <behavior>
    - KubernetesMockAdapter.provision returns ProvisionResult(status=ready) with provider_metadata containing keys namespace (f"pecp-{team}"), manifest_path, team, system; activity_log of length >= 2 containing "Would call: kubectl create namespace" and "Would call: kubectl apply -f"
    - SalesforceMockAdapter.provision returns ProvisionResult(status=ready) with provider_metadata {"team": team, "system": "salesforce"} and activity_log=[f"Would provision Salesforce resource for team {team}"] (exact string per D-01)
    - AemMockAdapter.provision returns ProvisionResult(status=ready) with provider_metadata {"team": team, "system": "aem"} and activity_log=[f"Would provision AEM resource for team {team}"]
    - All three adapters use `resource.metadata.team or "unknown"` for the team value
    - All three adapters' deprovision returns ProvisionResult(status=ready) with a single delete/teardown activity_log entry
    - All three adapters' get_status returns ProvisionResult(status=ResourceStatus.ready) with empty metadata and log (D-03)
    - With asyncio.sleep patched, provision completes synchronously and mock_sleep.call_count >= 1
    - Non-matching spec type returns ProvisionResult(status=failed, error="Unexpected spec type: ...") for all three
  </behavior>
  <read_first>
    - src/pecp/adapters/mock/aws_lambda.py (Plan 02 Task 1) — canonical adapter template (do NOT inherit from anything except AdapterBase per D-02)
    - src/pecp/models/resource_spec.py (post-Plan 01) — KubernetesSpec, SalesforceSpec, AemSpec all present in the AnySpec union; this plan does NOT modify resource_spec.py
    - .planning/phases/02-core-engine/02-PATTERNS.md — "src/pecp/adapters/mock/salesforce.py and aem.py (adapter, request-response — generic team-scoped variant)" section showing the generic team-scoped pattern; KubernetesMockAdapter follows the same shape with richer metadata
    - .planning/phases/02-core-engine/02-CONTEXT.md — D-01 (Salesforce/AEM generic team-scoped messages; exact example `"Would provision Salesforce resource for team toxins-research"`), D-02 (separate classes — NO shared base class)
    - tests/test_adapters/mock/test_aws_container.py (Plan 03 Task 1; if Plan 03 has completed, this is the latest real adapter test reference) OR tests/test_adapters/mock/test_aws_lambda.py (Plan 02 Task 1; always available as the canonical template)
    - tests/test_adapters/mock/test_kubernetes.py, test_salesforce.py, test_aem.py — Wave 0 scaffolds from Plan 01 that currently contain importorskip + skip; replace them entirely
  </read_first>
  <action>
    Replace `src/pecp/adapters/mock/kubernetes.py` placeholder body with a real implementation:
    - Module docstring: `"""Mock adapter for PECPKubernetes resources (ADPT-02, ADPT-03)."""`
    - Imports: `asyncio`; `from pecp.adapters.base import AdapterBase`; `from pecp.models.enums import ResourceStatus`; `from pecp.models.provision_result import ProvisionResult`; `from pecp.models.resource_spec import KubernetesSpec, ResourceSpec`
    - `class KubernetesMockAdapter(AdapterBase):` with:
      - `async def provision(self, resource: ResourceSpec) -> ProvisionResult`: `await asyncio.sleep(2)`; defensive isinstance check on spec (if not KubernetesSpec, return ProvisionResult(status=failed, error="Unexpected spec type: ...")); `team = resource.metadata.team or "unknown"`; return ProvisionResult(status=ready, provider_metadata={"namespace": f"pecp-{team}", "manifest_path": f"/var/manifests/{team}/manifest.yaml", "team": team, "system": "kubernetes"}, activity_log=[f"Would call: kubectl create namespace pecp-{team}", f"Would call: kubectl apply -f /var/manifests/{team}/manifest.yaml --namespace pecp-{team}", f"Would call: kubectl get pods -n pecp-{team}"])
      - `async def deprovision(self, resource: ResourceSpec) -> ProvisionResult`: `await asyncio.sleep(1)`; isinstance check; return ProvisionResult(status=ready, activity_log=[f"Would call: kubectl delete namespace pecp-{team}"])
      - `async def get_status(self, resource: ResourceSpec) -> ProvisionResult`: return ProvisionResult(status=ResourceStatus.ready) (D-03)

    Replace `src/pecp/adapters/mock/salesforce.py` placeholder body:
    - Module docstring: `"""Mock adapter for PECPSalesforce resources (ADPT-02, ADPT-03, KINDS-05 — generic team-scoped placeholder per D-01)."""`
    - Imports: `asyncio`; `from pecp.adapters.base import AdapterBase`; `from pecp.models.enums import ResourceStatus`; `from pecp.models.provision_result import ProvisionResult`; `from pecp.models.resource_spec import SalesforceSpec, ResourceSpec`
    - `class SalesforceMockAdapter(AdapterBase):` with:
      - `provision`: `await asyncio.sleep(1)`; isinstance check; `team = resource.metadata.team or "unknown"`; return ProvisionResult(status=ready, provider_metadata={"team": team, "system": "salesforce"}, activity_log=[f"Would provision Salesforce resource for team {team}"]) — exact string match to D-01 example
      - `deprovision`: `await asyncio.sleep(1)`; isinstance check; return ProvisionResult(status=ready, activity_log=[f"Would deprovision Salesforce resource for team {team}"])
      - `get_status`: return ProvisionResult(status=ResourceStatus.ready)

    Replace `src/pecp/adapters/mock/aem.py` placeholder body — same structure as SalesforceMockAdapter, substituting "Salesforce" → "AEM" and `SalesforceSpec` → `AemSpec` everywhere:
    - Module docstring: `"""Mock adapter for PECPAem resources (ADPT-02, ADPT-03, KINDS-06 — generic team-scoped placeholder per D-01)."""`
    - Imports include `from pecp.models.resource_spec import AemSpec, ResourceSpec`
    - `class AemMockAdapter(AdapterBase):` mirroring SalesforceMockAdapter shape; provision activity_log: `[f"Would provision AEM resource for team {team}"]`; deprovision activity_log: `[f"Would deprovision AEM resource for team {team}"]`; provider_metadata: `{"team": team, "system": "aem"}`
    - Per D-02: NO shared base class. Salesforce and AEM are SEPARATE classes that both subclass AdapterBase directly. They look almost identical today; that is intentional. When real specs arrive (deferred to v2), they will diverge cleanly without refactoring.

    None of the three new adapter files contain the substring `(placeholder` (Plan 02 marker is removed).

    Replace `tests/test_adapters/mock/test_kubernetes.py` (mirror structure of `test_aws_container.py` from Plan 03, or `test_aws_lambda.py` from Plan 02 if Plan 03 has not yet completed):
    - Module docstring: `"""Tests for KubernetesMockAdapter (ADPT-02, ADPT-03)."""`
    - Imports: `pytest`, `from unittest.mock import patch`, `yaml`, `from pecp.adapters.mock.kubernetes import KubernetesMockAdapter`, `from pecp.models.enums import ResourceStatus`, `from pecp.models.resource_spec import ResourceSpec`
    - `KUBERNETES_YAML` constant with `apiVersion: pecp/v1`, `kind: PECPKubernetes`, `metadata.name: my-app`, `metadata.team: toxins-research`, `spec.config: {namespace: "custom-ns"}`
    - Tests (`async def`, no `@pytest.mark.asyncio`):
      - `test_kubernetes_provision_returns_ready_with_namespace` — patch sleep, call provision, assert status=ready, len(activity_log) >= 2, `"kubectl create namespace pecp-toxins-research"` in activity_log[0], provider_metadata["namespace"] == "pecp-toxins-research"
      - `test_kubernetes_provision_patches_sleep` — patch sleep, assert mock_sleep.call_count >= 1
      - `test_kubernetes_deprovision_returns_delete_namespace_log` — patch sleep, assert activity_log[0] starts with "Would call: kubectl delete namespace"
      - `test_kubernetes_get_status_returns_ready_no_metadata` — assert status=ready, provider_metadata == {}
      - `test_kubernetes_rejects_non_k8s_spec_returns_failed` — pass a ResourceSpec with kind=PECPLambda + LambdaSpec; assert status=failed and "Unexpected spec type" in error

    Replace `tests/test_adapters/mock/test_salesforce.py`:
    - Module docstring: `"""Tests for SalesforceMockAdapter (ADPT-02, ADPT-03, KINDS-05)."""`
    - Imports same as above but importing SalesforceMockAdapter
    - `SALESFORCE_YAML` constant with `kind: PECPSalesforce`, `metadata.team: toxins-research`, `spec.config: {connected_app: "demo"}`
    - Tests:
      - `test_salesforce_provision_returns_team_scoped_log` — patch sleep, assert activity_log[0] == "Would provision Salesforce resource for team toxins-research" (EXACT string match to D-01 example)
      - `test_salesforce_provision_team_unknown_when_metadata_team_none` — build a spec with metadata.team=None; assert activity_log[0] == "Would provision Salesforce resource for team unknown"
      - `test_salesforce_provision_patches_sleep` — patch sleep, assert mock_sleep.call_count >= 1
      - `test_salesforce_deprovision_returns_team_scoped_log` — assert activity_log[0] starts with "Would deprovision Salesforce resource for team"
      - `test_salesforce_get_status_returns_ready_no_metadata`
      - `test_salesforce_rejects_non_salesforce_spec_returns_failed`

    Replace `tests/test_adapters/mock/test_aem.py` — same shape as test_salesforce.py but using `AEM_YAML`, AemMockAdapter, and the AEM activity_log strings:
    - Key test: `test_aem_provision_returns_team_scoped_log` asserts activity_log[0] == "Would provision AEM resource for team toxins-research" (EXACT)
    - Other tests mirror Salesforce tests with naming substitutions

    All test files use `yaml.safe_load` only; NO `@pytest.mark.asyncio` decorators.
  </action>
  <verify>
    <automated>cd /Users/punitlad/projects/pecp-poc &amp;&amp; python -m pytest tests/test_adapters/mock/test_kubernetes.py tests/test_adapters/mock/test_salesforce.py tests/test_adapters/mock/test_aem.py -x -q &amp;&amp; ! grep "(placeholder" src/pecp/adapters/mock/kubernetes.py src/pecp/adapters/mock/salesforce.py src/pecp/adapters/mock/aem.py &amp;&amp; python -m mypy src/pecp/adapters/mock/kubernetes.py src/pecp/adapters/mock/salesforce.py src/pecp/adapters/mock/aem.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/pecp/adapters/mock/kubernetes.py` contains `class KubernetesMockAdapter(AdapterBase):` and the string `"Would call: kubectl create namespace"`
    - `src/pecp/adapters/mock/salesforce.py` contains `class SalesforceMockAdapter(AdapterBase):` and the EXACT string `"Would provision Salesforce resource for team "` (with the trailing space before the f-string interpolation site)
    - `src/pecp/adapters/mock/aem.py` contains `class AemMockAdapter(AdapterBase):` and the EXACT string `"Would provision AEM resource for team "`
    - None of these 3 adapter files contains the substring `(placeholder`
    - `grep -L "class GenericMockAdapter\|from.*GenericMockAdapter" src/pecp/adapters/mock/kubernetes.py src/pecp/adapters/mock/salesforce.py src/pecp/adapters/mock/aem.py` returns 3 file paths (proves D-02: no shared base class anywhere)
    - `tests/test_adapters/mock/test_kubernetes.py` contains 5 `async def test_kubernetes_*` functions
    - `tests/test_adapters/mock/test_salesforce.py` contains 6 `async def test_salesforce_*` functions and the exact assertion `"Would provision Salesforce resource for team toxins-research"`
    - `tests/test_adapters/mock/test_aem.py` contains 6 `async def test_aem_*` functions and the exact assertion `"Would provision AEM resource for team toxins-research"`
    - `python -m pytest tests/test_adapters/mock/test_kubernetes.py tests/test_adapters/mock/test_salesforce.py tests/test_adapters/mock/test_aem.py -x -q` exits 0 with at least 17 tests passing (5 + 6 + 6)
    - `python -m mypy src/pecp/adapters/mock/kubernetes.py src/pecp/adapters/mock/salesforce.py src/pecp/adapters/mock/aem.py` exits 0
    - `python -m ruff check src/pecp/adapters/mock/kubernetes.py src/pecp/adapters/mock/salesforce.py src/pecp/adapters/mock/aem.py` exits 0
  </acceptance_criteria>
  <done>KubernetesMockAdapter, SalesforceMockAdapter, AemMockAdapter are fully implemented per their D-01/D-02 constraints and ADPT-02/03 requirements; their test files cover happy path, sleep patching, deprovision, get_status, and the defensive spec-type fallback; the D-01 exact-string activity_log invariant for Salesforce and AEM is locked in.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace placeholder datadog.py, servicenow.py, jfrog.py with real adapters and promote their tests</name>
  <files>src/pecp/adapters/mock/datadog.py, src/pecp/adapters/mock/servicenow.py, src/pecp/adapters/mock/jfrog.py, tests/test_adapters/mock/test_datadog.py, tests/test_adapters/mock/test_servicenow.py, tests/test_adapters/mock/test_jfrog.py</files>
  <behavior>
    - DatadogMockAdapter.provision returns ProvisionResult(status=ready) with provider_metadata {"team": team, "system": "datadog"} and activity_log=[f"Would provision Datadog resource for team {team}"]
    - ServiceNowMockAdapter.provision returns ProvisionResult(status=ready) with provider_metadata {"team": team, "system": "servicenow"} and activity_log=[f"Would provision ServiceNow resource for team {team}"]
    - JFrogMockAdapter.provision returns ProvisionResult(status=ready) with provider_metadata {"team": team, "system": "jfrog"} and activity_log=[f"Would provision JFrog resource for team {team}"]
    - All three adapters subclass AdapterBase DIRECTLY (D-02) — no shared GenericMockAdapter base
    - All three adapters use `resource.metadata.team or "unknown"` and patch-friendly `await asyncio.sleep(1)` for provision/deprovision
    - get_status returns ProvisionResult(status=ResourceStatus.ready) with empty metadata and log
    - Non-matching spec returns ProvisionResult(status=failed, error="Unexpected spec type: ...")
  </behavior>
  <read_first>
    - src/pecp/adapters/mock/salesforce.py (Task 1 above) — newest reference implementation following the generic team-scoped pattern; Datadog/ServiceNow/JFrog adapters are structurally identical
    - src/pecp/models/resource_spec.py (post-Plan 01) — DatadogSpec, ServiceNowSpec, JFrogSpec are all in the AnySpec union with `config: dict[str, Any]`
    - .planning/phases/02-core-engine/02-PATTERNS.md — "src/pecp/adapters/mock/salesforce.py and aem.py (adapter, request-response — generic team-scoped variant)" section: AemMockAdapter, DatadogMockAdapter, ServiceNowMockAdapter, JFrogMockAdapter follow identical structure with substituted system name and log message
    - .planning/phases/02-core-engine/02-CONTEXT.md — D-02 (no shared base; each is a separate class)
    - tests/test_adapters/mock/test_salesforce.py (Task 1 above) — most recent real adapter test; copy structure and substitute system name
    - tests/test_adapters/mock/test_datadog.py, test_servicenow.py, test_jfrog.py — Wave 0 scaffolds from Plan 01; replace each entirely
  </read_first>
  <action>
    Replace `src/pecp/adapters/mock/datadog.py` placeholder body following the Salesforce template from Task 1:
    - Module docstring: `"""Mock adapter for PECPDatadog resources (ADPT-02, ADPT-03 — generic team-scoped placeholder per D-01 pattern)."""`
    - Imports: `asyncio`; AdapterBase; ResourceStatus; ProvisionResult; `from pecp.models.resource_spec import DatadogSpec, ResourceSpec`
    - `class DatadogMockAdapter(AdapterBase):` with:
      - `provision`: `await asyncio.sleep(1)`; isinstance check on DatadogSpec; `team = resource.metadata.team or "unknown"`; return ProvisionResult(status=ready, provider_metadata={"team": team, "system": "datadog"}, activity_log=[f"Would provision Datadog resource for team {team}"])
      - `deprovision`: `await asyncio.sleep(1)`; isinstance check; return ProvisionResult(status=ready, activity_log=[f"Would deprovision Datadog resource for team {team}"])
      - `get_status`: return ProvisionResult(status=ResourceStatus.ready)

    Replace `src/pecp/adapters/mock/servicenow.py` placeholder body — identical structure substituting ServiceNow and `ServiceNowSpec`:
    - Module docstring: `"""Mock adapter for PECPServiceNow resources (ADPT-02, ADPT-03 — generic team-scoped placeholder per D-01 pattern)."""`
    - provider_metadata: `{"team": team, "system": "servicenow"}`
    - provision activity_log: `[f"Would provision ServiceNow resource for team {team}"]`
    - deprovision activity_log: `[f"Would deprovision ServiceNow resource for team {team}"]`

    Replace `src/pecp/adapters/mock/jfrog.py` placeholder body — identical structure substituting JFrog and `JFrogSpec`:
    - Module docstring: `"""Mock adapter for PECPJFrog resources (ADPT-02, ADPT-03 — generic team-scoped placeholder per D-01 pattern)."""`
    - provider_metadata: `{"team": team, "system": "jfrog"}`
    - provision activity_log: `[f"Would provision JFrog resource for team {team}"]`
    - deprovision activity_log: `[f"Would deprovision JFrog resource for team {team}"]`

    Per D-02: NO shared base class — three separate classes that all subclass AdapterBase directly. The code is intentionally near-duplicate; this is by design (Salesforce/AEM also chose this path in Phase 1 D-11).

    Replace `tests/test_adapters/mock/test_datadog.py` mirroring `test_salesforce.py` from Task 1:
    - `DATADOG_YAML` constant with `kind: PECPDatadog`, `metadata.team: toxins-research`, `spec.config: {monitor_name: "fn-errors"}`
    - Tests (`async def`):
      - `test_datadog_provision_returns_team_scoped_log` — patch sleep, assert activity_log[0] == "Would provision Datadog resource for team toxins-research"
      - `test_datadog_provision_team_unknown_when_metadata_team_none`
      - `test_datadog_provision_patches_sleep`
      - `test_datadog_deprovision_returns_team_scoped_log`
      - `test_datadog_get_status_returns_ready_no_metadata`
      - `test_datadog_rejects_non_datadog_spec_returns_failed`

    Replace `tests/test_adapters/mock/test_servicenow.py` — mirror test_datadog.py:
    - `SERVICENOW_YAML` with `kind: PECPServiceNow`, `spec.config: {ticket_type: "change"}`
    - Tests mirror Datadog naming with substitution; key assertion: `activity_log[0] == "Would provision ServiceNow resource for team toxins-research"`

    Replace `tests/test_adapters/mock/test_jfrog.py` — mirror test_datadog.py:
    - `JFROG_YAML` with `kind: PECPJFrog`, `spec.config: {repo_name: "team-artifacts"}`
    - Key assertion: `activity_log[0] == "Would provision JFrog resource for team toxins-research"`

    All test files use `yaml.safe_load` only; no `@pytest.mark.asyncio` decorators.
  </action>
  <verify>
    <automated>cd /Users/punitlad/projects/pecp-poc &amp;&amp; python -m pytest tests/test_adapters/mock/test_datadog.py tests/test_adapters/mock/test_servicenow.py tests/test_adapters/mock/test_jfrog.py -x -q &amp;&amp; ! grep "(placeholder" src/pecp/adapters/mock/datadog.py src/pecp/adapters/mock/servicenow.py src/pecp/adapters/mock/jfrog.py &amp;&amp; python -m mypy src/pecp/adapters/mock/datadog.py src/pecp/adapters/mock/servicenow.py src/pecp/adapters/mock/jfrog.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/pecp/adapters/mock/datadog.py` contains `class DatadogMockAdapter(AdapterBase):` and `"Would provision Datadog resource for team "`
    - `src/pecp/adapters/mock/servicenow.py` contains `class ServiceNowMockAdapter(AdapterBase):` and `"Would provision ServiceNow resource for team "`
    - `src/pecp/adapters/mock/jfrog.py` contains `class JFrogMockAdapter(AdapterBase):` and `"Would provision JFrog resource for team "`
    - None of these 3 files contains `(placeholder`
    - `grep -L "GenericMockAdapter" src/pecp/adapters/mock/datadog.py src/pecp/adapters/mock/servicenow.py src/pecp/adapters/mock/jfrog.py` returns 3 file paths (D-02 compliance)
    - Each of the 3 test files contains 6 `async def test_*` functions
    - Each of the 3 test files contains the EXACT-string assertion for `"Would provision <System> resource for team toxins-research"` matching its system
    - `python -m pytest tests/test_adapters/mock/test_datadog.py tests/test_adapters/mock/test_servicenow.py tests/test_adapters/mock/test_jfrog.py -x -q` exits 0 with at least 18 tests passing (6 + 6 + 6)
    - `python -m mypy src/pecp/adapters/mock/datadog.py src/pecp/adapters/mock/servicenow.py src/pecp/adapters/mock/jfrog.py` exits 0
    - `python -m ruff check src/pecp/adapters/mock/datadog.py src/pecp/adapters/mock/servicenow.py src/pecp/adapters/mock/jfrog.py` exits 0
    - `grep -c "(placeholder" src/pecp/adapters/mock/*.py | grep -v ":0$" | grep -v "__init__"` produces no output (no remaining placeholders anywhere in the mock adapter package — combined with Plan 03, all 9 of Plan 02's placeholders are now real)
  </acceptance_criteria>
  <done>The remaining three generic team-scoped adapters (Datadog, ServiceNow, JFrog) are implemented per ADPT-02 with the team-scoped activity_log pattern and the D-02 invariant (no shared base). Combined with Plan 03's three AWS adapters and Plan 02's Lambda adapter + 9 placeholders, all 10 ADAPTER_REGISTRY entries are now real implementations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Dispatcher integration tests for 3 representative non-AWS kinds in a new test file</name>
  <files>tests/test_dispatcher/test_dispatch_extended_kinds.py</files>
  <behavior>
    - test_dispatch_drives_salesforce_pending_to_ready: insert PECPSalesforce ResourceRecord with status=pending, call dispatch, assert final status=ready and activity_log contains "Would provision Salesforce resource for team"
    - test_dispatch_drives_kubernetes_pending_to_ready: insert PECPKubernetes ResourceRecord, call dispatch, assert status=ready and activity_log contains "kubectl create namespace"
    - test_dispatch_drives_datadog_pending_to_ready: insert PECPDatadog ResourceRecord, call dispatch, assert status=ready and activity_log contains "Would provision Datadog resource for team"
    - All three tests use the conftest db_session fixture and patch asyncio.sleep; total wall time < 2 seconds
    - All three tests construct spec_json via ResourceSpec.model_validate(yaml.safe_load(YAML)).model_dump_json() — not hand-rolled JSON
  </behavior>
  <read_first>
    - tests/test_dispatcher/test_dispatch.py (Plan 02 Task 3 + Plan 03 Task 3 if Plan 03 has completed) — existing Dispatcher integration tests; this task creates a NEW file that follows the same pattern but in a separate file to avoid the shared-file conflict with Plan 03
    - src/pecp/dispatcher.py (Plan 02 Task 2) — for dispatch signature and ADAPTER_REGISTRY structure
    - src/pecp/persistence/models.py (post-Plan 01) — ResourceRecord with provider_metadata and activity_log columns
    - src/pecp/models/resource_spec.py (post-Plan 01) — KubernetesSpec, SalesforceSpec, DatadogSpec all in the union
    - tests/conftest.py (post-Plan 01) — db_session fixture
    - .planning/phases/02-core-engine/02-PATTERNS.md — "tests/test_dispatcher/test_dispatch.py" section for the canonical Dispatcher test structure
    - .planning/phases/02-core-engine/02-RESEARCH.md — Pattern 8 (end-to-end Dispatcher test) and Pitfalls 4 (expire_on_commit), 5 (patch target), 7 (model_dump_json round-trip)
  </read_first>
  <action>
    Create a NEW test file `tests/test_dispatcher/test_dispatch_extended_kinds.py` (this file does NOT exist; Plan 03 owns `test_dispatch.py` and this plan must NOT modify it):

    Module docstring: `"""Dispatcher integration tests for non-AWS kinds (ADPT-02, KINDS-05, KINDS-06)."""`

    Imports:
    - stdlib: `import json`
    - third-party: `import pytest`, `from unittest.mock import patch`, `import yaml`, `from sqlalchemy import select`, `from sqlalchemy.ext.asyncio import AsyncSession`
    - first-party: `from pecp.dispatcher import dispatch`, `from pecp.models.enums import ResourceStatus`, `from pecp.models.resource_spec import ResourceSpec`, `from pecp.persistence.models import ResourceRecord`

    Module-level YAML fixtures:
    - `SALESFORCE_YAML` — apiVersion: pecp/v1, kind: PECPSalesforce, metadata.name=demo-sf, metadata.team=toxins-research, spec.config={connected_app: "demo"}
    - `KUBERNETES_YAML` — kind: PECPKubernetes, metadata.team=toxins-research, spec.config={namespace: "custom-ns"}
    - `DATADOG_YAML` — kind: PECPDatadog, metadata.team=toxins-research, spec.config={monitor_name: "fn-errors"}

    Helper: `def _spec_json(yaml_text: str) -> str:` returns `ResourceSpec.model_validate(yaml.safe_load(yaml_text)).model_dump_json()` — uses the canonical serializer per Pitfall 7 (do NOT hand-roll the JSON string).

    Tests:
    - `async def test_dispatch_drives_salesforce_pending_to_ready(db_session: AsyncSession) -> None:`
      - Insert ResourceRecord with id="sf-id-001", kind="PECPSalesforce", name="demo-sf", team="toxins-research", status="pending", spec_json=_spec_json(SALESFORCE_YAML)
      - `with patch("asyncio.sleep", return_value=None): await dispatch("sf-id-001", db_session)`
      - Re-query: assert updated.status == ResourceStatus.ready.value
      - Parse log: `log = json.loads(updated.activity_log or "[]")`; assert any("Would provision Salesforce resource for team" in entry for entry in log)
      - Parse metadata: `metadata = json.loads(updated.provider_metadata or "{}")`; assert metadata.get("system") == "salesforce"
    - `async def test_dispatch_drives_kubernetes_pending_to_ready(db_session: AsyncSession) -> None:`
      - Same structure with id="k8s-id-001", kind="PECPKubernetes", spec_json=_spec_json(KUBERNETES_YAML)
      - Assert log contains "kubectl create namespace pecp-toxins-research"
      - Assert metadata.get("namespace") == "pecp-toxins-research"
    - `async def test_dispatch_drives_datadog_pending_to_ready(db_session: AsyncSession) -> None:`
      - Same structure with id="dd-id-001", kind="PECPDatadog", spec_json=_spec_json(DATADOG_YAML)
      - Assert log contains "Would provision Datadog resource for team toxins-research"
      - Assert metadata.get("system") == "datadog"

    Notes:
    - All tests are `async def` with NO `@pytest.mark.asyncio` decorator (auto mode)
    - Use `yaml.safe_load` only
    - Do NOT modify `tests/test_dispatcher/test_dispatch.py` — this file belongs to Plan 03 and Plan 02 conjointly. Plan 04's new tests live in a separate file to keep wave-3 parallelism intact.
  </action>
  <verify>
    <automated>cd /Users/punitlad/projects/pecp-poc &amp;&amp; python -m pytest tests/test_dispatcher/test_dispatch_extended_kinds.py -x -q &amp;&amp; python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_dispatcher/test_dispatch_extended_kinds.py` exists (newly created — not present before this plan)
    - File contains exactly 3 test functions: `test_dispatch_drives_salesforce_pending_to_ready`, `test_dispatch_drives_kubernetes_pending_to_ready`, `test_dispatch_drives_datadog_pending_to_ready`
    - File contains `from pecp.dispatcher import dispatch` (no `importorskip` — real import)
    - File contains `_spec_json` helper that calls `ResourceSpec.model_validate(yaml.safe_load(` (Pitfall 7 round-trip pattern)
    - File contains `json.loads(updated.activity_log` for each test (the Dispatcher persists activity_log as JSON-encoded Text per D-04)
    - `python -m pytest tests/test_dispatcher/test_dispatch_extended_kinds.py -x -q` exits 0 with 3 tests passing
    - `python -m pytest tests/test_dispatcher/test_dispatch_extended_kinds.py -q` reports wall time < 2 seconds (asyncio.sleep patched)
    - `python -m pytest tests/ -q` exits 0 across the full repo with no failures
    - `grep "@pytest.mark.asyncio" tests/test_dispatcher/test_dispatch_extended_kinds.py` produces no output
    - `grep "yaml.load(" tests/test_dispatcher/test_dispatch_extended_kinds.py | grep -v safe_load` produces no output
    - `diff <(git show HEAD:tests/test_dispatcher/test_dispatch.py) tests/test_dispatcher/test_dispatch.py | wc -l` is small (< 5) — proves Plan 04 did NOT modify Plan 03's file (this gate may not apply on the first commit; the requirement is that this plan SHALL NOT edit `test_dispatch.py`)
  </acceptance_criteria>
  <done>Three representative non-AWS kinds (Salesforce, Kubernetes, Datadog) are now under Dispatcher integration test coverage in a dedicated file that does NOT collide with Plan 03's edits to test_dispatch.py. Combined with Plan 02's Lambda integration test and Plan 03's Account integration test, the Dispatcher's routing is exercised end-to-end for at least 5 of the 10 kinds — enough representative coverage to trust the registry pattern for the remaining 5 (whose unit tests already exhaustively cover their behavior).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mock adapter `resource.metadata.team` interpolation into activity_log strings | The team value is a `str | None` field from Pydantic; an f-string interpolating it into activity_log entries does NOT shell-escape, but the strings are persisted to a Text column and never executed as shell commands |
| Dispatcher routing → 6 new adapters | The Dispatcher's `spec.kind not in ADAPTER_REGISTRY` branch already handles unknown kinds; the 6 new adapters add only new known-kind branches and inherit the security posture of Plan 02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-T01 | Tampering | Hostile `metadata.team` value (e.g., `"foo\n; rm -rf /"`) flowing into f-string in activity_log | accept | Activity log strings are persisted to the Text column via `json.dumps` (in Plan 02 Task 2's dispatcher); `json.dumps` correctly escapes any control characters or quotes. No shell execution occurs anywhere — the "Would call:" prefix is documentary, not interpreted. PoC scope; v2 may want to constrain `team` with a regex validator |
| T-02-04-T02 | Tampering | Salesforce/AEM exact-string activity_log assertions | mitigate | Tests in Task 1 assert exact equality against the D-01 example string `"Would provision Salesforce resource for team toxins-research"`. If a contributor changes the format, the test breaks immediately — protecting the contract Phase 5's demo script depends on (PROJECT.md "Core demo scenario") |
| T-02-04-T03 | Tampering | Contributor adds a `GenericMockAdapter` base class to share code between the 6 generic adapters | mitigate | Acceptance criteria `grep -L "GenericMockAdapter" ...` enforces D-02; any future PR introducing the shared base fails the gate. D-02 explicitly forbade this for the reason that future real specs (Salesforce Connected App fields, AEM site fields) will diverge cleanly |
| T-02-04-V01 | Input Validation (ASVS V5) | All 6 adapters receive a ResourceSpec of the wrong subtype | mitigate | Each adapter performs `isinstance` narrowing and returns `ProvisionResult(status=failed, error="Unexpected spec type: ...")` on mismatch; covered by `test_*_rejects_non_*_spec_returns_failed` for every adapter. Defense-in-depth: the Dispatcher's ADAPTER_REGISTRY lookup by `spec.kind` should already prevent cross-routing |
| T-02-04-D01 | Denial of Service | Adapter `asyncio.sleep(1)` blocks the dispatch coroutine | accept | All sleeps are bounded ≤ 2s and patched to zero in tests. v2 real adapters will need configurable timeouts (INTG-V2-*) |
| T-02-04-I01 | Information Disclosure | Synthetic team-scoped metadata leaks team membership | accept | The team value is a public identifier intended for use in resource labels (PROJECT.md: teams are the central organizing unit); no PII is involved |

ASVS Level 1 mapping:
- V5 Input Validation: covered by T-02-04-V01
- V2/V3/V4/V6: not applicable
</threat_model>

<verification>
- All three task `<automated>` blocks pass
- `python -m pytest tests/ -q` exits 0 across the entire repo (all of Plan 01+02+03+04 plus the existing Phase 1 tests stay green)
- `python -m mypy src/` exits 0
- `python -m ruff check src/ tests/` exits 0
- `grep -r "(placeholder" src/pecp/adapters/mock/` produces no output (combined with Plan 03's three AWS adapters, all 9 of Plan 02's placeholders are now real implementations — only `aws_lambda.py` was real from Plan 02; the other 9 transitioned through Plan 03 + Plan 04)
- `grep -r "class GenericMockAdapter\|class.*MockAdapter.*Generic" src/pecp/adapters/mock/` produces no output (D-02 invariant enforced)
- Full Phase 2 suite wall time < 5 seconds (all `asyncio.sleep` calls patched in tests; KINDS-04 ≥3s manual check excluded from CI)
</verification>

<success_criteria>
- KINDS-05: SalesforceMockAdapter implemented with the EXACT D-01 placeholder activity_log message
- KINDS-06: AemMockAdapter implemented with the analogous AEM placeholder activity_log message
- ADPT-02: All 10 ADAPTER_REGISTRY entries are now real implementations (Plan 02 Lambda + Plan 03 Container/Data/Account + Plan 04 Kubernetes/Salesforce/AEM/Datadog/ServiceNow/JFrog)
- ADPT-03: All 10 adapters simulate latency (`asyncio.sleep`), produce structured `"Would call:"` or `"Would provision <system>"` activity_log entries, and return synthetic provider_metadata
- D-02: All 10 adapters subclass AdapterBase DIRECTLY — no GenericMockAdapter base appears anywhere
- The Dispatcher integration test file `test_dispatch_extended_kinds.py` proves 3 non-AWS kinds route correctly through dispatch()
- The MVP vertical slice is complete: every kind from PECPLambda through PECPJFrog can be dispatched end-to-end
- Test suite remains green; mypy strict and ruff pass
</success_criteria>

<output>
Create `.planning/phases/02-core-engine/02-04-SUMMARY.md` when done. Capture: which adapters are now real (with their key activity_log signatures, especially the EXACT D-01 Salesforce string and AEM analog), how many tests were added per adapter, the new dispatcher integration test names, and a final tally proving all 10 ADAPTER_REGISTRY entries are real implementations (combined with Plans 02 and 03).
</output>
