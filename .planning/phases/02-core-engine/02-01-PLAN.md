---
phase: 02-core-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - alembic.ini
  - alembic/env.py
  - alembic/script.py.mako
  - alembic/versions/0001_add_provider_cols.py
  - src/pecp/persistence/models.py
  - src/pecp/models/resource_spec.py
  - tests/conftest.py
  - tests/test_dispatcher/__init__.py
  - tests/test_dispatcher/test_dispatch.py
  - tests/test_adapters/mock/__init__.py
  - tests/test_adapters/mock/test_aws_lambda.py
  - tests/test_adapters/mock/test_aws_container.py
  - tests/test_adapters/mock/test_aws_data.py
  - tests/test_adapters/mock/test_aws_account.py
  - tests/test_adapters/mock/test_kubernetes.py
  - tests/test_adapters/mock/test_salesforce.py
  - tests/test_adapters/mock/test_aem.py
  - tests/test_adapters/mock/test_datadog.py
  - tests/test_adapters/mock/test_servicenow.py
  - tests/test_adapters/mock/test_jfrog.py
  - tests/test_models/test_resource_spec.py
autonomous: true
requirements:
  - ADPT-02
  - KINDS-01

must_haves:
  truths:
    - "Running `alembic current` returns revision 0001 (or later) — Alembic is initialized and applied to the dev SQLite database"
    - "`ResourceRecord.provider_metadata` and `ResourceRecord.activity_log` columns exist in the ORM model"
    - "`ResourceSpec.model_validate` accepts a YAML spec with `kind: PECPKubernetes`, `kind: PECPDatadog`, `kind: PECPServiceNow`, or `kind: PECPJFrog` and returns a discriminated union with the matching spec subclass"
    - "`tests/conftest.py` exposes a `db_session` fixture yielding an `AsyncSession` bound to in-memory SQLite"
    - "Wave 0 test scaffolds exist with skip-marked or xfail-marked placeholder tests so the test suite still runs green"
  artifacts:
    - path: "alembic.ini"
      provides: "Alembic configuration"
      contains: "script_location"
    - path: "alembic/env.py"
      provides: "Async-compatible Alembic environment"
      contains: "create_async_engine"
    - path: "alembic/versions/0001_add_provider_cols.py"
      provides: "First Alembic migration adding provider_metadata and activity_log columns"
      contains: "add_column"
    - path: "src/pecp/persistence/models.py"
      provides: "ResourceRecord with provider_metadata and activity_log columns"
      contains: "provider_metadata"
    - path: "src/pecp/models/resource_spec.py"
      provides: "Four new spec classes (KubernetesSpec, DatadogSpec, ServiceNowSpec, JFrogSpec) added to AnySpec union — bringing total to 10 kinds"
      contains: "KubernetesSpec"
    - path: "tests/conftest.py"
      provides: "Shared db_session fixture for Dispatcher and adapter tests"
      contains: "db_session"
    - path: "tests/test_dispatcher/test_dispatch.py"
      provides: "Wave 0 placeholder for Dispatcher tests"
    - path: "tests/test_adapters/mock/__init__.py"
      provides: "Test package marker so pytest collects adapter mock tests"
  key_links:
    - from: "alembic/env.py"
      to: "src/pecp/persistence/database.py"
      via: "import DATABASE_URL"
      pattern: "from pecp.persistence.database import DATABASE_URL"
    - from: "alembic/env.py"
      to: "src/pecp/persistence/models.py"
      via: "import Base for target_metadata"
      pattern: "from pecp.persistence.models import Base"
    - from: "tests/conftest.py"
      to: "src/pecp/persistence/models.py"
      via: "Base.metadata.create_all for in-memory schema"
      pattern: "Base.metadata.create_all"
---

<objective>
Establish the schema and test scaffolding foundation Phase 2's adapter and Dispatcher slices depend on, without yet introducing any adapter or Dispatcher behavior.

Purpose: Phase 2 cannot ship an end-to-end Dispatcher slice (Plan 02) until (a) `ResourceRecord` has `provider_metadata` and `activity_log` columns, (b) Alembic owns schema evolution from here forward, (c) `ResourceSpec` recognizes all 10 kinds the registry will route (`PECPKubernetes`, `PECPDatadog`, `PECPServiceNow`, `PECPJFrog` added on top of the 6 Phase 1 kinds) so adapters can validate any spec the registry forwards, and (d) Wave 0 test scaffolds exist so subsequent plans inherit a green test suite. This plan delivers exactly those preconditions and nothing else.

Output:
- `alembic.ini` + `alembic/` directory with async-compatible `env.py` and `0001_add_provider_cols.py` migration
- `ResourceRecord` ORM model extended with two `Mapped[str | None]` columns (`provider_metadata`, `activity_log`)
- `ResourceSpec` discriminated union extended with FOUR new placeholder spec classes (`KubernetesSpec`, `DatadogSpec`, `ServiceNowSpec`, `JFrogSpec`) — total kinds now equal 10
- `pyproject.toml` adds `alembic` to the project dependencies
- `tests/conftest.py` exposes a shared `db_session` fixture
- Wave 0 test scaffolds: `tests/test_dispatcher/{__init__,test_dispatch}.py` and `tests/test_adapters/mock/{__init__,test_*}.py` for all 10 adapters
- Extended `tests/test_models/test_resource_spec.py` proving the 4 new spec kinds parse from YAML
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-engine/02-CONTEXT.md
@.planning/phases/02-core-engine/02-RESEARCH.md
@.planning/phases/02-core-engine/02-PATTERNS.md
@.planning/phases/02-core-engine/02-VALIDATION.md
@.planning/phases/01-foundation-contracts/01-CONTEXT.md

@src/pecp/persistence/models.py
@src/pecp/persistence/database.py
@src/pecp/models/resource_spec.py
@src/pecp/models/enums.py
@src/pecp/models/provision_result.py
@src/pecp/adapters/base.py
@tests/conftest.py
@tests/test_persistence/test_database.py
@tests/test_models/test_resource_spec.py
@pyproject.toml
@example.yaml
</context>

## Phase Goal

**As a** PECP backend developer or contributor,
**I want to** instantiate any of the 10 mock adapters and dispatch a resource through the full `PENDING → PROVISIONING → READY|FAILED` lifecycle (with synthetic provider metadata and structured `"Would call: ..."` activity logs),
**so that** Phase 3 can wire FastAPI `BackgroundTasks` to the engine without further design work and stakeholders can trust the engine is testable in complete isolation from the HTTP layer.

*(Note: the ROADMAP Phase 2 `**Goal:**` line is goal-shaped — actor: "developer", capability: "instantiate any of the 7 mock adapters, call `provision()`, wait for simulated latency, and inspect a structured activity log and synthetic provider metadata", outcome: "all without a running HTTP server". The user story above formalizes that goal into the canonical `As a / I want to / so that` shape. Adapter count is 10 per CONTEXT.md D-06, not 7 — the ROADMAP count predates the ADPT-02 expansion.)*

<tasks>

<task type="auto">
  <name>Task 1: Initialize Alembic and write the async env.py + 0001 migration adding provider_metadata and activity_log columns</name>
  <files>pyproject.toml, alembic.ini, alembic/env.py, alembic/script.py.mako, alembic/versions/0001_add_provider_cols.py, src/pecp/persistence/models.py</files>
  <read_first>
    - pyproject.toml — current dependency list and tool config; alembic must be appended to `dependencies` (not `dev`) since the migration is part of the production stack
    - src/pecp/persistence/database.py — exposes `DATABASE_URL` (env var driven) and `engine`; the Alembic env must reuse `DATABASE_URL` so test, dev, and prod all migrate the same way
    - src/pecp/persistence/models.py — current `ResourceRecord` definition; the two new columns must use the same `mapped_column(Text, nullable=True, default="<json-literal>")` pattern as `spec_json`
    - .planning/phases/02-core-engine/02-RESEARCH.md — Patterns 4 and 5 (async env.py, migration ops, ORM column addition); Pitfalls 1, 2, 3 (Alembic init gap, sync engine default, ORM-vs-migration drift)
    - .planning/phases/02-core-engine/02-PATTERNS.md — "alembic/ initialization + env.py + migration" section (setup sequence) and "src/pecp/persistence/models.py — add two columns" section
  </read_first>
  <action>
    Add `alembic>=1.13` to `pyproject.toml` `[project] dependencies` (production dep, not dev — migrations ship with the runtime).

    Run `alembic init alembic` from the project root (per Pitfall 1, this is mandatory before any `alembic revision` succeeds). Replace the generated `alembic/env.py` entirely with the async-compatible version per RESEARCH.md Pattern 4:
    - Imports: `asyncio`, `fileConfig` from `logging.config`, `context` from `alembic`, `create_async_engine` from `sqlalchemy.ext.asyncio`, plus `DATABASE_URL` from `pecp.persistence.database` and `Base` from `pecp.persistence.models`
    - `target_metadata = Base.metadata`
    - `do_run_migrations(connection)` calls `context.configure(connection=connection, target_metadata=target_metadata)` inside a `context.begin_transaction()` block
    - `run_async_migrations()` creates `create_async_engine(DATABASE_URL)`, uses `async with connectable.connect() as connection:` then `await connection.run_sync(do_run_migrations)`, then `await connectable.dispose()`
    - `run_migrations_online()` calls `asyncio.run(run_async_migrations())`
    - At module bottom, `if context.is_offline_mode(): raise RuntimeError("Offline migration mode not supported for async engine") else: run_migrations_online()`

    Edit the generated `alembic.ini` so `sqlalchemy.url` is empty (or set to `sqlite+aiosqlite:///./pecp.db` as a fallback) — the `env.py` overrides via `DATABASE_URL` env var. Leave `script_location = alembic`.

    Add the two new columns to `ResourceRecord` in `src/pecp/persistence/models.py` (after `created_at`):
    - `provider_metadata: Mapped[str | None] = mapped_column(Text, nullable=True, default="{}")`
    - `activity_log: Mapped[str | None] = mapped_column(Text, nullable=True, default="[]")`
    (Use `str | None`, not `Optional[str]` — matches the project's Python 3.12 union syntax per RESEARCH.md "State of the Art".)

    Create the migration file `alembic/versions/0001_add_provider_cols.py` by hand (do NOT use `--autogenerate` — the in-place edit is simpler and matches RESEARCH.md Pattern 4 exactly):
    - `revision = "0001"`, `down_revision = None`, `branch_labels = None`, `depends_on = None`
    - `def upgrade()` calls `op.add_column("resource_records", sa.Column("provider_metadata", sa.Text(), nullable=True, server_default="{}"))` and the analogous call for `activity_log` with `server_default="[]"`
    - `def downgrade()` drops the two columns in reverse order

    Apply the migration to the local dev database: run `alembic upgrade head` (the existing `pecp.db` already has the resource_records table from Phase 1, so this only ADDs the two new columns — verify with `sqlite3 pecp.db ".schema resource_records"` after).

    Per Pitfall 3: the ORM model AND the Alembic migration must both list the columns — tests that call `Base.metadata.create_all` (the conftest in-memory pattern) rely on the ORM model, not the migration, so they stay in sync.
  </action>
  <verify>
    <automated>cd /Users/punitlad/projects/pecp-poc &amp;&amp; alembic current 2>&amp;1 | grep -E "0001|head" &amp;&amp; sqlite3 pecp.db ".schema resource_records" | grep -E "provider_metadata|activity_log" &amp;&amp; python -m pytest tests/test_persistence/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` lists `alembic>=1.13` (or compatible) in `[project] dependencies`
    - `alembic.ini` exists at project root with `script_location = alembic`
    - `alembic/env.py` contains the string `create_async_engine` and the string `from pecp.persistence.database import DATABASE_URL`
    - `alembic/env.py` does NOT contain `engine_from_config` (the sync default that the generated template uses)
    - `alembic/versions/0001_add_provider_cols.py` contains the string `op.add_column("resource_records"` twice (once per column)
    - `alembic/versions/0001_add_provider_cols.py` contains `revision = "0001"` and `down_revision = None`
    - `src/pecp/persistence/models.py` contains `provider_metadata: Mapped[str | None] = mapped_column(Text, nullable=True, default="{}")`
    - `src/pecp/persistence/models.py` contains `activity_log: Mapped[str | None] = mapped_column(Text, nullable=True, default="[]")`
    - Running `alembic current` from the project root exits 0 and prints a revision matching `0001` (or `0001 (head)`)
    - Running `sqlite3 pecp.db ".schema resource_records"` lists both `provider_metadata` and `activity_log` as columns
    - Running `python -m pytest tests/test_persistence/ -x -q` exits 0 (the existing persistence tests must remain green — `Base.metadata.create_all` picks up the new columns automatically)
    - Running `python -m mypy src/pecp/persistence/models.py` exits 0
  </acceptance_criteria>
  <done>Alembic owns schema evolution from here forward, the migration is applied to dev SQLite, the ORM model agrees with the migration, and the existing persistence test suite stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Extend ResourceSpec discriminated union with KubernetesSpec, DatadogSpec, ServiceNowSpec, JFrogSpec and lock the union via tests</name>
  <files>src/pecp/models/resource_spec.py, tests/test_models/test_resource_spec.py</files>
  <read_first>
    - src/pecp/models/resource_spec.py — current discriminated union (`AnySpec`) with 6 kinds; the 4 new specs follow the exact `SalesforceSpec` / `AemSpec` template (`config: dict[str, Any] = Field(default_factory=dict)`)
    - tests/test_models/test_resource_spec.py — existing `test_all_six_kinds_constructable` and YAML round-trip tests; the new tests extend the same fixture style with `yaml.safe_load`
    - .planning/phases/02-core-engine/02-RESEARCH.md — "Open Questions" #1 (the recommendation to add minimal placeholder spec classes with `config: dict[str, Any]` matching SalesforceSpec/AemSpec — exactly what D-11 from Phase 1 established for placeholder specs). The recommendation in RESEARCH.md is 3 new specs (Datadog/ServiceNow/JFrog) but the registry CONTEXT.md D-06 also includes PECPKubernetes which is missing from the Phase 1 union — this plan therefore adds 4 specs, not 3
    - .planning/phases/02-core-engine/02-PATTERNS.md — "src/pecp/models/resource_spec.py — extend with 3 new spec kinds" section (the section name says 3 but the pattern applies identically to 4 — same template applied 4 times)
    - .planning/phases/02-core-engine/02-CONTEXT.md — D-06 (ADAPTER_REGISTRY lists 10 kinds including PECPKubernetes); the Dispatcher in Plan 02 cannot route PECPKubernetes resources unless this spec exists
    - .planning/phases/01-foundation-contracts/01-CONTEXT.md — D-11 (placeholder spec pattern for `SalesforceSpec` and `AemSpec`)
  </read_first>
  <action>
    In `src/pecp/models/resource_spec.py`, immediately after `AemSpec` (around current line 52), add FOUR new Pydantic v2 spec classes following the exact pattern of `SalesforceSpec`/`AemSpec`:
    - `class KubernetesSpec(BaseModel):` with `kind: Literal["PECPKubernetes"]` and `config: dict[str, Any] = Field(default_factory=dict)`
    - `class DatadogSpec(BaseModel):` with `kind: Literal["PECPDatadog"]` and `config: dict[str, Any] = Field(default_factory=dict)`
    - `class ServiceNowSpec(BaseModel):` with `kind: Literal["PECPServiceNow"]` and `config: dict[str, Any] = Field(default_factory=dict)`
    - `class JFrogSpec(BaseModel):` with `kind: Literal["PECPJFrog"]` and `config: dict[str, Any] = Field(default_factory=dict)`

    Extend `AnySpec` to include the four new specs in the `Union[...]` (preserve discriminator):
    `AnySpec = Annotated[Union[LambdaSpec, ContainerSpec, DataServiceSpec, AccountSpec, SalesforceSpec, AemSpec, KubernetesSpec, DatadogSpec, ServiceNowSpec, JFrogSpec], Field(discriminator="kind")]`

    Do NOT alter `LambdaSpec`, `ContainerSpec`, `DataServiceSpec`, `AccountSpec`, `SalesforceSpec`, `AemSpec`, `ResourceMetadata`, `ResourceSpec`, or the `inject_kind_into_spec` model validator — leave the working Phase 1 contract untouched.

    In `tests/test_models/test_resource_spec.py`, add FOUR new tests that mirror the existing `test_lambda_spec_parses_from_example_yaml` style — each parses a small inline YAML via `yaml.safe_load` (NEVER `yaml.load`, per CLAUDE.md) and asserts the resulting `resource.spec` is the expected class:
    - `test_kubernetes_spec_parses_with_team_scope` — YAML with `kind: PECPKubernetes`, `metadata.name`, `metadata.team`, `spec.config: {namespace: "team-ns"}`; assert `isinstance(resource.spec, KubernetesSpec)` and `resource.spec.config == {"namespace": "team-ns"}`
    - `test_datadog_spec_parses_with_team_scope` — YAML with `kind: PECPDatadog`, `spec.config: {monitor_name: "fn-errors"}`; assert `isinstance(resource.spec, DatadogSpec)`
    - `test_servicenow_spec_parses_with_team_scope` — `kind: PECPServiceNow`, `spec.config: {ticket_type: "change"}`
    - `test_jfrog_spec_parses_with_team_scope` — `kind: PECPJFrog`, `spec.config: {repo_name: "team-artifacts"}`

    Also extend `test_all_six_kinds_constructable` (keep the function name to avoid renames in CI history) to also construct `KubernetesSpec(kind="PECPKubernetes")`, `DatadogSpec(kind="PECPDatadog")`, `ServiceNowSpec(kind="PECPServiceNow")`, `JFrogSpec(kind="PECPJFrog")` — bringing the constructable kinds total to 10.

    Update the imports at the top of `tests/test_models/test_resource_spec.py` to include `KubernetesSpec`, `DatadogSpec`, `ServiceNowSpec`, `JFrogSpec` alongside the existing imports.

    Per Phase 1 D-12, do NOT add new entries to `ResourceStatus` — the four states remain canonical. Per CONTEXT.md D-02, do NOT introduce a shared base class for these specs — four separate classes, exactly like `SalesforceSpec`/`AemSpec` (which are themselves separate per Phase 1 D-11 enforcement).
  </action>
  <verify>
    <automated>cd /Users/punitlad/projects/pecp-poc &amp;&amp; python -m pytest tests/test_models/test_resource_spec.py -x -q &amp;&amp; python -m mypy src/pecp/models/resource_spec.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/pecp/models/resource_spec.py` contains class definitions for `KubernetesSpec`, `DatadogSpec`, `ServiceNowSpec`, and `JFrogSpec`, each with `kind: Literal[...]` and `config: dict[str, Any] = Field(default_factory=dict)`
    - The `AnySpec` annotation lists all 10 spec classes inside `Union[...]` with `Field(discriminator="kind")`
    - `tests/test_models/test_resource_spec.py` contains tests named `test_kubernetes_spec_parses_with_team_scope`, `test_datadog_spec_parses_with_team_scope`, `test_servicenow_spec_parses_with_team_scope`, `test_jfrog_spec_parses_with_team_scope`
    - `python -m pytest tests/test_models/test_resource_spec.py -x -q` exits 0 with all tests passing (existing 5 + new 4 = 9 minimum)
    - `python -c "from pecp.models.resource_spec import KubernetesSpec, DatadogSpec, ServiceNowSpec, JFrogSpec; print('ok')"` prints `ok` and exits 0
    - `python -m mypy src/pecp/models/resource_spec.py` exits 0 — no `Argument "kind"` errors or discriminator complaints
    - `grep -n "yaml.load(" tests/test_models/test_resource_spec.py | grep -v "yaml.safe_load" | grep -v "^#"` produces no output (unsafe yaml.load remains forbidden)
  </acceptance_criteria>
  <done>The `ResourceSpec` discriminated union recognizes all 10 kinds the Dispatcher will route, the new specs round-trip through YAML, and the union is type-checked.</done>
</task>

<task type="auto">
  <name>Task 3: Add db_session conftest fixture and write Wave 0 test scaffolds for all 10 adapter mocks plus dispatcher</name>
  <files>tests/conftest.py, tests/test_dispatcher/__init__.py, tests/test_dispatcher/test_dispatch.py, tests/test_adapters/mock/__init__.py, tests/test_adapters/mock/test_aws_lambda.py, tests/test_adapters/mock/test_aws_container.py, tests/test_adapters/mock/test_aws_data.py, tests/test_adapters/mock/test_aws_account.py, tests/test_adapters/mock/test_kubernetes.py, tests/test_adapters/mock/test_salesforce.py, tests/test_adapters/mock/test_aem.py, tests/test_adapters/mock/test_datadog.py, tests/test_adapters/mock/test_servicenow.py, tests/test_adapters/mock/test_jfrog.py</files>
  <read_first>
    - tests/conftest.py — current `client` fixture and `os.environ.setdefault("PECP_DATABASE_URL", "sqlite+aiosqlite:///:memory:")` pattern; the new `db_session` fixture is added alongside, not in place of, `client`
    - tests/test_persistence/test_database.py — definitive `db_session` fixture pattern (lines 16-31): `create_async_engine` + `Base.metadata.create_all` + `async_sessionmaker(..., expire_on_commit=False)` + cleanup with `drop_all` and `engine.dispose()`. The conftest fixture must match this exactly so behavior is identical
    - .planning/phases/02-core-engine/02-PATTERNS.md — "tests/conftest.py — add `db_session` fixture" section
    - .planning/phases/02-core-engine/02-RESEARCH.md — Pattern 6 (in-memory session fixture) and Pitfall 4 (`expire_on_commit=False` is mandatory in async context)
    - tests/test_adapters/test_adapter_base.py — existing ABC test patterns; the new adapter mock tests will follow this style (no `@pytest.mark.asyncio` — `asyncio_mode = "auto"` is configured in pyproject.toml line 63)
    - .planning/phases/02-core-engine/02-VALIDATION.md — Wave 0 Requirements checklist (the file list this task must satisfy)
  </read_first>
  <action>
    Add a `db_session` fixture to `tests/conftest.py` placed AFTER the existing `client` fixture. The fixture is async, yields `AsyncSession`, and follows the `tests/test_persistence/test_database.py` lines 16-31 pattern exactly:
    - Import additions at top of file: `from sqlalchemy.ext.asyncio import AsyncSession, async_sessionmaker, create_async_engine` and `from pecp.persistence.models import Base`
    - Decorator: `@pytest.fixture`
    - Signature: `async def db_session() -> AsyncGenerator[AsyncSession, None]:`
    - Body: create `engine = create_async_engine("sqlite+aiosqlite:///:memory:", echo=False, future=True)`, run `Base.metadata.create_all` inside `async with engine.begin() as conn:`, build factory with `async_sessionmaker(engine, expire_on_commit=False, class_=AsyncSession)`, `yield` from `async with factory() as session:`, then drop_all and dispose. Hardcode the in-memory URL — do NOT read `PECP_DATABASE_URL` from env (test fixtures must be deterministic; the env var is only for the `client` fixture path).

    Create `tests/test_dispatcher/__init__.py` as an empty file (pytest needs the package marker per the project's discovery rules).

    Create `tests/test_dispatcher/test_dispatch.py` as a Wave 0 SCAFFOLD ONLY — do NOT import from `pecp.dispatcher` (the module does not exist until Plan 02). The scaffold must contain:
    - A module docstring `"""Tests for Dispatcher state machine (Wave 0 scaffold — populated by Plan 02)."""`
    - One test function `def test_dispatcher_module_not_yet_implemented() -> None:` that uses `pytest.importorskip("pecp.dispatcher", reason="Wave 0 scaffold — Dispatcher implementation arrives in Plan 02")` followed by `pytest.skip("Plan 02 will populate this file")` so the file is collected but no failures emit until Plan 02 lands real tests. This matches the `pytest.importorskip` pattern in `tests/conftest.py` line 18.

    Create `tests/test_adapters/mock/__init__.py` as an empty file.

    Create the 10 adapter mock test scaffold files under `tests/test_adapters/mock/`:
    - `test_aws_lambda.py`, `test_aws_container.py`, `test_aws_data.py`, `test_aws_account.py`, `test_kubernetes.py`, `test_salesforce.py`, `test_aem.py`, `test_datadog.py`, `test_servicenow.py`, `test_jfrog.py`

    Each scaffold file contains:
    - A module docstring naming the requirement IDs it will cover once implemented (e.g., `"""Tests for AwsLambdaMockAdapter (Wave 0 scaffold — populated by Plan 02; covers ADPT-02, ADPT-03, KINDS-01)."""`)
    - One test function `def test_<adapter>_module_not_yet_implemented() -> None:` using `pytest.importorskip("pecp.adapters.mock.<adapter>", reason="Wave 0 scaffold — adapter arrives in Plan 02/03/04")` then `pytest.skip("Implementation plan owns this test")` — substituting the correct module name. NEVER `pytest.fail` — Wave 0 must keep the suite green.

    Per CONTEXT.md "Test strategy": all real adapter tests in Plan 02/03/04 will patch `asyncio.sleep` via `unittest.mock.patch("asyncio.sleep", return_value=None)`. Do NOT add this patch in the Wave 0 scaffolds — they don't exercise sleep yet.

    Do NOT add `@pytest.mark.asyncio` to any of the Wave 0 scaffolds — `pyproject.toml` line 63 sets `asyncio_mode = "auto"` and the scaffolds themselves are sync `def`, not `async def`. The async-ness arrives with the real tests in Plan 02+.
  </action>
  <verify>
    <automated>cd /Users/punitlad/projects/pecp-poc &amp;&amp; python -m pytest tests/ -q &amp;&amp; python -m pytest --collect-only tests/test_dispatcher/ tests/test_adapters/mock/ -q | grep -E "test_(dispatcher|aws_lambda|aws_container|aws_data|aws_account|kubernetes|salesforce|aem|datadog|servicenow|jfrog)_module_not_yet_implemented" | wc -l | grep -E "^[[:space:]]*11[[:space:]]*$"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` contains the substring `async def db_session` and `expire_on_commit=False` and `"sqlite+aiosqlite:///:memory:"`
    - `tests/conftest.py` still contains the existing `client` fixture unchanged (no regression)
    - `tests/test_dispatcher/__init__.py` exists and is empty (0 bytes or whitespace-only)
    - `tests/test_dispatcher/test_dispatch.py` contains `pytest.importorskip("pecp.dispatcher"` and a `pytest.skip(` call
    - `tests/test_adapters/mock/__init__.py` exists
    - All 10 files exist: `tests/test_adapters/mock/test_aws_lambda.py`, `test_aws_container.py`, `test_aws_data.py`, `test_aws_account.py`, `test_kubernetes.py`, `test_salesforce.py`, `test_aem.py`, `test_datadog.py`, `test_servicenow.py`, `test_jfrog.py`
    - Each of the 10 adapter scaffold files contains `pytest.importorskip("pecp.adapters.mock.` and `pytest.skip(`
    - `python -m pytest tests/ -q` exits 0 — no failures and no errors (skipped tests are acceptable)
    - `python -m pytest --collect-only tests/test_dispatcher/ tests/test_adapters/mock/ -q` collects exactly 11 tests (1 dispatcher + 10 adapters), each named `test_*_module_not_yet_implemented`
    - `grep -r "@pytest.mark.asyncio" tests/test_dispatcher/ tests/test_adapters/mock/` produces no output (auto mode means the decorator is forbidden)
    - `grep -r "yaml.load(" tests/test_dispatcher/ tests/test_adapters/mock/ | grep -v safe_load` produces no output
  </acceptance_criteria>
  <done>The shared `db_session` fixture is available to every test in the suite, the test layout for Plan 02/03/04 already exists with skip-marked placeholders, and `python -m pytest tests/ -q` remains green so subsequent plans inherit a working test gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stored spec_json → Dispatcher (Plan 02) | Persisted JSON crosses from the database back into Python via `ResourceSpec.model_validate_json`; Phase 1 D-04 says adapters never raise, but the model layer must reject malformed stored JSON before the adapter sees it |
| Test YAML fixtures → ResourceSpec validator | All YAML test fixtures are developer-authored, but the project rule (CLAUDE.md) is absolute: `yaml.safe_load` everywhere; `yaml.load` is forbidden because it allows arbitrary code execution if a future contributor parses an untrusted spec with the same code path |
| Alembic migration runtime → SQLite file | Alembic's `env.py` runs with full database privileges; an attacker who can write to `alembic/versions/` can drop the resource_records table. PoC-only risk: the directory is in-repo and reviewed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-T01 | Tampering | Test YAML fixtures using `yaml.load` | mitigate | Acceptance criteria gate: `grep "yaml.load(" tests/test_models/test_resource_spec.py | grep -v safe_load` returns empty; CLAUDE.md "What NOT to Use" lists `yaml.load (unsafe)` as forbidden |
| T-02-01-T02 | Tampering | New `KubernetesSpec` / `DatadogSpec` / `ServiceNowSpec` / `JFrogSpec` accepting arbitrary `config: dict[str, Any]` | accept | Phase 1 D-11 explicitly chose `dict[str, Any]` for `SalesforceSpec`/`AemSpec` as a placeholder; the 4 new specs follow the same pattern. Risk: a future caller could pass nested arbitrary structures. Real spec fields are deferred until product specs arrive (CONTEXT.md "Salesforce + AEM Mock Strategy"); the mocks never execute on the config payload, so tampering yields no privilege gain |
| T-02-01-T03 | Tampering | Alembic migration drops columns in `downgrade()` | accept | `downgrade()` requires explicit operator intent (`alembic downgrade -1`); not invoked by the runtime; mitigated by code review at commit time |
| T-02-01-I01 | Information Disclosure | Alembic migration writes `server_default="{}"` and `server_default="[]"` to all existing rows | accept | No sensitive data: defaults are empty JSON; existing rows (test/dev only) gain harmless empty containers |
| T-02-01-D01 | Denial of Service | Migration applied to dev SQLite during execution could fail if `resource_records` table is absent | accept | `Base.metadata.create_all` ran in Phase 1 created the table; the dev DB file `pecp.db` already has it. For a fresh checkout, `init_schema()` runs at FastAPI startup before any migration is needed. Risk window is implausible in PoC |
| T-02-01-SC | Tampering | npm/pip/cargo installs (adding alembic) | mitigate | RESEARCH.md `## Package Legitimacy Audit` marks `alembic 1.18.4` as `[VERIFIED: OK]`. Already in the environment via dev install — no `[ASSUMED]` or `[SUS]` package needs a human checkpoint. New `alembic>=1.13` declaration in `pyproject.toml` just pins what is already verified |
| T-02-01-V01 | Input Validation (ASVS V5) | `ResourceSpec.model_validate` on YAML/JSON input | mitigate | Pydantic v2 discriminated union rejects malformed kind strings with `ValidationError`; Tests `test_invalid_kind_raises_validation_error` and `test_lambda_spec_missing_required_field_raises_validation_error` already encode this and remain in the suite |

ASVS Level 1 mapping:
- V5 Input Validation: covered by Pydantic discriminator enforcement (T-02-01-V01)
- V2/V3/V4/V6: not applicable to this plan (no HTTP layer, no sessions, no access control, no crypto)
</threat_model>

<verification>
- All three task `<automated>` blocks pass
- `python -m pytest tests/ -q` exits 0 across the entire repo
- `python -m mypy src/` exits 0
- `python -m ruff check src/ tests/` exits 0
- `alembic current` from the project root prints a revision id (not "no current revision")
- `sqlite3 pecp.db ".schema resource_records"` lists `provider_metadata` and `activity_log` columns
- No `yaml.load(` (unguarded) calls appear under `tests/` per the security gate
</verification>

<success_criteria>
- Alembic is initialized and the 0001 migration is applied to the dev SQLite database
- `ResourceRecord` ORM model has `provider_metadata` and `activity_log` columns matching the migration
- `ResourceSpec` discriminated union accepts all 10 spec kinds (the 6 Phase 1 kinds plus Kubernetes/Datadog/ServiceNow/JFrog)
- `tests/conftest.py` exposes a working `db_session` fixture
- Wave 0 test scaffolds for the Dispatcher and all 10 mock adapters exist as skip-marked placeholders
- Full test suite remains green (no regressions, no failures, only skipped scaffolds)
- mypy strict mode passes for all modified source files
</success_criteria>

<output>
Create `.planning/phases/02-core-engine/02-01-SUMMARY.md` when done. Capture: files modified, migration revision id, the exact `db_session` fixture signature, the 4 new spec class names, and any deviations from this plan.
</output>
