---
phase: 02-core-engine
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/pecp/adapters/mock/aws_container.py
  - src/pecp/adapters/mock/aws_data.py
  - src/pecp/adapters/mock/aws_account.py
  - tests/test_adapters/mock/test_aws_container.py
  - tests/test_adapters/mock/test_aws_data.py
  - tests/test_adapters/mock/test_aws_account.py
  - tests/test_dispatcher/test_dispatch.py
autonomous: true
requirements:
  - ADPT-02
  - ADPT-03
  - KINDS-02
  - KINDS-03
  - KINDS-04

must_haves:
  truths:
    - "AwsContainerMockAdapter.provision(ContainerSpec) returns ProvisionResult(status=ready) with a 'Would call: aws ecs ...' activity_log entry referencing spec.image and provider_metadata containing a cluster or task identifier"
    - "AwsDataMockAdapter.provision(DataServiceSpec) branches on spec.subtype: s3 → 'aws s3api create-bucket' log + bucket_arn metadata; dynamodb → 'aws dynamodb create-table' log + table_arn metadata; sqs/sns/rds → corresponding service-specific log and metadata"
    - "AwsAccountMockAdapter.provision(AccountSpec) calls await asyncio.sleep(3) once (verified via mock_sleep.call_args == call(3)) and returns ProvisionResult(status=ready) with provider_metadata containing account_id, account_email, account_name"
    - "When AwsAccountMockAdapter.provision runs without the sleep patch, total wall time exceeds 3 seconds (manual-only verification, captured in 02-VALIDATION.md)"
    - "An integration test through dispatch() drives a PECPAccount record from pending → provisioning → ready and the final activity_log contains 'aws organizations create-account'"
    - "All 3 newly-implemented adapters no longer contain the '(placeholder — implemented in plan' marker string"
  artifacts:
    - path: "src/pecp/adapters/mock/aws_container.py"
      provides: "Real AwsContainerMockAdapter per KINDS-02"
      contains: "spec.image"
    - path: "src/pecp/adapters/mock/aws_data.py"
      provides: "Real AwsDataMockAdapter with subtype branching per KINDS-03"
      contains: "DataServiceSubtype.s3"
    - path: "src/pecp/adapters/mock/aws_account.py"
      provides: "Real AwsAccountMockAdapter with 3s dwell per KINDS-04"
      contains: "await asyncio.sleep(3)"
    - path: "tests/test_dispatcher/test_dispatch.py"
      provides: "Adds a KINDS-04 slow-path integration test"
      contains: "test_dispatch_drives_account_pending_to_ready"
  key_links:
    - from: "src/pecp/adapters/mock/aws_data.py"
      to: "src/pecp/models/resource_spec.py"
      via: "import DataServiceSpec, DataServiceSubtype and branch on spec.subtype"
      pattern: "DataServiceSubtype\\."
    - from: "src/pecp/adapters/mock/aws_account.py"
      to: "asyncio standard library"
      via: "await asyncio.sleep(3) inline (Dispatcher does NOT poll)"
      pattern: "asyncio\\.sleep\\(3\\)"
    - from: "tests/test_dispatcher/test_dispatch.py"
      to: "src/pecp/dispatcher.py"
      via: "dispatch() drives PECPAccount end-to-end via the same ADAPTER_REGISTRY"
      pattern: "dispatch\\(.*PECPAccount"
---

<objective>
Replace the three AWS-family placeholder adapters from Plan 02 with their real implementations: `AwsContainerMockAdapter` (KINDS-02), `AwsDataMockAdapter` with subtype branching (KINDS-03), and `AwsAccountMockAdapter` with the inline 3-second slow-path dwell (KINDS-04). Promote their Wave 0 scaffold tests to real tests, and add one new Dispatcher integration test exercising the slow-path through the full state machine.

Purpose: With Plan 02 having proved the engine works end-to-end for Lambda, this plan completes the AWS family. The slow-path is the highest-risk piece — KINDS-04 requires ≥3 seconds dwell in PROVISIONING, and the test must prove the sleep is wired without actually sleeping in CI (patch target verified per Pitfall 5). The new Dispatcher test exercises Account through `dispatch()` end-to-end, proving the registry routing handles the slow-path identically to the fast path.

This plan runs in Wave 3 in parallel with Plan 04 (the 6 generic-team-scoped adapters). The two plans modify disjoint adapter files, so file ownership is clean. The only shared file is `tests/test_dispatcher/test_dispatch.py`, but Plan 03 only APPENDS one new test (no edits to Plan 02's three tests) and Plan 04 does not touch it. (If conflict avoidance is desired, Plan 04 could open a separate `test_dispatch_other_kinds.py` file — see Plan 04 for the resolution.)

Output:
- Real `aws_container.py`, `aws_data.py`, `aws_account.py` replacing the Plan 02 placeholders
- Real test files for each: `test_aws_container.py`, `test_aws_data.py`, `test_aws_account.py` (promoted from Wave 0 scaffolds)
- One new Dispatcher integration test appended to `tests/test_dispatcher/test_dispatch.py`: `test_dispatch_drives_account_pending_to_ready` (proves slow-path through Dispatcher)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-engine/02-CONTEXT.md
@.planning/phases/02-core-engine/02-RESEARCH.md
@.planning/phases/02-core-engine/02-PATTERNS.md
@.planning/phases/02-core-engine/02-VALIDATION.md
@.planning/phases/02-core-engine/02-01-PLAN.md
@.planning/phases/02-core-engine/02-02-PLAN.md
@.planning/phases/01-foundation-contracts/01-CONTEXT.md

@src/pecp/adapters/base.py
@src/pecp/adapters/mock/aws_lambda.py
@src/pecp/dispatcher.py
@src/pecp/models/provision_result.py
@src/pecp/models/resource_spec.py
@src/pecp/models/enums.py
@src/pecp/persistence/models.py
@tests/conftest.py
@tests/test_adapters/mock/test_aws_lambda.py
@tests/test_dispatcher/test_dispatch.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace placeholder aws_container.py with real AwsContainerMockAdapter and promote its test</name>
  <files>src/pecp/adapters/mock/aws_container.py, tests/test_adapters/mock/test_aws_container.py</files>
  <behavior>
    - AwsContainerMockAdapter.provision(ContainerSpec) returns ProvisionResult(status=ready) with provider_metadata containing a synthetic ECS task definition arn and cluster name; activity_log of length >= 1 with the first entry starting "Would call: aws ecs" and referencing spec.image
    - AwsContainerMockAdapter.deprovision returns ProvisionResult(status=ready) with an activity_log entry "Would call: aws ecs delete-service" or "deregister-task-definition" referencing spec.name
    - AwsContainerMockAdapter.get_status returns ProvisionResult(status=ready) with empty activity_log and provider_metadata (Phase 1 D-03)
    - Non-Container spec passed to provision returns ProvisionResult(status=failed, error="Unexpected spec type: ...") — never raises (D-04, Pitfall 6)
    - With asyncio.sleep patched, provision completes synchronously and mock_sleep.call_count >= 1 (ADPT-03 latency simulation wired)
  </behavior>
  <read_first>
    - src/pecp/adapters/mock/aws_lambda.py (Plan 02 Task 1) — the canonical template; AwsContainerMockAdapter follows the exact same structure, only the imports, isinstance target (ContainerSpec), and synthetic AWS resource shapes change
    - src/pecp/adapters/mock/aws_container.py (Plan 02 Task 1 placeholder) — the file already exists with a placeholder body; this task overwrites the body
    - src/pecp/models/resource_spec.py — ContainerSpec fields: `kind`, `name`, `exposure`, `image` (note: no `api_gateway` or `source_code` — different field set from LambdaSpec)
    - .planning/phases/02-core-engine/02-PATTERNS.md — Pattern 1 (Mock Adapter) for the canonical structure, the same template applies
    - .planning/phases/02-core-engine/02-RESEARCH.md — Pattern 1 (mock adapter template), Pitfalls 5 (patch target) and 6 (isinstance narrowing)
    - tests/test_adapters/mock/test_aws_lambda.py (Plan 02 Task 1) — the test pattern this file mirrors; copy structure, change spec fixture to PECPContainer with `image: nginx:latest`
    - tests/test_adapters/mock/test_aws_container.py (Plan 01 Wave 0 scaffold) — the file currently contains importorskip + skip; this task replaces that with real tests
  </read_first>
  <action>
    Open `src/pecp/adapters/mock/aws_container.py`. Remove the placeholder body entirely and replace with a real implementation mirroring AwsLambdaMockAdapter's shape:
    - Module docstring: `"""Mock adapter for PECPContainer resources (ADPT-02, ADPT-03, KINDS-02)."""`
    - Imports: `asyncio`; `from pecp.adapters.base import AdapterBase`; `from pecp.models.enums import ResourceStatus`; `from pecp.models.provision_result import ProvisionResult`; `from pecp.models.resource_spec import ContainerSpec, ResourceSpec`
    - `class AwsContainerMockAdapter(AdapterBase):` with three methods:
      - `async def provision(self, resource: ResourceSpec) -> ProvisionResult`: `await asyncio.sleep(2)`; defensive isinstance check (if not ContainerSpec, return ProvisionResult(status=failed, error="Unexpected spec type: ...")); return ProvisionResult(status=ready, provider_metadata containing keys `task_definition_arn` (synthetic ARN built from spec.name), `cluster` (e.g., `f"pecp-{spec.name}-cluster"`), `image` (echo spec.image), `exposure` (echo spec.exposure)); activity_log with at least two entries:
        - `f"Would call: aws ecs register-task-definition --family pecp-{spec.name} --container-definitions name={spec.name},image={spec.image}"`
        - `f"Would call: aws ecs create-service --cluster pecp-{spec.name}-cluster --service-name {spec.name} --desired-count 1"`
      - `async def deprovision(self, resource: ResourceSpec) -> ProvisionResult`: `await asyncio.sleep(1)`; isinstance check; return ProvisionResult(status=ready, activity_log=[f"Would call: aws ecs delete-service --cluster pecp-{spec.name}-cluster --service {spec.name} --force"])
      - `async def get_status(self, resource: ResourceSpec) -> ProvisionResult`: return `ProvisionResult(status=ResourceStatus.ready)` (no metadata, no log — Phase 1 D-03)
    The file must NOT contain the string `(placeholder` anymore (Plan 02 marker is removed).

    Replace `tests/test_adapters/mock/test_aws_container.py`:
    - Module docstring: `"""Tests for AwsContainerMockAdapter (ADPT-02, ADPT-03, KINDS-02)."""`
    - Imports: `pytest`, `from unittest.mock import patch`, `yaml`, `from pecp.adapters.mock.aws_container import AwsContainerMockAdapter`, `from pecp.models.enums import ResourceStatus`, `from pecp.models.resource_spec import ResourceSpec`
    - `CONTAINER_YAML` constant with `apiVersion: pecp/v1`, `kind: PECPContainer`, `metadata.name: my-svc`, `metadata.team: toxins-research`, `spec.name: my-svc`, `spec.exposure: public`, `spec.image: nginx:1.27`
    - Tests (all `async def`, no `@pytest.mark.asyncio`):
      - `test_aws_container_provision_returns_ready_with_image_in_log` — patch asyncio.sleep, call provision, assert status=ready, len(activity_log) >= 2, every entry starts "Would call:", at least one entry contains the substring `"nginx:1.27"`, provider_metadata["image"] == "nginx:1.27"
      - `test_aws_container_provision_patches_sleep` — patch asyncio.sleep, assert mock_sleep.call_count >= 1 after provision
      - `test_aws_container_deprovision_returns_delete_log` — patch sleep, assert activity_log[0] starts with `"Would call: aws ecs delete-service"`
      - `test_aws_container_get_status_returns_ready_no_metadata` — assert status=ready, provider_metadata == {}
      - `test_aws_container_rejects_non_container_spec_returns_failed` — pass a ResourceSpec with kind=PECPLambda + LambdaSpec; assert result.status == failed and "Unexpected spec type" in result.error
    Use `yaml.safe_load` everywhere. No `@pytest.mark.asyncio` decorator.
  </action>
  <verify>
    <automated>cd /Users/punitlad/projects/pecp-poc &amp;&amp; python -m pytest tests/test_adapters/mock/test_aws_container.py -x -q &amp;&amp; ! grep "(placeholder" src/pecp/adapters/mock/aws_container.py &amp;&amp; python -m mypy src/pecp/adapters/mock/aws_container.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/pecp/adapters/mock/aws_container.py` contains `class AwsContainerMockAdapter(AdapterBase):`
    - `src/pecp/adapters/mock/aws_container.py` contains `await asyncio.sleep(`
    - `src/pecp/adapters/mock/aws_container.py` contains `"Would call: aws ecs register-task-definition"` (or `aws ecs create-service` — at least one is fine, both required for the two activity_log entries)
    - `src/pecp/adapters/mock/aws_container.py` does NOT contain the string `(placeholder`
    - `grep -c "ContainerSpec" src/pecp/adapters/mock/aws_container.py` returns a value >= 1 (isinstance narrowing imports the type)
    - `tests/test_adapters/mock/test_aws_container.py` contains 5 `async def test_aws_container_*` functions matching the names above
    - `python -m pytest tests/test_adapters/mock/test_aws_container.py -x -q` exits 0 with 5 tests passing
    - `python -m pytest tests/test_adapters/mock/test_aws_container.py -q` reports wall time < 2 seconds
    - `python -m mypy src/pecp/adapters/mock/aws_container.py` exits 0 (strict mode)
    - `grep "(placeholder" src/pecp/adapters/mock/aws_container.py` produces no output
  </acceptance_criteria>
  <done>AwsContainerMockAdapter is fully implemented per KINDS-02; its test file covers happy path, sleep patching, deprovision, get_status, and the defensive spec-type fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace placeholder aws_data.py with real AwsDataMockAdapter (subtype branching) and promote its test</name>
  <files>src/pecp/adapters/mock/aws_data.py, tests/test_adapters/mock/test_aws_data.py</files>
  <behavior>
    - AwsDataMockAdapter.provision(DataServiceSpec) branches on spec.subtype:
      - s3 → log "Would call: aws s3api create-bucket --bucket pecp-{name}", metadata {"bucket_arn": "arn:aws:s3:::pecp-{name}"}
      - sqs → log "Would call: aws sqs create-queue --queue-name pecp-{name}", metadata {"queue_url": "https://sqs.us-east-1.amazonaws.com/123456789012/pecp-{name}"}
      - sns → log "Would call: aws sns create-topic --name pecp-{name}", metadata {"topic_arn": "arn:aws:sns:us-east-1:123456789012:pecp-{name}"}
      - rds → log "Would call: aws rds create-db-instance --db-instance-identifier pecp-{name}", metadata {"db_instance_arn": "arn:aws:rds:us-east-1:123456789012:db:pecp-{name}"}
      - dynamodb → log "Would call: aws dynamodb create-table --table-name pecp-{name}", metadata {"table_arn": "arn:aws:dynamodb:us-east-1:123456789012:table/pecp-{name}"}
    - All five subtype branches return status=ready and activity_log with at least one "Would call:" entry
    - Non-DataServiceSpec returns ProvisionResult(status=failed, error="Unexpected spec type: ...")
    - With asyncio.sleep patched, mock_sleep.call_count >= 1
    - Tests parametrize over all 5 subtypes — every subtype is exercised
  </behavior>
  <read_first>
    - src/pecp/adapters/mock/aws_container.py (Task 1 above) — newest reference implementation following the canonical pattern
    - src/pecp/adapters/mock/aws_lambda.py (Plan 02 Task 1) — original template
    - src/pecp/models/resource_spec.py — DataServiceSpec fields (`kind`, `name`, `subtype`) and DataServiceSubtype enum members (`s3`, `sqs`, `sns`, `rds`, `dynamodb`)
    - .planning/phases/02-core-engine/02-PATTERNS.md — "src/pecp/adapters/mock/aws_data.py (adapter, request-response — branching variant)" for the canonical branching pattern
    - .planning/phases/02-core-engine/02-RESEARCH.md — Pattern 1 and Pitfall 6 (isinstance narrowing for the parameter subtype, not the spec subtype)
    - tests/test_adapters/mock/test_aws_data.py (Plan 01 Wave 0 scaffold) — file currently contains importorskip + skip; replace entirely
  </read_first>
  <action>
    Open `src/pecp/adapters/mock/aws_data.py`. Replace the placeholder body with a real implementation:
    - Module docstring: `"""Mock adapter for PECPDataService resources (ADPT-02, ADPT-03, KINDS-03)."""`
    - Imports: `asyncio`; `from pecp.adapters.base import AdapterBase`; `from pecp.models.enums import ResourceStatus`; `from pecp.models.provision_result import ProvisionResult`; `from pecp.models.resource_spec import DataServiceSpec, DataServiceSubtype, ResourceSpec`
    - `class AwsDataMockAdapter(AdapterBase):` with:
      - `async def provision(self, resource: ResourceSpec) -> ProvisionResult`:
        - `await asyncio.sleep(2)`
        - isinstance narrowing: if not DataServiceSpec, return failed
        - Match on `spec.subtype` using `if/elif` (Python `match` statement is acceptable but `if/elif` is simpler given enum members) and assemble `log_line` and `metadata` for each of the 5 subtypes following the behavior table above. Account ID synthetic value is `123456789012` for consistency with aws_lambda.py.
        - Return ProvisionResult(status=ready, provider_metadata={...subtype-specific...}, activity_log=[log_line]). Add a `"name"` and `"subtype"` field to provider_metadata in every branch for downstream inspection.
      - `async def deprovision(self, resource: ResourceSpec) -> ProvisionResult`: `await asyncio.sleep(1)`; isinstance check; for any subtype emit a generic `f"Would call: aws {subtype.value} delete-resource --name pecp-{name}"` log line; return status=ready.
      - `async def get_status(self, resource: ResourceSpec) -> ProvisionResult`: return ready, empty metadata, empty log.
    The file must NOT contain the string `(placeholder`.

    Replace `tests/test_adapters/mock/test_aws_data.py`:
    - Module docstring: `"""Tests for AwsDataMockAdapter (ADPT-02, ADPT-03, KINDS-03)."""`
    - Imports: same as test_aws_container.py, plus `from pecp.models.resource_spec import DataServiceSubtype`
    - Helper: a function `def _build_spec(subtype: str, name: str = "my-data") -> ResourceSpec:` that constructs a valid ResourceSpec dict (apiVersion, kind=PECPDataService, metadata, spec with subtype) via dict-literal + ResourceSpec.model_validate; do not use yaml strings (parametrized tests across 5 subtypes are cleaner with a constructor)
    - Tests (`async def`):
      - `@pytest.mark.parametrize("subtype,expected_log_prefix,expected_metadata_key", [("s3", "Would call: aws s3api create-bucket", "bucket_arn"), ("sqs", "Would call: aws sqs create-queue", "queue_url"), ("sns", "Would call: aws sns create-topic", "topic_arn"), ("rds", "Would call: aws rds create-db-instance", "db_instance_arn"), ("dynamodb", "Would call: aws dynamodb create-table", "table_arn")]) async def test_aws_data_provision_branches_per_subtype(subtype, expected_log_prefix, expected_metadata_key):` — patch asyncio.sleep, build spec for the subtype, call provision, assert status=ready, activity_log[0].startswith(expected_log_prefix), expected_metadata_key in provider_metadata
      - `test_aws_data_provision_patches_sleep` — patch sleep, call provision once (any subtype), assert mock_sleep.call_count >= 1
      - `test_aws_data_deprovision_returns_delete_log` — patch sleep, assert "delete-resource" in activity_log[0]
      - `test_aws_data_get_status_returns_ready_no_metadata` — assert status=ready, provider_metadata == {}
      - `test_aws_data_rejects_non_data_spec_returns_failed` — pass a LambdaSpec; assert status=failed and "Unexpected spec type" in error
    Use `yaml.safe_load` only if a YAML literal is used (the parametrized tests use dict literals).
  </action>
  <verify>
    <automated>cd /Users/punitlad/projects/pecp-poc &amp;&amp; python -m pytest tests/test_adapters/mock/test_aws_data.py -x -q &amp;&amp; ! grep "(placeholder" src/pecp/adapters/mock/aws_data.py &amp;&amp; python -m mypy src/pecp/adapters/mock/aws_data.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/pecp/adapters/mock/aws_data.py` contains `class AwsDataMockAdapter(AdapterBase):`
    - `src/pecp/adapters/mock/aws_data.py` contains all five subtype-specific log substrings: `"aws s3api create-bucket"`, `"aws sqs create-queue"`, `"aws sns create-topic"`, `"aws rds create-db-instance"`, `"aws dynamodb create-table"`
    - `src/pecp/adapters/mock/aws_data.py` contains `DataServiceSubtype` (used in branching logic)
    - `src/pecp/adapters/mock/aws_data.py` does NOT contain the string `(placeholder`
    - `tests/test_adapters/mock/test_aws_data.py` contains `@pytest.mark.parametrize` with all 5 subtypes
    - `python -m pytest tests/test_adapters/mock/test_aws_data.py -x -q` exits 0 with at least 9 tests passing (5 parametrized + 4 standalone)
    - `python -m pytest tests/test_adapters/mock/test_aws_data.py -q` reports wall time < 2 seconds
    - `python -m mypy src/pecp/adapters/mock/aws_data.py` exits 0 (strict mode)
    - `grep "(placeholder" src/pecp/adapters/mock/aws_data.py` produces no output
  </acceptance_criteria>
  <done>AwsDataMockAdapter is fully implemented per KINDS-03 with subtype branching for all five DataServiceSubtype values, each subtype produces a service-specific activity_log entry and metadata key, and parametrized tests exercise every branch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace placeholder aws_account.py with real AwsAccountMockAdapter (3s slow-path), promote its test, and add the slow-path Dispatcher integration test</name>
  <files>src/pecp/adapters/mock/aws_account.py, tests/test_adapters/mock/test_aws_account.py, tests/test_dispatcher/test_dispatch.py</files>
  <behavior>
    - AwsAccountMockAdapter.provision(AccountSpec) calls `await asyncio.sleep(3)` (literal 3, single call) before transitioning to ready (KINDS-04 ≥3s dwell — adapter handles inline; Dispatcher does NOT poll)
    - Provision uses resource.metadata.team (defaults to "unknown" if None) in the synthetic provider_metadata (account_email = f"aws+{team}@example.com", account_name = f"pecp-{team}")
    - provider_metadata contains: account_id ("123456789012" synthetic), account_email, account_name, management_console_url
    - activity_log contains at least three "Would call: aws organizations ..." entries (create-account, describe-create-account-status, sts assume-role per PATTERNS.md)
    - deprovision returns ProvisionResult(status=ready) with a "Would call: aws organizations close-account (manual PE action required)" log entry (per PATTERNS.md)
    - get_status returns ProvisionResult(status=ready) with empty metadata and log (D-03)
    - Test asserts mock_sleep.call_args_list contains a call(3) — proves the literal 3 is the dwell value (Pitfall 5: patch target is "asyncio.sleep")
    - New Dispatcher integration test in test_dispatch.py: a PECPAccount record goes pending → ready through dispatch() with asyncio.sleep patched; activity_log on the persisted ResourceRecord contains "aws organizations create-account"
  </behavior>
  <read_first>
    - src/pecp/adapters/mock/aws_lambda.py (Plan 02 Task 1) — canonical adapter template
    - src/pecp/adapters/mock/aws_container.py (Task 1 above) — most recent in-plan reference
    - src/pecp/models/resource_spec.py — AccountSpec has only `kind: Literal["PECPAccount"]` (no other spec fields per Phase 1 D-09); the team flows through metadata.team
    - .planning/phases/02-core-engine/02-PATTERNS.md — "src/pecp/adapters/mock/aws_account.py (adapter, request-response — slow-path variant)" for the canonical slow-path body including the team-scoped synthetic metadata
    - .planning/phases/02-core-engine/02-RESEARCH.md — Pattern 2 (AwsAccountMockAdapter slow-path), Pitfall 5 (patch target), Open Question 1 resolution (no spec changes needed for AccountSpec — it's already in the union)
    - .planning/phases/02-core-engine/02-CONTEXT.md — "PECPAccount slow-path" (Claude's Discretion): adapter handles dwell inline; Dispatcher does NOT poll get_status; tests mock sleep
    - .planning/phases/02-core-engine/02-VALIDATION.md — Manual-Only Verifications row about the 3-second dwell under real conditions (the manual check this plan documents but does NOT automate)
    - tests/test_dispatcher/test_dispatch.py (Plan 02 Task 3) — current 3 Dispatcher tests; this task APPENDS one more without modifying the existing tests
  </read_first>
  <action>
    Open `src/pecp/adapters/mock/aws_account.py`. Replace the placeholder body:
    - Module docstring: `"""Mock adapter for PECPAccount resources (ADPT-02, ADPT-03, KINDS-04 slow-path)."""`
    - Imports: `asyncio`; `from pecp.adapters.base import AdapterBase`; `from pecp.models.enums import ResourceStatus`; `from pecp.models.provision_result import ProvisionResult`; `from pecp.models.resource_spec import AccountSpec, ResourceSpec`
    - `class AwsAccountMockAdapter(AdapterBase):` with:
      - `async def provision(self, resource: ResourceSpec) -> ProvisionResult`:
        - Defensive isinstance check on spec (return failed if not AccountSpec)
        - `team = resource.metadata.team or "unknown"`
        - `await asyncio.sleep(3)` — LITERAL 3 (KINDS-04). This is the single sleep call; do not split into multiple smaller sleeps.
        - `account_id = "123456789012"` (synthetic)
        - Return ProvisionResult(status=ready, provider_metadata={"account_id": account_id, "account_email": f"aws+{team}@example.com", "account_name": f"pecp-{team}", "management_console_url": f"https://console.aws.amazon.com/switch-role?account={account_id}"}, activity_log=[three entries per PATTERNS.md "Pattern 2 — AwsAccountMockAdapter — Slow-Path Simulation"])
      - `async def deprovision(self, resource: ResourceSpec) -> ProvisionResult`: no sleep (deprovision is fast); return ProvisionResult(status=ready, activity_log=["Would call: aws organizations close-account (manual PE action required)"])
      - `async def get_status(self, resource: ResourceSpec) -> ProvisionResult`: return ProvisionResult(status=ResourceStatus.ready) (D-03)
    The file must NOT contain `(placeholder`.

    Replace `tests/test_adapters/mock/test_aws_account.py`:
    - Module docstring: `"""Tests for AwsAccountMockAdapter (ADPT-02, ADPT-03, KINDS-04 slow-path)."""`
    - Imports: same as test_aws_container.py but importing AwsAccountMockAdapter and including `from unittest.mock import patch, call`
    - `ACCOUNT_YAML` constant with `apiVersion: pecp/v1`, `kind: PECPAccount`, `metadata.name: aws-account-toxins-research`, `metadata.team: toxins-research`, `spec: {}` (AccountSpec has no spec fields beyond `kind`)
    - Tests:
      - `test_aws_account_provision_calls_sleep_3_seconds` — patch asyncio.sleep, call provision, assert mock_sleep.call_args == call(3) (literal verification of KINDS-04 dwell value)
      - `test_aws_account_provision_returns_ready_with_synthetic_metadata` — patch sleep, assert status=ready, provider_metadata["account_id"] == "123456789012", provider_metadata["account_email"] == "aws+toxins-research@example.com", "account_name" and "management_console_url" present
      - `test_aws_account_provision_activity_log_has_organizations_create_account` — patch sleep, assert any(entry.startswith("Would call: aws organizations create-account") for entry in activity_log)
      - `test_aws_account_provision_team_unknown_when_metadata_team_none` — build a spec with metadata.team=None; assert provider_metadata["account_email"] == "aws+unknown@example.com"
      - `test_aws_account_deprovision_returns_close_account_log` — assert activity_log[0] contains "close-account"
      - `test_aws_account_get_status_returns_ready_no_metadata` — assert status=ready, provider_metadata == {}
      - `test_aws_account_rejects_non_account_spec_returns_failed` — pass a LambdaSpec; assert status=failed and "Unexpected spec type" in error

    Append to `tests/test_dispatcher/test_dispatch.py` (do NOT delete or modify the 3 existing tests from Plan 02):
    - Add a new module-level YAML constant `ACCOUNT_YAML` matching the shape from `test_aws_account.py`
    - Add helper `def _account_spec_json() -> str:` returning `ResourceSpec.model_validate(yaml.safe_load(ACCOUNT_YAML)).model_dump_json()`
    - Add `async def test_dispatch_drives_account_pending_to_ready(db_session: AsyncSession) -> None:`:
      - Insert a ResourceRecord with id="account-id-001", team="toxins-research", kind="PECPAccount", name="aws-account-toxins-research", status="pending", spec_json=_account_spec_json()
      - `with patch("asyncio.sleep", return_value=None): await dispatch("account-id-001", db_session)`
      - Re-query the record, assert updated.status == ResourceStatus.ready.value
      - Assert `metadata = json.loads(updated.provider_metadata); metadata.get("account_id") == "123456789012"`
      - Assert `log = json.loads(updated.activity_log); any("aws organizations create-account" in entry for entry in log)`
      - This proves dispatch() routes PECPAccount through the registry, the inline 3s sleep is patched, and provider_metadata/activity_log persist correctly through the Dispatcher
    Do NOT add the manual ≥3s wall-time check to the automated suite — it is captured in `.planning/phases/02-core-engine/02-VALIDATION.md` Manual-Only Verifications and remains a one-time check on demo prep.
  </action>
  <verify>
    <automated>cd /Users/punitlad/projects/pecp-poc &amp;&amp; python -m pytest tests/test_adapters/mock/test_aws_account.py tests/test_dispatcher/test_dispatch.py -x -q &amp;&amp; ! grep "(placeholder" src/pecp/adapters/mock/aws_account.py &amp;&amp; python -m mypy src/pecp/adapters/mock/aws_account.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/pecp/adapters/mock/aws_account.py` contains `await asyncio.sleep(3)` (exact substring — the literal 3 is the KINDS-04 dwell)
    - `src/pecp/adapters/mock/aws_account.py` contains `class AwsAccountMockAdapter(AdapterBase):`
    - `src/pecp/adapters/mock/aws_account.py` contains the substring `"Would call: aws organizations create-account"`
    - `src/pecp/adapters/mock/aws_account.py` contains `account_id` and `account_email` and `account_name` and `management_console_url` keys
    - `src/pecp/adapters/mock/aws_account.py` does NOT contain `(placeholder`
    - `tests/test_adapters/mock/test_aws_account.py` contains `mock_sleep.call_args == call(3)` (the literal verification of the 3-second dwell value)
    - `tests/test_dispatcher/test_dispatch.py` contains a new test named `test_dispatch_drives_account_pending_to_ready`
    - `tests/test_dispatcher/test_dispatch.py` retains all 3 tests from Plan 02 (`test_dispatch_drives_pending_to_ready`, `test_dispatch_writes_provisioning_before_adapter_returns`, `test_dispatch_unknown_kind_writes_failed`)
    - `python -m pytest tests/test_adapters/mock/test_aws_account.py -x -q` exits 0 with 7 tests passing
    - `python -m pytest tests/test_dispatcher/test_dispatch.py -x -q` exits 0 with 4 tests passing (3 from Plan 02 + 1 new)
    - `python -m pytest tests/test_adapters/mock/test_aws_account.py tests/test_dispatcher/test_dispatch.py -q` reports total wall time < 2 seconds (sleep patched)
    - `python -m mypy src/pecp/adapters/mock/aws_account.py` exits 0
  </acceptance_criteria>
  <done>AwsAccountMockAdapter implements the KINDS-04 slow-path with the literal 3-second dwell, its unit tests verify the sleep target value and synthetic metadata shape, and the Dispatcher integration test proves the slow-path routes correctly through dispatch() — completing the AWS family.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adapter `provision()` arguments → ResourceSpec.spec | The spec subtype must be checked before access; mismatched subtypes return a failed result instead of raising (D-04, Pitfall 6) |
| `await asyncio.sleep(3)` in production AwsAccountMockAdapter | In real production usage (not tests), the adapter blocks for 3 seconds; this is a deliberate simulation, not a bug. Future real adapter implementations must add timeouts (INTG-V2-01) |
| `resource.metadata.team` value flowing into synthetic email and account name | `team` is a `str | None` field validated by Pydantic; injection of newlines or special characters into the synthetic `aws+{team}@example.com` string is bounded by Pydantic's string type but not by additional sanitization. PoC scope accepts this (the value is mock; no real email/AWS API is called) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-T01 | Tampering | AwsDataMockAdapter receives a DataServiceSpec with an unknown `subtype` enum value | mitigate | DataServiceSubtype is a Python Enum — Pydantic rejects values not in {s3, sqs, sns, rds, dynamodb} at spec validation time before the adapter is called. The adapter's if/elif chain falls through to a default `aws {subtype} provision` branch (PATTERNS.md branching example) — there is no "unknown" path because enum values are bounded |
| T-02-03-T02 | Tampering | AwsAccountMockAdapter receives a spec where `metadata.team` is a hostile string (e.g., `"foo\nbar"`) | accept | Pydantic accepts any string for `team` (PoC scope). The hostile string flows into the synthetic `aws+{team}@example.com` activity_log entry; `json.dumps` correctly escapes any control characters when the Dispatcher persists the log. No real AWS API call is made, so the impact is bounded to log readability |
| T-02-03-D01 | Denial of Service | AwsAccountMockAdapter's 3-second `asyncio.sleep` blocks the dispatch coroutine | accept | This is the intended behavior per KINDS-04 — simulating AWS Organizations create-account latency. In Phase 3, `BackgroundTasks` will run dispatch() out-of-band so the request thread does not block. The patched-zero-sleep in tests ensures the CI suite stays under 2 seconds |
| T-02-03-V01 | Input Validation (ASVS V5) | All three adapters receive a ResourceSpec of the wrong subtype | mitigate | Each adapter's first action is an `isinstance` check; mismatched specs return `ProvisionResult(status=failed, error="Unexpected spec type: ...")`. Tests `test_aws_*_rejects_non_*_spec_returns_failed` are the acceptance gates. This is defense-in-depth — the Dispatcher's ADAPTER_REGISTRY lookup by `spec.kind` should already prevent cross-routing |
| T-02-03-I01 | Information Disclosure | Synthetic AWS ARNs and account IDs in provider_metadata | accept | All values are constants (`123456789012`, fake bucket names) — no real AWS account IDs or ARNs are present. PoC explicitly mocks all backends |

ASVS Level 1 mapping:
- V5 Input Validation: covered by T-02-03-V01 (and DataServiceSubtype enum constraint)
- V2/V3/V4/V6: not applicable (adapters are internal Python objects)
</threat_model>

<verification>
- All three task `<automated>` blocks pass
- `python -m pytest tests/ -q` exits 0 across the entire repo (Plan 01 + 02 tests stay green; the 5 + 9 + 7 = 21 new adapter tests plus 1 new Dispatcher test are green)
- `python -m mypy src/` exits 0
- `python -m ruff check src/ tests/` exits 0
- `grep -r "(placeholder" src/pecp/adapters/mock/aws_*.py` produces no output (all 3 placeholders replaced)
- `grep -c "Would call: aws" src/pecp/adapters/mock/aws_data.py | grep -v '^#'` returns at least 5 (one per DataServiceSubtype branch)
- Wall time of full Phase 2 test suite remains under 5 seconds (asyncio.sleep patched everywhere; KINDS-04 manual check is the only place 3s dwell is observable, and that is excluded from CI)
</verification>

<success_criteria>
- KINDS-02: PECPContainer adapter implemented with image and exposure handling, deprovision and get_status conform to AdapterBase
- KINDS-03: PECPDataService adapter branches on all 5 DataServiceSubtype values, each producing service-specific activity_log and metadata
- KINDS-04: PECPAccount adapter uses literal `await asyncio.sleep(3)` inline; team-scoped synthetic email/name in provider_metadata; Dispatcher integration test proves end-to-end routing of the slow-path
- All three placeholder markers are gone from the 3 AWS adapter files
- Test suite remains green; mypy strict and ruff pass
- The MVP vertical slice now covers 4 of 10 adapter kinds end-to-end through dispatch() (Lambda + Container + Data + Account), proving the engine generalizes across the AWS family
</success_criteria>

<output>
Create `.planning/phases/02-core-engine/02-03-SUMMARY.md` when done. Capture: which AWS adapters are now real (with their key activity_log signatures), the 5 DataServiceSubtype branches in aws_data.py with their log/metadata pairs, the KINDS-04 sleep verification approach (`mock_sleep.call_args == call(3)`), and the new Dispatcher integration test name.
</output>
