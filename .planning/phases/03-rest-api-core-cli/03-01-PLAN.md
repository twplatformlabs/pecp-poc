---
phase: 03-rest-api-core-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pecp/persistence/models.py
  - src/pecp/models/resource_spec.py
  - alembic/env.py
  - alembic/versions/0002_add_env_notes_unique.py
  - tests/test_api/test_idempotency.py
  - tests/test_api/test_notes.py
  - tests/test_api/test_dispatch_wiring.py
  - tests/test_api/test_routes.py
  - tests/test_api/test_cli.py
autonomous: true
requirements:
  - CTRL-03
  - CTRL-04
  - CTRL-02
user_setup: []
tags:
  - schema-migration
  - alembic
  - wave-0-tests
  - test-scaffolds
  - resource-metadata
must_haves:
  truths:
    - "ResourceMetadata accepts an optional env field that round-trips from YAML"
    - "ResourceRecord rows expose nullable env and notes columns persisted to SQLite"
    - "Alembic upgrade head succeeds against SQLite and adds env, notes, and uq_resource_team_kind_name"
    - "Wave 0 test scaffolds exist and fail with importorskip or assertion (not collection errors) so Wave 2 can turn them green"
    - "render_as_batch=True is enabled in alembic/env.py so SQLite batch_alter_table works for constraint operations"
  artifacts:
    - path: "src/pecp/persistence/models.py"
      provides: "ResourceRecord with env, notes columns and __table_args__ UniqueConstraint(team, kind, name)"
      contains: "uq_resource_team_kind_name"
    - path: "src/pecp/models/resource_spec.py"
      provides: "ResourceMetadata with optional env: str | None field"
      contains: "env: str | None"
    - path: "alembic/versions/0002_add_env_notes_unique.py"
      provides: "Alembic migration adding env, notes columns and unique constraint via batch_alter_table"
      contains: "uq_resource_team_kind_name"
    - path: "alembic/env.py"
      provides: "Alembic env with render_as_batch=True"
      contains: "render_as_batch=True"
    - path: "tests/test_api/test_idempotency.py"
      provides: "Failing test scaffolds for CTRL-03 (no-op and update paths)"
      contains: "def test_"
    - path: "tests/test_api/test_notes.py"
      provides: "Failing test scaffolds for CTRL-04 (append note, 201, full list returned)"
      contains: "def test_"
    - path: "tests/test_api/test_dispatch_wiring.py"
      provides: "Failing test scaffolds for CTRL-02 (BackgroundTasks transitions status)"
      contains: "def test_"
  key_links:
    - from: "alembic/versions/0002_add_env_notes_unique.py"
      to: "src/pecp/persistence/models.py"
      via: "ORM __table_args__ mirrors DB-level UniqueConstraint"
      pattern: "uq_resource_team_kind_name"
    - from: "src/pecp/models/resource_spec.py (ResourceMetadata.env)"
      to: "src/pecp/persistence/models.py (ResourceRecord.env)"
      via: "Wave 2 route sets record.env = spec.metadata.env on create"
      pattern: "metadata.env"
---

## Phase Goal

**As a** platform engineer using PECP, **I want to** apply a YAML resource spec from my terminal, watch it transition from `pending` to `ready` on a running server, and review PE notes in `pecp status`, **so that** I can prove the control plane is reachable end-to-end without touching the database directly.

<objective>
This plan ships the data-layer vertical slice that unblocks the rest of Phase 3: the ORM gains the env + notes columns + a unique constraint on (team, kind, name), the YAML schema gains an optional metadata.env field, the Alembic migration applies cleanly to SQLite (which requires render_as_batch=True), and the three new failing test files exist so Wave 2 can drive them to green.

Purpose: Lock the schema contract that the Wave 2 server routes and Wave 3 CLI commands depend on. Establish the Wave 0 test scaffolds so the rest of the phase is verification-first.

Output:
- ResourceMetadata.env field (D-01)
- ResourceRecord.env, ResourceRecord.notes columns + UniqueConstraint(team, kind, name) (D-02, D-03, D-08)
- Alembic migration 0002_add_env_notes_unique.py with batch_alter_table for SQLite
- alembic/env.py upgrade adding render_as_batch=True (research Open Question 1)
- Three new failing test files (test_idempotency.py, test_notes.py, test_dispatch_wiring.py) and extensions to test_routes.py and test_cli.py covering the new routes and CLI commands
</objective>

## Artifacts this phase produces

The following symbols are introduced by Phase 3 and MUST be referenced as new (not drift) by source-grounding passes:

- `src/pecp/models/resource_spec.py`:
  - `ResourceMetadata.env: str | None` (new optional field)
- `src/pecp/persistence/models.py`:
  - `ResourceRecord.env: Mapped[str | None]` (new column)
  - `ResourceRecord.notes: Mapped[str | None]` (new column, JSON-list text)
  - `ResourceRecord.__table_args__` containing `UniqueConstraint("team", "kind", "name", name="uq_resource_team_kind_name")`
- `alembic/versions/0002_add_env_notes_unique.py`:
  - `revision = "0002"`, `down_revision = "0001"`, `upgrade()`, `downgrade()`
  - DB constraint name `uq_resource_team_kind_name`
- `tests/test_api/test_idempotency.py`:
  - `test_post_resources_same_spec_returns_existing_id`
  - `test_post_resources_changed_spec_updates_and_redispatches`
  - `test_post_resources_unique_constraint_blocks_duplicate_team_kind_name`
- `tests/test_api/test_notes.py`:
  - `test_post_notes_appends_and_returns_201_with_full_list`
  - `test_get_resource_id_includes_notes_list`
  - `test_post_notes_missing_text_returns_422`
- `tests/test_api/test_dispatch_wiring.py`:
  - `test_post_resources_enqueues_background_dispatch`
  - `test_dispatch_transitions_pending_to_ready_with_fresh_session`
- `tests/test_api/test_routes.py` (extended):
  - `test_get_resource_by_id_returns_full_record`
  - `test_get_resource_by_id_not_found_returns_404`
  - `test_delete_resource_returns_204_when_team_matches`
  - `test_delete_resource_returns_404_when_team_mismatch`
  - `test_list_resources_kind_filter`
- `tests/test_api/test_cli.py` (extended):
  - `test_get_command_renders_table_with_status_badge`
  - `test_status_command_renders_table_and_notes_block`
  - `test_status_command_no_notes_omits_block`
  - `test_delete_command_finds_id_then_deletes`
  - `test_delete_command_passes_team_query_param`

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-rest-api-core-cli/03-CONTEXT.md
@.planning/phases/03-rest-api-core-cli/03-RESEARCH.md
@.planning/phases/03-rest-api-core-cli/03-VALIDATION.md
@src/pecp/persistence/models.py
@src/pecp/models/resource_spec.py
@alembic/env.py
@alembic/versions/0001_add_provider_cols.py
@tests/conftest.py
@tests/test_api/test_routes.py
@tests/test_api/test_cli.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add env field to ResourceMetadata and env+notes columns + UniqueConstraint to ResourceRecord</name>
  <files>src/pecp/models/resource_spec.py, src/pecp/persistence/models.py</files>
  <read_first>
    - src/pecp/models/resource_spec.py (current ResourceMetadata with name + team fields, populate_by_name pattern)
    - src/pecp/persistence/models.py (current ResourceRecord with id, team, kind, name, status, spec_json, created_at, provider_metadata, activity_log)
    - .planning/phases/03-rest-api-core-cli/03-CONTEXT.md (D-01, D-02, D-03, D-08 lock the field and column shapes)
    - .planning/phases/03-rest-api-core-cli/03-RESEARCH.md Pattern 4 (UniqueConstraint via __table_args__)
  </read_first>
  <behavior>
    - ResourceMetadata accepts metadata.env as an optional string and round-trips via model_dump_json() (default None)
    - ResourceMetadata still accepts existing YAML payloads with no env field (back-compat — no test failure on existing Phase 1/2 YAML fixtures)
    - ResourceRecord exposes env (nullable Text) and notes (nullable Text, default "[]") columns
    - ResourceRecord declares __table_args__ = (UniqueConstraint("team", "kind", "name", name="uq_resource_team_kind_name"),)
    - mypy --strict and ruff pass against both files
  </behavior>
  <action>
    Add `env: str | None = None` to `ResourceMetadata` in `src/pecp/models/resource_spec.py` per D-01. Field name MUST be exactly `env` (lowercase, no alias) — the spec.metadata.env path is what the Wave 2 route reads. Keep `populate_by_name=True` semantics; do not add a Field alias for `env`.

    In `src/pecp/persistence/models.py` per D-02 and D-03:
    - Import `UniqueConstraint` from `sqlalchemy`.
    - Add `__table_args__ = (UniqueConstraint("team", "kind", "name", name="uq_resource_team_kind_name"),)` to `ResourceRecord` (D-08).
    - Add `env: Mapped[str | None] = mapped_column(Text, nullable=True)` (no default — distinct from `provider_metadata`/`activity_log` which default to "{}"/"[]" since env has no JSON structure).
    - Add `notes: Mapped[str | None] = mapped_column(Text, nullable=True, default="[]")` matching the activity_log JSON-text-column pattern.

    Do NOT modify existing columns, defaults, or the `Base` class. Do NOT add a Pydantic schema for notes here (that lives in the Wave 2 route handler).
  </action>
  <verify>
    <automated>cd /Users/punitlad/projects/pecp-poc && python -m mypy --strict src/pecp/models/resource_spec.py src/pecp/persistence/models.py && python -m ruff check src/pecp/models/resource_spec.py src/pecp/persistence/models.py && python -c "from pecp.models.resource_spec import ResourceMetadata; m = ResourceMetadata(name='x', env='dev'); assert m.env == 'dev'; m2 = ResourceMetadata(name='y'); assert m2.env is None" && python -c "from pecp.persistence.models import ResourceRecord; from sqlalchemy import UniqueConstraint; cons = [c for c in ResourceRecord.__table_args__ if isinstance(c, UniqueConstraint)]; assert cons and cons[0].name == 'uq_resource_team_kind_name'"</automated>
  </verify>
  <acceptance_criteria>
    - source: `src/pecp/models/resource_spec.py` contains the literal string `env: str | None = None`
    - source: `src/pecp/persistence/models.py` contains `UniqueConstraint("team", "kind", "name", name="uq_resource_team_kind_name")`
    - source: `src/pecp/persistence/models.py` contains `env: Mapped[str | None]` and `notes: Mapped[str | None]`
    - behavior: `ResourceMetadata(name="x", env="dev").env == "dev"`; `ResourceMetadata(name="y").env is None`
    - behavior: `mypy --strict` exits 0 against both files
    - behavior: `ruff check` exits 0 against both files
  </acceptance_criteria>
  <done>ResourceMetadata.env exists as Optional[str], ResourceRecord has env + notes columns + table-level UniqueConstraint, mypy --strict and ruff pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add render_as_batch=True to alembic/env.py and write migration 0002_add_env_notes_unique</name>
  <files>alembic/env.py, alembic/versions/0002_add_env_notes_unique.py</files>
  <read_first>
    - alembic/env.py (current async run_async_migrations + do_run_migrations without render_as_batch)
    - alembic/versions/0001_add_provider_cols.py (template for revision/down_revision/upgrade/downgrade structure)
    - .planning/phases/03-rest-api-core-cli/03-RESEARCH.md Pattern 3 (batch_alter_table example), Pitfall 2 (SQLite UniqueConstraint), Open Question 1 (render_as_batch decision)
    - src/pecp/persistence/models.py (final shape from Task 1 — column names, constraint name)
  </read_first>
  <behavior>
    - `context.configure(...)` in alembic/env.py is called with `render_as_batch=True` so SQLite batch operations succeed
    - Migration revision "0002" with down_revision "0001"
    - upgrade() adds the env column (Text, nullable, no server_default), the notes column (Text, nullable, server_default='[]'), and uq_resource_team_kind_name unique constraint via batch_alter_table
    - downgrade() drops the constraint then drops the columns (reverse order)
    - `alembic upgrade head` against a clean SQLite DB succeeds with no OperationalError
    - `alembic downgrade base` then `alembic upgrade head` round-trips cleanly
  </behavior>
  <action>
    In `alembic/env.py`, modify `do_run_migrations(connection)` to pass `render_as_batch=True` to `context.configure(...)`. Final call: `context.configure(connection=connection, target_metadata=target_metadata, render_as_batch=True)`. Do NOT modify imports, the async runner, the offline-mode guard, or DATABASE_URL resolution.

    Create `alembic/versions/0002_add_env_notes_unique.py` modeled on `0001_add_provider_cols.py`:
    - `revision = "0002"`, `down_revision = "0001"`, `branch_labels = None`, `depends_on = None`.
    - `upgrade()` calls:
      1. `op.add_column("resource_records", sa.Column("env", sa.Text(), nullable=True))` (no server_default — env is plain string, distinct from JSON columns).
      2. `op.add_column("resource_records", sa.Column("notes", sa.Text(), nullable=True, server_default="[]"))` (mirrors activity_log pattern).
      3. Inside `with op.batch_alter_table("resource_records") as batch_op: batch_op.create_unique_constraint("uq_resource_team_kind_name", ["team", "kind", "name"])`.
    - `downgrade()` reverses in order: drop constraint inside `batch_alter_table`, then `op.drop_column("resource_records", "notes")`, then `op.drop_column("resource_records", "env")`.

    Do NOT introduce a new revision number scheme. Do NOT touch `0001_add_provider_cols.py`.
  </action>
  <verify>
    <automated>cd /Users/punitlad/projects/pecp-poc && grep -F 'render_as_batch=True' alembic/env.py && grep -F 'revision = "0002"' alembic/versions/0002_add_env_notes_unique.py && grep -F 'down_revision = "0001"' alembic/versions/0002_add_env_notes_unique.py && grep -F 'uq_resource_team_kind_name' alembic/versions/0002_add_env_notes_unique.py && grep -F 'batch_alter_table' alembic/versions/0002_add_env_notes_unique.py && PECP_DATABASE_URL='sqlite+aiosqlite:///./tmp_test_phase3.db' python -m alembic upgrade head && PECP_DATABASE_URL='sqlite+aiosqlite:///./tmp_test_phase3.db' python -m alembic downgrade base && rm -f tmp_test_phase3.db</automated>
  </verify>
  <acceptance_criteria>
    - source: `alembic/env.py` contains `render_as_batch=True`
    - source: `alembic/versions/0002_add_env_notes_unique.py` contains `revision = "0002"`, `down_revision = "0001"`, `uq_resource_team_kind_name`, and `batch_alter_table`
    - behavior: `alembic upgrade head` against a fresh SQLite file exits 0
    - behavior: `alembic downgrade base` (full unwind) exits 0
    - behavior: re-running `alembic upgrade head` after downgrade exits 0 (round-trip)
  </acceptance_criteria>
  <done>render_as_batch=True is enabled in alembic/env.py; migration 0002 applies cleanly to SQLite, round-trips downgrade→upgrade, adds env + notes columns and uq_resource_team_kind_name.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add failing Wave 0 test scaffolds for CTRL-02, CTRL-03, CTRL-04, GET-by-id, DELETE, list kind filter, and new CLI commands</name>
  <files>tests/test_api/test_idempotency.py, tests/test_api/test_notes.py, tests/test_api/test_dispatch_wiring.py, tests/test_api/test_routes.py, tests/test_api/test_cli.py</files>
  <read_first>
    - tests/conftest.py (client fixture with importorskip on pecp.api.main; db_session fixture pattern)
    - tests/test_api/test_routes.py (existing GET /resources and POST /resources test patterns)
    - tests/test_api/test_cli.py (existing httpx.post mocking pattern with unittest.mock.patch and typer.testing.CliRunner)
    - .planning/phases/03-rest-api-core-cli/03-VALIDATION.md (Per-Task Verification Map shows exact test file → requirement mapping)
    - .planning/phases/03-rest-api-core-cli/03-CONTEXT.md (D-04, D-05 fix the notes route body shape and 201 response shape; D-09, D-10 fix the idempotency 202 response shape)
    - .planning/phases/03-rest-api-core-cli/03-RESEARCH.md Pattern 5 (notes append), Pattern 7 (CLI delete needs ID lookup then DELETE)
  </read_first>
  <behavior>
    - Five test files exist with tests that FAIL (assertion failures or 404/405, NOT collection errors). Failures must be due to missing route/CLI behavior, not import errors or syntax.
    - All tests use existing fixtures (`client` AsyncClient for HTTP, `runner = CliRunner()` for CLI, `httpx.post`/`httpx.get`/`httpx.delete` mocked via `unittest.mock.patch`).
    - `pytest tests/test_api/ -q` reports failures with reasons like "404 != 202" or "AssertionError: expected 'Notes:' in output" — NOT "ImportError" or "fixture not found".
    - Each test name encodes the exact behavior it verifies (matches the symbol list under "Artifacts this phase produces").
  </behavior>
  <action>
    Create three new files and extend two existing files. All tests MUST be discoverable (correct imports) and MUST FAIL when run (the routes/commands they exercise do not exist yet — that is intentional, this is Wave 0 RED).

    `tests/test_api/test_idempotency.py` (new). Use `client: AsyncClient` fixture. Three tests:
    1. `test_post_resources_same_spec_returns_existing_id`: POST a valid PECPLambda YAML twice with same `team=platform`. Assert second response has same `id` as first and status_code 202 (D-09 no-op path).
    2. `test_post_resources_changed_spec_updates_and_redispatches`: POST a PECPLambda YAML, then POST a YAML with same `metadata.name` and `team` but different `spec.exposure` (public vs private). Assert second response status_code 202, same `id`, and that the returned `status` is `"pending"` (D-10 reset).
    3. `test_post_resources_unique_constraint_blocks_duplicate_team_kind_name`: Use `db_session` fixture to insert two `ResourceRecord` rows with identical `(team, kind, name)` and different ids. Assert `IntegrityError` raised on commit (or that the second insert fails). Wrap the second insert/commit in a try/except expecting `sqlalchemy.exc.IntegrityError`.

    `tests/test_api/test_notes.py` (new). Three tests using `client`:
    1. `test_post_notes_appends_and_returns_201_with_full_list`: POST a resource, capture `id`, then POST `/resources/{id}/notes` with body `{"text": "rolling out v2"}`. Assert 201, JSON body has key `notes` which is a list with one entry containing `author`, `timestamp`, and `text` keys.
    2. `test_get_resource_id_includes_notes_list`: POST a resource, append a note, then GET `/resources/{id}`. Assert response includes `notes` key with the appended note.
    3. `test_post_notes_missing_text_returns_422`: POST `/resources/{id}/notes` with body `{"foo": "bar"}`. Assert 422 (Pydantic validation, NOT 500/KeyError — covers Pitfall 5).

    `tests/test_api/test_dispatch_wiring.py` (new). Two tests:
    1. `test_post_resources_enqueues_background_dispatch`: Use `monkeypatch` (pytest fixture) to patch `pecp.api.routes.resources._dispatch_with_session` (or whatever wrapper name is used) with a `MagicMock`. POST a valid resource. Assert the mock was called once with the new resource_id as its single argument.
    2. `test_dispatch_transitions_pending_to_ready_with_fresh_session`: Integration test. POST a PECPLambda resource via `client`, then poll `GET /resources/{id}` up to 30 times with `await asyncio.sleep(0.1)` between polls. Assert eventual `status == "ready"`. (Lambda mock adapter is the fastest path.)

    Extend `tests/test_api/test_routes.py` (append, do not modify existing tests):
    - `test_get_resource_by_id_returns_full_record`: POST a resource, GET `/resources/{id}`, assert response has keys `id`, `team`, `kind`, `name`, `status`, `env`, `created_at`, `provider_metadata`, `activity_log`, `notes`.
    - `test_get_resource_by_id_not_found_returns_404`: GET `/resources/nonexistent`, assert 404.
    - `test_delete_resource_returns_204_when_team_matches`: POST a resource with `team=platform`, DELETE `/resources/{id}?team=platform`, assert 204. Subsequent GET returns 404.
    - `test_delete_resource_returns_404_when_team_mismatch`: POST a resource with `team=platform`, DELETE `/resources/{id}?team=other`, assert 404 (Security Domain mitigation A5 — cross-team delete blocked).
    - `test_list_resources_kind_filter`: POST two resources with different kinds for the same team. GET `/resources?team=platform&kind=PECPLambda`, assert only the Lambda is returned (Open Question 2 — kind filter on list route).

    Extend `tests/test_api/test_cli.py` (append, do not modify existing tests). Use the existing `unittest.mock.patch("httpx.get", ...)` / `httpx.delete` pattern:
    - `test_get_command_renders_table_with_status_badge`: Mock `httpx.get` to return a list with two resources. Invoke `pecp get PECPLambda --team toxins-research --api-url http://t:8000`. Assert exit_code 0, `name` of each resource appears in `result.output`, and the table header `Status` is present.
    - `test_status_command_renders_table_and_notes_block`: Mock two `httpx.get` calls (list then detail). Detail response includes one note. Invoke `pecp status PECPLambda hello-world --team toxins-research --api-url http://t:8000`. Assert exit_code 0, `Notes:` appears in output, and the note text appears.
    - `test_status_command_no_notes_omits_block`: Same setup but detail response has empty notes list. Assert `Notes:` is NOT in output.
    - `test_delete_command_finds_id_then_deletes`: Mock `httpx.get` (list) and `httpx.delete`. Invoke `pecp delete PECPLambda hello-world --team toxins-research --api-url http://t:8000`. Assert exit_code 0 and that `httpx.delete` was called with a URL containing the resource id from the mocked list response.
    - `test_delete_command_passes_team_query_param`: Same as above but assert the DELETE call's `params` kwarg contains `team=toxins-research` (Security A5 enforced through CLI).

    All tests use the same YAML fixture style already in `test_cli.py` (multi-line bytes with apiVersion/kind/metadata/spec) and the same monkeypatching idioms. Do NOT implement the routes or CLI commands here — those land in Waves 2 and 3.
  </action>
  <verify>
    <automated>cd /Users/punitlad/projects/pecp-poc && python -m pytest tests/test_api/test_idempotency.py tests/test_api/test_notes.py tests/test_api/test_dispatch_wiring.py --collect-only -q && python -m pytest tests/test_api/ -q --no-header 2>&1 | grep -E '(passed|failed|error)' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - source: `tests/test_api/test_idempotency.py` defines `test_post_resources_same_spec_returns_existing_id`, `test_post_resources_changed_spec_updates_and_redispatches`, `test_post_resources_unique_constraint_blocks_duplicate_team_kind_name`
    - source: `tests/test_api/test_notes.py` defines `test_post_notes_appends_and_returns_201_with_full_list`, `test_get_resource_id_includes_notes_list`, `test_post_notes_missing_text_returns_422`
    - source: `tests/test_api/test_dispatch_wiring.py` defines `test_post_resources_enqueues_background_dispatch`, `test_dispatch_transitions_pending_to_ready_with_fresh_session`
    - source: `tests/test_api/test_routes.py` contains `test_get_resource_by_id_returns_full_record`, `test_get_resource_by_id_not_found_returns_404`, `test_delete_resource_returns_204_when_team_matches`, `test_delete_resource_returns_404_when_team_mismatch`, `test_list_resources_kind_filter`
    - source: `tests/test_api/test_cli.py` contains `test_get_command_renders_table_with_status_badge`, `test_status_command_renders_table_and_notes_block`, `test_status_command_no_notes_omits_block`, `test_delete_command_finds_id_then_deletes`, `test_delete_command_passes_team_query_param`
    - behavior: `pytest tests/test_api/test_idempotency.py tests/test_api/test_notes.py tests/test_api/test_dispatch_wiring.py --collect-only` exits 0 (tests discoverable, no syntax errors)
    - behavior: existing tests in `test_routes.py` and `test_cli.py` still pass (no regression to Phase 1/2)
    - behavior: new tests FAIL with assertion or 404/405 (not collection or import errors)
  </acceptance_criteria>
  <done>Five test files exist with the listed test functions. Tests are discoverable and either pass (those exercising Phase 1/2 behavior that already works, like list_resources_kind_filter base call) or fail with route-not-found / assertion errors (intentional RED). Wave 2 will turn them green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → SQLite DB | DDL applied via Alembic must not corrupt existing resource_records rows; must round-trip downgrade↔upgrade |
| YAML metadata.env → ORM column | New optional field flows from untrusted client YAML into a DB Text column |
| Test files → CI | New failing tests in the suite are intentional RED; must not block unrelated phases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01-01 | Tampering | Alembic migration on SQLite | mitigate | Use `batch_alter_table` for `create_unique_constraint`; set `render_as_batch=True` in `alembic/env.py` so migration applies on SQLite without OperationalError (Pitfall 2) |
| T-3-01-02 | Tampering | ResourceRecord uniqueness | mitigate | DB-level `UniqueConstraint("team", "kind", "name", name="uq_resource_team_kind_name")` enforces D-08 even under concurrent POSTs that bypass the route's lookup-before-insert check |
| T-3-01-03 | Information Disclosure | metadata.env from YAML | accept | env is a plain string set by the team; no cross-team leakage since env is owned by the same team that owns the resource. No sanitization required for PoC |
| T-3-01-SC | Tampering | No new packages | accept | Phase 3 introduces zero new dependencies (RESEARCH "Package Legitimacy Audit" — no entries). No supply-chain checkpoint required |
</threat_model>

<verification>
- `mypy --strict src/pecp/models/resource_spec.py src/pecp/persistence/models.py` passes
- `ruff check src/pecp/models/ src/pecp/persistence/ alembic/` passes
- `alembic upgrade head` against a fresh SQLite file succeeds
- `alembic downgrade base` then `alembic upgrade head` round-trips cleanly
- `pytest tests/test_api/ -q --collect-only` discovers all new tests without import errors
- Existing Phase 1/2 tests in `test_routes.py`, `test_cli.py`, `test_walking_skeleton.py` still pass
</verification>

<success_criteria>
- ResourceMetadata.env optional field exists (D-01) and round-trips
- ResourceRecord has env + notes columns + __table_args__ UniqueConstraint (D-02, D-03, D-08)
- alembic/versions/0002_add_env_notes_unique.py applies cleanly on SQLite using batch_alter_table (Pitfall 2)
- alembic/env.py has render_as_batch=True (Open Question 1 resolved)
- 5 test files contain the 15 named test functions; new tests are discoverable and FAIL (intentional RED for Waves 2 + 3 to turn green)
- Phase 1/2 test suite is still green (no regressions)
</success_criteria>

<output>
Create `.planning/phases/03-rest-api-core-cli/03-01-SUMMARY.md` when done, including:
- Final ResourceMetadata.env signature
- Final ResourceRecord.__table_args__ tuple
- Migration revision id and constraint name
- alembic/env.py render_as_batch=True line
- List of the 15 new test function names
- Any deviations from the plan (with rationale)
</output>
