apiVersion: pecp/v1
kind: PECPLambda
metadata:
  name: broken-lambda
  team: customer-product-app
  env: dev
spec:
  name: broken-lambda
  exposure: public
  api-gateway: /broken
  # source-code is intentionally omitted — this spec is invalid
